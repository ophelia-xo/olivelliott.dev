---
phase: 01-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - biome.json
  - vitest.config.ts
  - vitest.setup.ts
  - .gitignore
  - tests/tokens.test.ts
  - tests/contrast.test.ts
  - tests/layout.test.tsx
  - tests/providers.test.tsx
  - tests/motion-provider.test.ts
  - tests/fade-in.test.ts
  - tests/skip-link.test.tsx
  - tests/nav-link.test.tsx
autonomous: true
requirements:
  - FND-01
  - FND-04
user_setup: []

must_haves:
  truths:
    - "pnpm 9.15.9 is active via corepack; pnpm --version prints 9.15.9"
    - "Next.js 16.2 + React 19.2 + TypeScript 5.7 strict are installed"
    - "Biome 2.4 is installed and configured; ESLint/Prettier are purged"
    - "Vitest 3 harness is installed with jsdom env and path alias @/*"
    - "All Wave 0 test files exist and assert the contracts they must assert (tests fail until implementation, but they RUN — no MISSING references)"
    - "pnpm scripts exist: dev, build, start, lint, format, check, typecheck, test, test:ci"
  artifacts:
    - path: "package.json"
      provides: "Locked stack: next 16.2.x, react 19.2.x, motion 12.38+, tailwindcss 4.2+, geist 1.7+, zod 3.23+, typescript 5.7, biome 2.4, pnpm@9.15.9"
      contains: '"packageManager": "pnpm@9.15.9"'
    - path: "tsconfig.json"
      provides: "TS strict mode + @/* alias + noUncheckedIndexedAccess"
      contains: '"strict": true'
    - path: "next.config.ts"
      provides: "MDX wrapper skeleton (inert until Phase 2), Turbopack defaults, pageExtensions ts/tsx/md/mdx, poweredByHeader false"
      contains: "withMDX"
    - path: "biome.json"
      provides: "Lint + format single binary config, replaces ESLint/Prettier"
      contains: '"formatter"'
    - path: "vitest.config.ts"
      provides: "jsdom env, path alias, setup file reference"
      contains: "jsdom"
    - path: "tests/fade-in.test.ts"
      provides: "Contract test — FadeIn must be opacity-only, no transforms"
      contains: "opacity"
  key_links:
    - from: "package.json"
      to: "pnpm 9.15.9 (via corepack)"
      via: "packageManager field"
      pattern: "pnpm@9\\.15\\.9"
    - from: "vitest.config.ts"
      to: "@/* import resolution"
      via: "resolve.alias"
      pattern: "@/"
    - from: "next.config.ts"
      to: "@next/mdx"
      via: "withMDX wrapper"
      pattern: "createMDX"
---

<objective>
Install the locked stack exactly as pinned in 01-RESEARCH.md, replace ESLint/Prettier with Biome, and author the full Vitest test harness with every contract test the downstream waves depend on. After this plan, downstream plans can run their tests against failing (RED) specs and drive them to GREEN by implementing the spec'd components.

Purpose: Eliminate MISSING references across the phase. Every Wave 1–3 plan has a pre-authored test file in `tests/` that asserts its contract. Tests RED now, GREEN after implementation.

Output:
- Fully configured Next.js 16.2 / React 19.2 / TS 5.7 / Tailwind 4.2 / Motion 12 / Geist / Biome 2.4 / Vitest 3 project.
- 8 test files in `tests/` covering tokens, contrast, layout, providers, motion-provider, fade-in, skip-link, nav-link.
- `pnpm typecheck`, `pnpm check`, `pnpm test -- --run` all execute (tests may fail — that's intentional, they're spec'd against unimplemented components).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<interfaces>
<!-- Locked versions (from 01-RESEARCH.md "Standard Stack" — registry-verified 2026-04-20) -->
<!-- STACK.md pins are honored where they diverge from current registry. -->

Runtime deps (exact pins to use — copy verbatim):
- next@16.2.4
- react@19.2.5, react-dom@19.2.5
- motion@^12.38.0
- next-themes@^0.4.6
- geist@^1.7.0
- clsx@^2.1.1
- tailwind-merge@^3.5.0
- zod@^3.23.0          (NOT zod 4 — STACK.md locks 3.x; zod 4 has breaking API changes)
- lucide-react@^1.8.0
- @vercel/analytics@^2.0.1
- @vercel/speed-insights@^2.0.0

MDX (wire config only; no .mdx files yet):
- @next/mdx@^16.2.4
- @mdx-js/react@^3.1.1
- @mdx-js/loader@^3.1.1

Image pipeline:
- sharp@^0.34.5

Dev deps:
- @biomejs/biome@^2.4.12
- typescript@^5.7.0     (NOT typescript 6 — STACK.md locks 5.7; Biome 2.4 parser targets TS 5.7)
- @types/node, @types/react, @types/react-dom, @types/mdx

Tailwind (installed by create-next-app with --tailwind --turbopack):
- tailwindcss@^4.2.2
- @tailwindcss/postcss@^4.2.2

Test harness (Wave 0 installs):
- vitest@^3 @vitest/ui
- @testing-library/react @testing-library/jest-dom @testing-library/user-event
- jsdom

Package scripts (must be present in package.json):
- dev: "next dev"
- build: "next build"
- start: "next start"
- lint: "biome lint ."
- format: "biome format --write ."
- check: "biome check --write ."
- typecheck: "tsc --noEmit"
- test: "vitest"
- test:ci: "vitest run"

CSS variable / token keys that tests will assert (from UI-SPEC "Design Tokens: Single-file Reference"):
- --color-bg: #0a0a0a
- --color-surface-1: #0a0a0a
- --color-surface-2: #141414
- --color-hairline: #1f1f1f
- --color-text-primary: #f5f5f5
- --color-text-secondary: #a3a3a3
- --color-text-tertiary: #737373
- --color-text-on-accent: #0a0a0a
- --color-accent: #fbbf24
- --color-accent-hover: #f59e0b
- --color-danger: #f87171
- --motion-duration-fast: 120ms
- --motion-duration-base: 220ms
- --motion-duration-slow: 420ms
- --motion-ease-standard: cubic-bezier(0.22, 1, 0.36, 1)
- --radius-none: 0
- --radius-sm: 2px
- --radius-md: 6px
- --radius-lg: 12px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: Scaffold Next.js 16.2 + install locked deps + pin pnpm via corepack</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, next.config.ts, .gitignore, app/, postcss.config.mjs</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack, Installation Commands, Gap #1/#2/#3, Pattern 5, Pattern 6, Pitfall 5)
    - .planning/research/STACK.md (version locks)
    - CLAUDE.md (stack constraints)
  </read_first>
  <action>
    Pre-flight:
    1. Run `node --version` — confirm ≥ v20.18 (currently v22.18.0 per init).
    2. Run `corepack enable` then `corepack prepare pnpm@9.15.9 --activate`.
    3. Run `pnpm --version` — MUST print `9.15.9`. If not, abort and report.

    Scaffold (from repo root `/Users/olive/Documents/GitHub/portfolio` — directory already contains `.planning/`, `CLAUDE.md`, `Olive_Elliott_Resume.docx`):
    4. Run: `pnpm create next-app@16.2.4 . --typescript --tailwind --eslint --app --use-pnpm --turbopack --no-src-dir --import-alias "@/*"`
       - When asked to overwrite/merge into existing dir, accept. Do NOT delete `.planning/`, `CLAUDE.md`, or `Olive_Elliott_Resume.docx`.
       - We pass `--eslint` because create-next-app needs a lint choice; we remove ESLint in Task 0.2.

    Install runtime deps (pin versions — no `latest`):
    5. `pnpm add motion@^12.38.0 next-themes@^0.4.6 geist@^1.7.0 clsx@^2.1.1 tailwind-merge@^3.5.0 zod@^3.23.0 lucide-react@^1.8.0 @vercel/analytics@^2.0.1 @vercel/speed-insights@^2.0.0`
    6. `pnpm add @next/mdx@^16.2.4 @mdx-js/react@^3.1.1 @mdx-js/loader@^3.1.1`
    7. `pnpm add sharp@^0.34.5`

    Install dev deps:
    8. `pnpm add -D typescript@^5.7.0 @types/node @types/react @types/react-dom @types/mdx`
       - If TS > 5.7.x is already present from create-next-app, downgrade to 5.7.x per STACK.md Gap #2.

    Pin packageManager + Node engine (edit package.json):
    9. Set `"packageManager": "pnpm@9.15.9"` at the top level of package.json.
    10. Set `"engines": { "node": ">=20.18.0" }`.
    11. Ensure `"private": true`.
    12. Scripts block MUST contain exactly:
        ```
        "dev": "next dev",
        "build": "next build",
        "start": "next start",
        "lint": "biome lint .",
        "format": "biome format --write .",
        "check": "biome check --write .",
        "typecheck": "tsc --noEmit",
        "test": "vitest",
        "test:ci": "vitest run"
        ```
        (Biome scripts point to a config we'll create in Task 0.2.)

    Replace tsconfig.json with the Pattern 6 config from 01-RESEARCH.md (line ~580):
    13. Overwrite tsconfig.json with:
        ```jsonc
        {
          "compilerOptions": {
            "target": "ES2022",
            "lib": ["dom", "dom.iterable", "esnext"],
            "module": "esnext",
            "moduleResolution": "bundler",
            "allowJs": true,
            "skipLibCheck": true,
            "strict": true,
            "noUncheckedIndexedAccess": true,
            "noImplicitOverride": true,
            "noEmit": true,
            "esModuleInterop": true,
            "resolveJsonModule": true,
            "isolatedModules": true,
            "jsx": "preserve",
            "incremental": true,
            "plugins": [{ "name": "next" }],
            "paths": { "@/*": ["./*"] }
          },
          "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
          "exclude": ["node_modules", ".next", "out", ".vercel"]
        }
        ```

    Create next.config.ts (Pattern 5 from RESEARCH — MDX wrapper skeleton):
    14. Overwrite next.config.ts (or create if .js) with:
        ```ts
        import type { NextConfig } from 'next'
        import createMDX from '@next/mdx'

        const withMDX = createMDX({
          extension: /\.(md|mdx)$/,
          options: { remarkPlugins: [], rehypePlugins: [] },
        })

        const nextConfig: NextConfig = {
          pageExtensions: ['ts', 'tsx', 'md', 'mdx'],
          poweredByHeader: false,
          reactStrictMode: true,
        }

        export default withMDX(nextConfig)
        ```
    15. If a `next.config.js` was scaffolded, delete it.

    Update .gitignore — verify it contains `node_modules`, `.next`, `out`, `.vercel`, `coverage`, `*.tsbuildinfo`. Append any missing.

    Verify install:
    16. `pnpm why next motion tailwindcss geist zod typescript` — confirm versions match the pins above.
    17. `pnpm typecheck` — MUST exit 0 (empty scaffolded app + strict TS).
    18. `pnpm build` — MUST exit 0 (Turbopack default; placeholder Next home still renders).
  </action>
  <verify>
    <automated>pnpm --version | grep -q "^9\.15\.9$" && node -e "const p=require('./package.json'); if(p.packageManager!=='pnpm@9.15.9') throw new Error('packageManager not pinned'); if(!p.engines||!p.engines.node) throw new Error('engines.node missing'); for(const s of ['dev','build','start','lint','format','check','typecheck','test','test:ci']){ if(!p.scripts[s]) throw new Error('missing script: '+s); } console.log('OK')" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --version` prints exactly `9.15.9`
    - package.json contains `"packageManager": "pnpm@9.15.9"`
    - package.json `engines.node` >= 20.18.0
    - package.json `scripts` contains all 9 required entries
    - tsconfig.json contains `"strict": true` AND `"noUncheckedIndexedAccess": true` AND `"paths": { "@/*": ["./*"] }`
    - next.config.ts exists AND contains `createMDX` AND `poweredByHeader: false` AND `pageExtensions` includes `mdx`
    - `pnpm typecheck` exits 0
    - `pnpm why next` shows `next@16.2.4` present
    - `pnpm why motion` shows `motion@12.38.*` or later 12.x
    - `pnpm why typescript` shows `typescript@5.7.*` (NOT 6.x)
    - `pnpm why zod` shows `zod@3.*` (NOT 4.x)
    - `.planning/` directory still exists and is not overwritten
    - `CLAUDE.md` still exists at repo root
  </acceptance_criteria>
  <done>Locked stack installed per 01-RESEARCH.md; pnpm 9.15.9 pinned via corepack; TS strict + alias configured; MDX wrapper skeleton wired (inert); typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 0.2: Install Biome 2.4, delete ESLint/Prettier configs, author biome.json</name>
  <files>biome.json, .eslintrc.json (DELETE), eslint.config.mjs (DELETE), .prettierrc (DELETE), .prettierignore (DELETE), package.json</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4 — Biome config block, line ~480–520)
    - .planning/phases/01-foundation/01-CONTEXT.md (FND-04 decision)
    - package.json (current state after Task 0.1)
  </read_first>
  <action>
    Install Biome (dev dep):
    1. `pnpm add -D @biomejs/biome@^2.4.12`

    Create biome.json in repo root — copy VERBATIM from 01-RESEARCH.md Pattern 4:
    2. Write biome.json:
        ```jsonc
        {
          "$schema": "https://biomejs.dev/schemas/2.4.12/schema.json",
          "vcs": { "enabled": true, "clientKind": "git", "useIgnoreFile": true },
          "files": {
            "ignoreUnknown": false,
            "includes": ["**", "!**/node_modules", "!**/.next", "!**/out", "!**/.vercel", "!**/.planning"]
          },
          "formatter": {
            "enabled": true,
            "indentStyle": "space",
            "indentWidth": 2,
            "lineWidth": 100,
            "lineEnding": "lf"
          },
          "linter": {
            "enabled": true,
            "rules": {
              "recommended": true,
              "a11y": { "recommended": true },
              "correctness": { "recommended": true, "useExhaustiveDependencies": "warn" },
              "style": { "recommended": true, "useImportType": "error", "useConst": "error" },
              "suspicious": { "recommended": true, "noExplicitAny": "warn" },
              "nursery": { "useSortedClasses": { "level": "warn", "options": { "functions": ["clsx", "cn", "cva", "tw"] } } }
            }
          },
          "javascript": {
            "formatter": {
              "quoteStyle": "single",
              "semicolons": "asNeeded",
              "trailingCommas": "all",
              "jsxQuoteStyle": "double",
              "arrowParentheses": "always"
            }
          },
          "assist": {
            "enabled": true,
            "actions": { "source": { "organizeImports": "on" } }
          }
        }
        ```

    Purge ESLint/Prettier artifacts create-next-app dropped in:
    3. `rm -f .eslintrc.json .eslintrc.js eslint.config.mjs eslint.config.js .prettierrc .prettierrc.json .prettierrc.js .prettierignore`
    4. `pnpm remove eslint eslint-config-next prettier @eslint/eslintrc 2>/dev/null || true`
    5. If package.json still has an `eslintConfig` block or `"lint": "next lint"`, remove them. Lint script is now `"biome lint ."` (set in Task 0.1).

    Verify Biome runs:
    6. `pnpm check --write=false` — should run without crashing; may report lint warnings on scaffolded files (that's OK for Wave 0).
    7. `pnpm lint` — same.
  </action>
  <verify>
    <automated>test -f biome.json && test ! -f .eslintrc.json && test ! -f eslint.config.mjs && test ! -f .prettierrc && node -e "const b=require('./biome.json'); if(!b.formatter||!b.linter) throw new Error('biome.json missing formatter/linter'); console.log('OK')" && pnpm check --write=false > /tmp/biome-check.log 2>&1 && echo "biome ok" || grep -q "Checked" /tmp/biome-check.log</automated>
  </verify>
  <acceptance_criteria>
    - biome.json exists at repo root
    - biome.json contains `"formatter"` AND `"linter"` AND `"useSortedClasses"`
    - biome.json `formatter.lineWidth` is 100
    - biome.json `javascript.formatter.quoteStyle` is `single`
    - No `.eslintrc.*` files exist at repo root
    - No `eslint.config.*` files exist at repo root
    - No `.prettierrc*` or `.prettierignore` files exist at repo root
    - `package.json` does not contain `"eslint-config-next"` in deps or devDeps
    - `package.json` scripts.lint is `"biome lint ."` (not `"next lint"`)
    - `pnpm check --write=false` runs to completion (exit 0 OR exit 1 with lint warnings, but not parser crash)
  </acceptance_criteria>
  <done>Biome 2.4 is the sole lint/format tool. ESLint and Prettier artifacts purged from repo and package.json. FND-04 satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 0.3: Install Vitest harness and author all Wave 0 test files</name>
  <files>
    vitest.config.ts,
    vitest.setup.ts,
    tests/tokens.test.ts,
    tests/contrast.test.ts,
    tests/layout.test.tsx,
    tests/providers.test.tsx,
    tests/motion-provider.test.ts,
    tests/fade-in.test.ts,
    tests/skip-link.test.tsx,
    tests/nav-link.test.tsx
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (entire file — per-task verification map, Wave 0 Requirements)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Design Tokens: Single-file Reference, Verified pairings table, MotionProvider contract, FadeIn contract, SkipLink spec, NavLink spec)
    - .planning/phases/01-foundation/01-RESEARCH.md (Validation Architecture, Wave 0 Gaps)
  </read_first>
  <behavior>
    All tests are authored to RED (fail) against unimplemented components. Downstream Wave 1/2 plans drive them to GREEN. No watch-mode flags — all commands use `--run` for determinism.

    Per-file test expectations:
    - tests/tokens.test.ts: parse `styles/tokens.css`; assert every token from the Design Tokens table exists with exact value. Fails if file doesn't exist (RED until Plan 01 creates it).
    - tests/contrast.test.ts: pure WCAG ratio function; assert each AA-pairing from UI-SPEC "Verified pairings" passes ≥ 4.5:1. Does NOT depend on implementation files — passes as soon as Vitest is installed.
    - tests/layout.test.tsx: render root layout; assert `<html>` has both `GeistSans.variable` and `GeistMono.variable` class names; assert `<main id="main">` exists. RED until Plan 02.
    - tests/providers.test.tsx: assert `Providers` source contains `defaultTheme="dark"` AND `enableSystem={false}`. RED until Plan 02.
    - tests/motion-provider.test.ts: read `components/motion/motion-provider.tsx` as string; assert `reducedMotion="user"`, `features={domAnimation}`, `strict`. RED until Plan 03.
    - tests/fade-in.test.ts: read `components/motion/fade-in.tsx` as string; assert `opacity: 0` AND `opacity: 1` present; assert NONE of `y:`, `x:`, `scale:`, `rotate:`, `rotateX:`, `rotateY:`, `skewX:`, `skewY:` appear in motion props. RED until Plan 03.
    - tests/skip-link.test.tsx: render `<SkipLink>`; assert `href="#main"`, default class includes `sr-only`, focus reveals via `focus:not-sr-only` class. RED until Plan 04.
    - tests/nav-link.test.tsx: render `<NavLink>` active (usePathname mock) and inactive; assert active state contains `border-accent` or `border-b-[1px]` class AND `aria-current="page"`. RED until Plan 04.
  </behavior>
  <action>
    Install harness:
    1. `pnpm add -D vitest@^3 @vitest/ui @testing-library/react@^16 @testing-library/jest-dom@^6 @testing-library/user-event@^14 jsdom@^25`

    Create vitest.config.ts:
    2. Write:
        ```ts
        import { defineConfig } from 'vitest/config'
        import path from 'node:path'

        export default defineConfig({
          test: {
            environment: 'jsdom',
            globals: true,
            setupFiles: ['./vitest.setup.ts'],
            include: ['tests/**/*.test.{ts,tsx}'],
          },
          resolve: {
            alias: { '@': path.resolve(__dirname, '.') },
          },
        })
        ```

    Create vitest.setup.ts:
    3. Write:
        ```ts
        import '@testing-library/jest-dom/vitest'
        ```

    Create tests/tokens.test.ts — parses styles/tokens.css as string, asserts every key from UI-SPEC Design Tokens block:
    4. Write:
        ```ts
        import { readFileSync, existsSync } from 'node:fs'
        import path from 'node:path'
        import { describe, it, expect } from 'vitest'

        const TOKENS_PATH = path.resolve(__dirname, '../styles/tokens.css')

        const REQUIRED_TOKENS: Record<string, string> = {
          '--color-bg': '#0a0a0a',
          '--color-surface-1': '#0a0a0a',
          '--color-surface-2': '#141414',
          '--color-hairline': '#1f1f1f',
          '--color-text-primary': '#f5f5f5',
          '--color-text-secondary': '#a3a3a3',
          '--color-text-tertiary': '#737373',
          '--color-text-on-accent': '#0a0a0a',
          '--color-accent': '#fbbf24',
          '--color-accent-hover': '#f59e0b',
          '--color-danger': '#f87171',
          '--motion-duration-fast': '120ms',
          '--motion-duration-base': '220ms',
          '--motion-duration-slow': '420ms',
          '--motion-ease-standard': 'cubic-bezier(0.22, 1, 0.36, 1)',
          '--radius-none': '0',
          '--radius-sm': '2px',
          '--radius-md': '6px',
          '--radius-lg': '12px',
          '--font-weight-regular': '400',
          '--font-weight-medium': '500',
          '--tracking-tight': '-0.02em',
          '--tracking-normal': '0em',
          '--tracking-wide': '0.02em',
          '--spacing': '0.25rem',
        }

        describe('styles/tokens.css — UI-SPEC Design Tokens contract', () => {
          it('exists', () => {
            expect(existsSync(TOKENS_PATH)).toBe(true)
          })

          it('declares @theme block', () => {
            const source = readFileSync(TOKENS_PATH, 'utf-8')
            expect(source).toMatch(/@theme\s*\{/)
          })

          for (const [name, value] of Object.entries(REQUIRED_TOKENS)) {
            it(`declares ${name}: ${value}`, () => {
              const source = readFileSync(TOKENS_PATH, 'utf-8')
              // Allow whitespace variance around ':' and ';'
              const pattern = new RegExp(
                `${name.replace(/-/g, '-')}\\s*:\\s*${value.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')}\\s*;`,
              )
              expect(source).toMatch(pattern)
            })
          }

          it('contains type-scale tokens (text-body, text-label, text-display)', () => {
            const source = readFileSync(TOKENS_PATH, 'utf-8')
            expect(source).toMatch(/--text-body\s*:\s*1rem/)
            expect(source).toMatch(/--text-label\s*:\s*0\.875rem/)
            expect(source).toMatch(/--text-display\s*:\s*clamp\(\s*2rem\s*,\s*5vw\s*,\s*3rem\s*\)/)
          })

          it('has no indigo/violet/purple color tokens (AI-aesthetic tell)', () => {
            const source = readFileSync(TOKENS_PATH, 'utf-8').toLowerCase()
            expect(source).not.toMatch(/--color-(indigo|violet|purple)/)
          })
        })
        ```

    Create tests/contrast.test.ts — pure math, no implementation dependency:
    5. Write:
        ```ts
        import { describe, it, expect } from 'vitest'

        function hexToRgb(hex: string): [number, number, number] {
          const h = hex.replace('#', '')
          const bigint = parseInt(h, 16)
          return [(bigint >> 16) & 255, (bigint >> 8) & 255, bigint & 255]
        }

        function luminance([r, g, b]: [number, number, number]): number {
          const a = [r, g, b].map((v) => {
            const s = v / 255
            return s <= 0.03928 ? s / 12.92 : ((s + 0.055) / 1.055) ** 2.4
          })
          return 0.2126 * a[0]! + 0.7152 * a[1]! + 0.0722 * a[2]!
        }

        function contrastRatio(a: string, b: string): number {
          const la = luminance(hexToRgb(a))
          const lb = luminance(hexToRgb(b))
          const [light, dark] = la > lb ? [la, lb] : [lb, la]
          return (light + 0.05) / (dark + 0.05)
        }

        // Pairings from UI-SPEC "Verified pairings" table
        const AA_PAIRS: Array<[string, string, string, number]> = [
          ['text-primary on bg', '#f5f5f5', '#0a0a0a', 15], // ~18.4:1
          ['text-secondary on bg', '#a3a3a3', '#0a0a0a', 7], // ~8.4:1
          ['text-tertiary on bg', '#737373', '#0a0a0a', 4.5], // ~4.8:1
          ['accent on bg', '#fbbf24', '#0a0a0a', 10], // ~11.8:1
          ['accent-hover on bg', '#f59e0b', '#0a0a0a', 8], // ~9.7:1
          ['text-on-accent on accent', '#0a0a0a', '#fbbf24', 10], // ~11.8:1
        ]

        describe('Color token pairs — WCAG AA contrast (UI-SPEC Verified pairings)', () => {
          for (const [name, fg, bg, minRatio] of AA_PAIRS) {
            it(`${name} >= ${minRatio}:1`, () => {
              const ratio = contrastRatio(fg, bg)
              expect(ratio).toBeGreaterThanOrEqual(minRatio)
            })
          }

          it('text-primary on accent is KNOWN FAIL (must never be paired in source)', () => {
            // UI-SPEC explicitly marks #f5f5f5 on #fbbf24 as FAIL — encoded as a contract
            const ratio = contrastRatio('#f5f5f5', '#fbbf24')
            expect(ratio).toBeLessThan(3)
          })
        })
        ```

    Create tests/layout.test.tsx — RED until Plan 02:
    6. Write:
        ```tsx
        import { describe, it, expect } from 'vitest'
        import { readFileSync, existsSync } from 'node:fs'
        import path from 'node:path'

        const LAYOUT_PATH = path.resolve(__dirname, '../app/layout.tsx')

        describe('app/layout.tsx — Root layout contract (FND-03)', () => {
          it('exists', () => {
            expect(existsSync(LAYOUT_PATH)).toBe(true)
          })

          it('imports GeistSans from geist/font/sans', () => {
            const src = readFileSync(LAYOUT_PATH, 'utf-8')
            expect(src).toMatch(/from\s+['"]geist\/font\/sans['"]/)
            expect(src).toMatch(/GeistSans/)
          })

          it('imports GeistMono from geist/font/mono', () => {
            const src = readFileSync(LAYOUT_PATH, 'utf-8')
            expect(src).toMatch(/from\s+['"]geist\/font\/mono['"]/)
            expect(src).toMatch(/GeistMono/)
          })

          it('applies both font variable classes to <html>', () => {
            const src = readFileSync(LAYOUT_PATH, 'utf-8')
            expect(src).toMatch(/GeistSans\.variable/)
            expect(src).toMatch(/GeistMono\.variable/)
          })

          it('sets suppressHydrationWarning on <html> (next-themes requirement)', () => {
            const src = readFileSync(LAYOUT_PATH, 'utf-8')
            expect(src).toMatch(/suppressHydrationWarning/)
          })

          it('sets lang="en" on <html>', () => {
            const src = readFileSync(LAYOUT_PATH, 'utf-8')
            expect(src).toMatch(/lang=["']en["']/)
          })

          it('exports metadata with metadataBase', () => {
            const src = readFileSync(LAYOUT_PATH, 'utf-8')
            expect(src).toMatch(/metadata/)
            expect(src).toMatch(/metadataBase/)
          })
        })

        describe('app/(site)/layout.tsx — Site layout contract (FND-08)', () => {
          const SITE_LAYOUT = path.resolve(__dirname, '../app/(site)/layout.tsx')

          it('exists', () => {
            expect(existsSync(SITE_LAYOUT)).toBe(true)
          })

          it('renders <main id="main"> for skip-link target', () => {
            const src = readFileSync(SITE_LAYOUT, 'utf-8')
            expect(src).toMatch(/<main[^>]*id=["']main["']/)
          })
        })
        ```

    Create tests/providers.test.tsx — RED until Plan 02:
    7. Write:
        ```tsx
        import { describe, it, expect } from 'vitest'
        import { readFileSync, existsSync } from 'node:fs'
        import path from 'node:path'

        const PROVIDERS_PATH = path.resolve(__dirname, '../app/providers.tsx')

        describe('app/providers.tsx — next-themes configuration (FND-06)', () => {
          it('exists', () => {
            expect(existsSync(PROVIDERS_PATH)).toBe(true)
          })

          it('is a client component', () => {
            const src = readFileSync(PROVIDERS_PATH, 'utf-8')
            expect(src).toMatch(/^\s*['"]use client['"]/)
          })

          it('configures ThemeProvider with defaultTheme="dark"', () => {
            const src = readFileSync(PROVIDERS_PATH, 'utf-8')
            expect(src).toMatch(/defaultTheme=["']dark["']/)
          })

          it('configures enableSystem={false} (v1 is dark-only)', () => {
            const src = readFileSync(PROVIDERS_PATH, 'utf-8')
            expect(src).toMatch(/enableSystem=\{false\}/)
          })

          it('configures attribute="class"', () => {
            const src = readFileSync(PROVIDERS_PATH, 'utf-8')
            expect(src).toMatch(/attribute=["']class["']/)
          })

          it('configures disableTransitionOnChange', () => {
            const src = readFileSync(PROVIDERS_PATH, 'utf-8')
            expect(src).toMatch(/disableTransitionOnChange/)
          })
        })
        ```

    Create tests/motion-provider.test.ts — RED until Plan 03:
    8. Write:
        ```ts
        import { describe, it, expect } from 'vitest'
        import { readFileSync, existsSync } from 'node:fs'
        import path from 'node:path'

        const MP_PATH = path.resolve(__dirname, '../components/motion/motion-provider.tsx')

        describe('components/motion/motion-provider.tsx (FND-05)', () => {
          it('exists', () => {
            expect(existsSync(MP_PATH)).toBe(true)
          })

          it('is a client component', () => {
            const src = readFileSync(MP_PATH, 'utf-8')
            expect(src).toMatch(/^\s*['"]use client['"]/)
          })

          it('imports from motion/react (not framer-motion)', () => {
            const src = readFileSync(MP_PATH, 'utf-8')
            expect(src).toMatch(/from\s+['"]motion\/react['"]/)
            expect(src).not.toMatch(/from\s+['"]framer-motion['"]/)
          })

          it('configures MotionConfig with reducedMotion="user"', () => {
            const src = readFileSync(MP_PATH, 'utf-8')
            expect(src).toMatch(/reducedMotion=["']user["']/)
          })

          it('wraps children in LazyMotion with features={domAnimation}', () => {
            const src = readFileSync(MP_PATH, 'utf-8')
            expect(src).toMatch(/LazyMotion/)
            expect(src).toMatch(/features=\{domAnimation\}/)
          })

          it('enables strict mode on LazyMotion (enforces <m.*> over <motion.*>)', () => {
            const src = readFileSync(MP_PATH, 'utf-8')
            expect(src).toMatch(/\bstrict\b/)
          })
        })
        ```

    Create tests/fade-in.test.ts — RED until Plan 03; this is the OPACITY-ONLY contract:
    9. Write:
        ```ts
        import { describe, it, expect } from 'vitest'
        import { readFileSync, existsSync } from 'node:fs'
        import path from 'node:path'

        const FI_PATH = path.resolve(__dirname, '../components/motion/fade-in.tsx')

        describe('components/motion/fade-in.tsx — opacity-only contract (FND-05, UI-SPEC line 276)', () => {
          it('exists', () => {
            expect(existsSync(FI_PATH)).toBe(true)
          })

          it('is a client component', () => {
            const src = readFileSync(FI_PATH, 'utf-8')
            expect(src).toMatch(/^\s*['"]use client['"]/)
          })

          it('imports { m } from motion/react (NOT { motion })', () => {
            const src = readFileSync(FI_PATH, 'utf-8')
            expect(src).toMatch(/import\s*\{\s*m\s*\}\s*from\s+['"]motion\/react['"]/)
            // Must not use the heavy `motion` import
            expect(src).not.toMatch(/import\s*\{\s*motion\s*\}\s*from\s+['"]motion\/react['"]/)
          })

          it('contains opacity: 0 in initial', () => {
            const src = readFileSync(FI_PATH, 'utf-8')
            expect(src).toMatch(/opacity:\s*0\b/)
          })

          it('contains opacity: 1 in animate', () => {
            const src = readFileSync(FI_PATH, 'utf-8')
            expect(src).toMatch(/opacity:\s*1\b/)
          })

          it('does NOT contain any transform keys (y, x, scale, rotate, skew)', () => {
            const src = readFileSync(FI_PATH, 'utf-8')
            // These are motion prop keys; presence means a transform is animated
            expect(src).not.toMatch(/\by:\s*-?\d/)
            expect(src).not.toMatch(/\bx:\s*-?\d/)
            expect(src).not.toMatch(/\bscale:\s*-?\d/)
            expect(src).not.toMatch(/\brotate:\s*-?\d/)
            expect(src).not.toMatch(/\brotateX:/)
            expect(src).not.toMatch(/\brotateY:/)
            expect(src).not.toMatch(/\bskewX:/)
            expect(src).not.toMatch(/\bskewY:/)
          })

          it('uses UI-SPEC motion duration 0.22s and ease [0.22, 1, 0.36, 1]', () => {
            const src = readFileSync(FI_PATH, 'utf-8')
            expect(src).toMatch(/duration:\s*0\.22/)
            expect(src).toMatch(/\[\s*0\.22\s*,\s*1\s*,\s*0\.36\s*,\s*1\s*\]/)
          })
        })
        ```

    Create tests/skip-link.test.tsx — RED until Plan 04:
    10. Write:
        ```tsx
        import { describe, it, expect } from 'vitest'
        import { render, screen } from '@testing-library/react'
        import { existsSync } from 'node:fs'
        import path from 'node:path'

        const SL_PATH = path.resolve(__dirname, '../components/site/skip-link.tsx')

        describe('<SkipLink /> (FND-08)', () => {
          it('source file exists', () => {
            expect(existsSync(SL_PATH)).toBe(true)
          })

          it('renders an anchor targeting #main', async () => {
            const mod = await import('../components/site/skip-link')
            render(<mod.SkipLink />)
            const link = screen.getByRole('link', { name: /skip to content/i })
            expect(link).toHaveAttribute('href', '#main')
          })

          it('default class includes sr-only', async () => {
            const mod = await import('../components/site/skip-link')
            render(<mod.SkipLink />)
            const link = screen.getByRole('link', { name: /skip to content/i })
            expect(link.className).toMatch(/\bsr-only\b/)
          })

          it('reveals on focus via focus:not-sr-only utility', async () => {
            const mod = await import('../components/site/skip-link')
            render(<mod.SkipLink />)
            const link = screen.getByRole('link', { name: /skip to content/i })
            expect(link.className).toMatch(/\bfocus:not-sr-only\b/)
          })

          it('uses accent focus outline', async () => {
            const mod = await import('../components/site/skip-link')
            render(<mod.SkipLink />)
            const link = screen.getByRole('link', { name: /skip to content/i })
            expect(link.className).toMatch(/focus:(outline-accent|text-accent)/)
          })
        })
        ```

    Create tests/nav-link.test.tsx — RED until Plan 04; mocks next/navigation:
    11. Write:
        ```tsx
        import { describe, it, expect, vi, beforeEach } from 'vitest'
        import { render, screen } from '@testing-library/react'
        import { existsSync } from 'node:fs'
        import path from 'node:path'

        const NL_PATH = path.resolve(__dirname, '../components/site/nav-link.tsx')

        let mockPathname = '/'
        vi.mock('next/navigation', () => ({
          usePathname: () => mockPathname,
        }))

        describe('<NavLink /> (FND-08 — active route detection)', () => {
          beforeEach(() => {
            mockPathname = '/'
          })

          it('source file exists', () => {
            expect(existsSync(NL_PATH)).toBe(true)
          })

          it('is a client component', async () => {
            const { readFileSync } = await import('node:fs')
            const src = readFileSync(NL_PATH, 'utf-8')
            expect(src).toMatch(/^\s*['"]use client['"]/)
          })

          it('renders label and href', async () => {
            const mod = await import('../components/site/nav-link')
            render(<mod.NavLink href="/projects" label="projects" />)
            const link = screen.getByRole('link', { name: /projects/i })
            expect(link).toHaveAttribute('href', '/projects')
          })

          it('marks active route with aria-current="page" when pathname matches href', async () => {
            mockPathname = '/projects'
            const mod = await import('../components/site/nav-link')
            render(<mod.NavLink href="/projects" label="projects" />)
            const link = screen.getByRole('link', { name: /projects/i })
            expect(link).toHaveAttribute('aria-current', 'page')
          })

          it('does not mark aria-current on inactive route', async () => {
            mockPathname = '/'
            const mod = await import('../components/site/nav-link')
            render(<mod.NavLink href="/projects" label="projects" />)
            const link = screen.getByRole('link', { name: /projects/i })
            expect(link).not.toHaveAttribute('aria-current', 'page')
          })
        })
        ```

    Sanity verification:
    12. `pnpm test -- --run` — MUST execute (no config errors). Expected results:
        - tests/contrast.test.ts: all PASS (pure math).
        - All other test files: FAIL with "file does not exist" or "cannot resolve module" — that's intentional RED. They drive downstream plans.
        - NO test file should fail due to parse/syntax errors in the test file itself.

    Add a README comment at top of each spec file noting "RED-until-{Plan-ID}" to help executors trace causality.
  </action>
  <verify>
    <automated>test -f vitest.config.ts && test -f vitest.setup.ts && test -f tests/tokens.test.ts && test -f tests/contrast.test.ts && test -f tests/layout.test.tsx && test -f tests/providers.test.tsx && test -f tests/motion-provider.test.ts && test -f tests/fade-in.test.ts && test -f tests/skip-link.test.tsx && test -f tests/nav-link.test.tsx && pnpm test -- --run tests/contrast.test.ts 2>&1 | grep -qE "(Tests|passed)"</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` contains `environment: 'jsdom'` AND alias for `@`
    - `vitest.setup.ts` imports `@testing-library/jest-dom/vitest`
    - All 8 test files exist under `tests/`
    - `tests/fade-in.test.ts` contains assertion strings `opacity: 0`, `opacity: 1`, AND negative assertions for `y:`, `x:`, `scale:`, `rotate:`
    - `tests/contrast.test.ts` PASSES (pure math, no external deps)
    - `tests/tokens.test.ts` contains assertions for every token from UI-SPEC Design Tokens table
    - `tests/motion-provider.test.ts` asserts `reducedMotion="user"` AND `features={domAnimation}` AND `strict`
    - `pnpm test -- --run` exits (either 0 or 1 with failures) — does NOT crash on config
    - package.json scripts.test is `"vitest"` (already set in Task 0.1)
  </acceptance_criteria>
  <done>Vitest harness wired. All 8 Wave 0 test files exist and RUN. Contrast test passes; component-contract tests RED until downstream plans implement their targets. Zero MISSING references remain.</done>
</task>

</tasks>

<verification>
- `pnpm --version` prints `9.15.9`
- `pnpm typecheck` exits 0
- `pnpm check --write=false` runs to completion (warnings allowed, no parser crash)
- `pnpm test -- --run tests/contrast.test.ts` passes
- `pnpm test -- --run` executes all test files (failures on RED spec files are EXPECTED)
- No ESLint/Prettier config files remain at repo root
- biome.json, vitest.config.ts, tsconfig.json, next.config.ts all present and well-formed
- package.json has `packageManager: pnpm@9.15.9` and all 9 required scripts
- `.planning/` and `CLAUDE.md` were NOT clobbered by create-next-app
</verification>

<success_criteria>
Phase 1 Wave 0 is complete when:
- Locked stack installed (Next 16.2.4, React 19.2.5, TS 5.7.x, Tailwind 4.2.x, Motion 12.38+, Geist 1.7+, Zod 3.23+, Biome 2.4.x).
- pnpm 9.15.9 is active + pinned in package.json.
- Biome is the sole lint/format tool; ESLint + Prettier are purged.
- Vitest 3 harness + 8 test files exist; contrast.test.ts passes; other specs RED as expected until Wave 1/2 implements their targets.
- Every test referenced by 01-VALIDATION.md has a corresponding file on disk — zero MISSING references across the phase.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-00-SUMMARY.md` documenting: exact versions installed, files created, scripts added, and current test-suite status (how many RED vs GREEN and why).
</output>
