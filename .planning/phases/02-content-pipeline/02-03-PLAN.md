---
phase: 02-content-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - content/projects/myco.mdx
  - tests/content/content-load.test.ts
autonomous: false
requirements: [CNT-05]
user_setup:
  - task: "Locate Myco README"
    why: "CNT-05 requires authoring myco.mdx from the existing README; executor must read the README's prose voice before drafting"
    instructions: |
      Myco repo is at https://github.com/olive-elliott/myco. The README is the canonical source.
      Two strategies:
        1. If the Myco repo is checked out at ~/Documents/GitHub/myco (or similar), read README.md directly.
        2. Otherwise fetch via gh CLI: `gh repo view olive-elliott/myco --json description,readme | jq -r '.readme'`
           OR clone shallow: `git clone --depth 1 https://github.com/olive-elliott/myco /tmp/myco && cat /tmp/myco/README.md`
      If neither path resolves (private repo, repo rename, unauthenticated gh), STOP and ask the user for the README path or paste.
must_haves:
  truths:
    - "content/projects/myco.mdx exists at repo root and is authored from the Myco README"
    - "Myco MDX passes ProjectFrontmatterSchema.parse (via the Wave 2 loader at build time)"
    - "Body contains explicit H2 headings: ## Problem, ## Approach, ## Outcome"
    - "Body word count is 800–1200 words"
    - "Frontmatter values match CONTEXT.md locks: tier=hero, visibility=public, status=active, year=2025, order=10"
    - "Outcomes bullets are honest — no fabricated metrics or invented numbers"
    - "Tags include local-first, autonomous, open-source, ai, agents, typescript"
    - "Hero image is a placeholder path with a clear alt describing it as a placeholder"
    - "A human-verify checkpoint confirms editorial quality (voice, honesty, flow) before completion"
  artifacts:
    - path: "content/projects/myco.mdx"
      provides: "First real project case study; canonical fixture proving the pipeline against real content"
      contains: "## Problem"
  key_links:
    - from: "content/projects/myco.mdx"
      to: "lib/schemas.ts"
      via: "frontmatter validated by ProjectFrontmatterSchema at lib/content.ts import time"
      pattern: "slug:\\s*myco"
    - from: "content/projects/myco.mdx"
      to: "lib/content.ts"
      via: "file picked up by loadAll via fs.readdirSync on content/projects/"
      pattern: "myco.mdx"
---

<objective>
Author `content/projects/myco.mdx` — the first real project MDX, drafted from Myco's existing README and shaped into Problem → Approach → Outcome narrative (CNT-05). This is the canonical fixture that proves the Wave 2 pipeline on real content. Extend `content-load.test.ts` with integration assertions that bind to the real `content/projects/` directory (via `_loadForTests(CONTENT_DIR)`) so future phases have a regression gate against the real file.

Purpose: Unlock Phase 3. The project detail page template (`/projects/[slug]`) renders MDX bodies; without a real MDX file, Phase 3 has nothing to render. Also validates end-to-end that (a) YAML frontmatter passes the schema against a real-world shape, (b) `remark-frontmatter` from Wave 0 hides the YAML block from Phase 3's future body render, (c) content honesty is enforced by an explicit human-verify checkpoint before the file is accepted.

Output: One authored MDX (~800–1200 words), extended integration tests, one checkpoint gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-pipeline/02-CONTEXT.md
@.planning/phases/02-content-pipeline/02-RESEARCH.md
@.planning/phases/02-content-pipeline/02-VALIDATION.md
@.planning/phases/02-content-pipeline/02-01-SUMMARY.md
@.planning/phases/02-content-pipeline/02-02-SUMMARY.md
@lib/schemas.ts
@lib/content.ts
@tests/content/content-load.test.ts

<interfaces>
<!-- Frontmatter shape locked in CONTEXT.md. Executor must produce frontmatter that matches. -->

Locked frontmatter values (per CONTEXT.md "Myco MDX Authoring Scope"):
- slug: `myco`
- title: `Myco`
- tagline: derived from README — one line, ≤ 140 chars
- year: `2025`
- tier: `hero`
- order: `10`
- status: `active`
- visibility: `public`
- tags: `[local-first, autonomous, open-source, ai, agents, typescript]` (from the 11-tag TAGS list in `lib/tags.ts`; all 6 are valid)
- stack: derived from README — typical Myco stack: `[TypeScript, SQLite, Ollama, Node.js]` (adjust only if README explicitly contradicts)
- links.repo: `https://github.com/olive-elliott/myco` (or the correct repo URL as confirmed when reading the README)
- links.docs: include ONLY if a docs site demonstrably exists (check README for a link); otherwise omit — don't fabricate
- hero.src: `/images/projects/myco/hero-placeholder.png`
- hero.alt: `Myco knowledge graph visualization (placeholder — real image lands in Phase 7)` — literal string, must include the parenthetical
- outcomes: array of ≤ 5 HONEST descriptive bullets. Candidates from CONTEXT.md: "Apache 2.0 public release", "SQLite + Ollama fully-local stack", "Knowledge graph with approval-gated writes", "Four MCP-compatible memory operations (remember, recall, log_episode, forget)". Add or substitute only if README corroborates. Do NOT invent user counts, timing metrics, or adoption stats.
- description: ≤ 160 chars, SEO-friendly one-liner distinct from tagline
- ogImage: OMIT (Phase 6 handles OG; CONTEXT.md allows Phase 2 to skip)

Body structure (CNT-05 + PRJ-03 locked structure):
```mdx
## Problem

~300–400 words on the problem Myco addresses.

## Approach

~300–400 words on the architecture, approach, and design choices.

## Outcome

~200–400 words on what shipped and what it enables.
```

Reference to existing content-load.test.ts (created in Wave 2, must not regress):
- Wave 2's tests use `_loadForTests(tmpDir)` with fixture copies — these keep passing unchanged.
- This plan ADDS new tests that call `_loadForTests(CONTENT_DIR)` or hit `allProjects` directly against the real content directory.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author content/projects/myco.mdx from the README</name>
  <files>content/projects/myco.mdx</files>
  <read_first>
    - Myco README — PRIMARY SOURCE. Locate per `user_setup` block above (check ~/Documents/GitHub/myco/README.md first, then gh CLI, then clone). If README is unreachable, HALT the task with a checkpoint asking the user for the README path.
    - .planning/phases/02-content-pipeline/02-CONTEXT.md (§ "Myco MDX Authoring Scope" — all locked frontmatter values)
    - .planning/phases/02-content-pipeline/02-RESEARCH.md (§ Example 1 — Myco MDX shape example showing frontmatter structure)
    - .planning/PROJECT.md (§ Constraints, specifically "Content honesty: placeholders are explicitly placeholders — never fabricate outcomes, metrics, or claims")
    - lib/tags.ts (confirm the 6 tags listed in CONTEXT.md are valid enum members)
    - lib/schemas.ts (confirm frontmatter fields the schema will validate)
  </read_first>
  <action>
    Step 1 — Acquire README. Try in order:
    1. `cat ~/Documents/GitHub/myco/README.md` (local clone)
    2. `gh repo view olive-elliott/myco --json name,description,repositoryTopics` + `gh api repos/olive-elliott/myco/readme -H "Accept: application/vnd.github.raw"`
    3. `git clone --depth 1 https://github.com/olive-elliott/myco /tmp/myco-readme && cat /tmp/myco-readme/README.md`

    If all three fail, pause and ask the user to paste the README or supply a path. Do NOT fabricate Myco content from memory — the CONTEXT.md "content honesty" constraint is strict.

    Step 2 — Draft the frontmatter. Use this exact skeleton and fill in values from the README:

    ```mdx
    ---
    slug: myco
    title: Myco
    tagline: <one-line pitch derived from README, ≤ 140 chars>
    year: 2025
    tier: hero
    order: 10
    status: active
    visibility: public
    tags: [local-first, autonomous, open-source, ai, agents, typescript]
    stack: [TypeScript, SQLite, Ollama, Node.js]
    links:
      repo: <exact Myco repo URL from README>
      <docs: ...>   # include only if README documents a docs site
    hero:
      src: /images/projects/myco/hero-placeholder.png
      alt: Myco knowledge graph visualization (placeholder — real image lands in Phase 7)
    outcomes:
      - Apache 2.0 public release
      - SQLite + Ollama fully-local stack
      - Knowledge graph with approval-gated writes
      - Four MCP-compatible memory operations (remember, recall, log_episode, forget)
    description: <SEO description, ≤ 160 chars, must differ from tagline>
    ---
    ```

    If the README contradicts any of the outcomes bullets, REVISE them. Keep them descriptive, not metric-bearing. Example of OK: "SQLite + Ollama fully-local stack". Example of NOT OK: "Used by 500+ developers in the first month" (fabricated metric).

    Step 3 — Draft the body. Three H2 sections with these exact anchors (Phase 3's template depends on the slugs):

    ```mdx
    ## Problem

    [300–400 words] — What problem Myco addresses. Draw from README's "Why Myco exists" / "The Problem" / motivation sections. Preserve voice but don't copy verbatim — shape into problem-statement prose. Mention: why persistent memory across Claude Code sessions matters; the autonomy/local-first thesis; what's broken about cloud-dependent or session-scoped memory.

    ## Approach

    [300–400 words] — How Myco solves it. Architecture-level: SQLite as the storage layer, Ollama for embeddings (local), the approval-gated write pattern, the MCP protocol as the integration surface, the four memory operations (remember, recall, log_episode, forget). Name the design choices that embody the thesis (why local-first SQLite, why Ollama vs cloud embeddings, why approval-gated writes).

    ## Outcome

    [200–400 words] — What shipped. Apache 2.0 release. Integration with Claude Code via MCP. Real usage pattern: the knowledge graph accumulates across sessions without leaking to cloud. Frame what this enables — what becomes possible that wasn't before (e.g., cross-session context, durable developer memory). Keep honest: no fabricated adoption stats, no invented benchmarks. If the README has measurable facts (e.g., number of commits, date of public release), those are fair game.
    ```

    Constraints on body:
    - Total body word count: 800–1200 (excluding frontmatter). Aim mid-range (~1000).
    - Do NOT include raw placeholder strings like "[TODO]" or "[placeholder]" — the hero image alt uses the parenthetical literally, but body prose must be real prose.
    - Do NOT include code blocks in Phase 2 body — Shiki highlighting is Phase 3; code blocks will render as plain `pre` and look broken.
    - Do NOT include images in the body (other than the hero image reference in frontmatter) — gallery is Phase 3.
    - Do NOT link to external articles mid-body. Keep the narrative self-contained.
    - Voice should match the README — engineer-positioned, thesis-forward, not marketing-fluff.

    Step 4 — Create the file at exactly `content/projects/myco.mdx`. The loader in `lib/content.ts` will pick it up automatically on the next `pnpm build` / `pnpm test` run via `fs.readdirSync(CONTENT_DIR)`.

    Step 5 — Sanity-check against the schema locally:
    `pnpm test tests/content/ --run` should still be green. Any schema violation surfaces as a Zod error in `lib/content.ts` load (the module-init load runs when `tag-index.test.ts` or `content-load.test.ts` import `@/lib/projects`).

    If the parse fails: READ THE ERROR. Common fixes:
    - Number field quoted: `year: "2025"` → `year: 2025`
    - Missing required field: add it
    - Tagline too long: tighten to ≤ 140
    - Description too long: tighten to ≤ 160
    - Unknown tag: confirm exact strings match the 11-entry TAGS list in `lib/tags.ts`
  </action>
  <verify>
    <automated>test -f content/projects/myco.mdx && pnpm test:ci</automated>
  </verify>
  <acceptance_criteria>
    - `content/projects/myco.mdx` exists
    - Frontmatter contains `slug: myco` (grep exact)
    - Frontmatter contains `tier: hero` (grep)
    - Frontmatter contains `visibility: public` (grep)
    - Frontmatter contains `year: 2025` (grep, NOT `year: "2025"`)
    - Frontmatter contains `order: 10` (grep)
    - Frontmatter tags include at minimum `local-first`, `open-source`, `ai` per CNT-05 Success Criterion 5: `grep -E "tags:.*local-first" content/projects/myco.mdx` passes; same for `open-source` and `ai`
    - Body contains exactly these H2 anchors: `^## Problem$`, `^## Approach$`, `^## Outcome$` (all three grep matches with `^## ` prefix on a line by itself)
    - Body word count between 800 and 1200: `wc -w content/projects/myco.mdx` minus frontmatter lines — a rough check via `awk '/^---$/{p=!p;next}p{next}{print}' content/projects/myco.mdx | wc -w` returns a number in [800, 1200]
    - Body does NOT contain `[TODO]`, `[placeholder]`, `lorem ipsum`, or `XXX` (grep -i returns no matches)
    - Hero alt includes literal `(placeholder — real image lands in Phase 7)`
    - `pnpm test:ci` exits 0 — this proves the schema accepts the file (the loader runs at module init during tests)
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0 — proves end-to-end (Next.js + remark-frontmatter + MDX + schema + loader) accepts the real content file
  </acceptance_criteria>
  <done>`content/projects/myco.mdx` exists; frontmatter validates against schema; body has the three locked H2 sections within word budget; `pnpm test:ci` and `pnpm build` both green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend content-load.test.ts with real-content assertions</name>
  <files>tests/content/content-load.test.ts</files>
  <read_first>
    - tests/content/content-load.test.ts (Wave 2 baseline — existing tests must not regress)
    - content/projects/myco.mdx (just authored — fields being asserted against)
    - lib/content.ts (confirm `allProjects` is loaded from `CONTENT_DIR` by default)
  </read_first>
  <action>
    Append a new describe block to `tests/content/content-load.test.ts`. Do NOT remove or modify the existing Wave 2 tests.

    Add these imports at the top (if not already present):
    ```ts
    import { allProjects } from '@/lib/content'
    ```

    Append this describe block at the end of the file:

    ```ts
    describe('content/projects/ — real content integration (CNT-05)', () => {
      it('loads at least one project (Myco)', () => {
        expect(allProjects.length).toBeGreaterThanOrEqual(1)
      })

      it('includes myco project with locked frontmatter values', () => {
        const myco = allProjects.find((p) => p.slug === 'myco')
        expect(myco).toBeDefined()
        expect(myco?.tier).toBe('hero')
        expect(myco?.visibility).toBe('public')
        expect(myco?.status).toBe('active')
        expect(myco?.year).toBe(2025)
        expect(myco?.order).toBe(10)
      })

      it('myco tags include local-first, open-source, ai', () => {
        const myco = allProjects.find((p) => p.slug === 'myco')
        expect(myco?.tags).toEqual(expect.arrayContaining(['local-first', 'open-source', 'ai']))
      })

      it('myco body contains Problem, Approach, Outcome H2 headings', () => {
        const myco = allProjects.find((p) => p.slug === 'myco')
        expect(myco?.body).toMatch(/^## Problem$/m)
        expect(myco?.body).toMatch(/^## Approach$/m)
        expect(myco?.body).toMatch(/^## Outcome$/m)
      })

      it('myco body is within 800–1200 word budget', () => {
        const myco = allProjects.find((p) => p.slug === 'myco')
        expect(myco).toBeDefined()
        const wordCount = (myco?.body ?? '').trim().split(/\s+/).length
        expect(wordCount).toBeGreaterThanOrEqual(800)
        expect(wordCount).toBeLessThanOrEqual(1200)
      })

      it('myco hero is a placeholder path (real image deferred to Phase 7)', () => {
        const myco = allProjects.find((p) => p.slug === 'myco')
        expect(myco?.hero.src).toContain('myco')
        expect(myco?.hero.alt).toContain('placeholder')
      })
    })
    ```

    Notes:
    - `allProjects` is imported at top level — when Vitest loads this file, it triggers `lib/content.ts` module init, which calls `load(CONTENT_DIR)` against the real `content/projects/`. This is the integration point.
    - These tests will fail in a fresh checkout if `content/projects/myco.mdx` is missing — that's correct behavior (CNT-05 is the point).
    - The word count regex `\s+` + `trim` is approximate; use mid-budget when authoring to avoid edge cases.
  </action>
  <verify>
    <automated>pnpm test tests/content/content-load.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `tests/content/content-load.test.ts` contains the new describe block `'content/projects/ — real content integration (CNT-05)'`
    - `grep -q "allProjects.find((p) => p.slug === 'myco')" tests/content/content-load.test.ts` succeeds
    - `grep -q "## Problem" tests/content/content-load.test.ts` succeeds (asserts presence of the regex expectation)
    - `pnpm test tests/content/content-load.test.ts --run` exits 0 with 6 (Wave 2) + 6 (new) = 12 tests passing
    - `pnpm test:ci` exits 0 full suite
    - `pnpm typecheck` exits 0
    - Pre-existing Wave 2 tests (`_loadForTests`, slug mismatch, invalid-tag, empty-dir) are UNCHANGED — grep the file to confirm each still exists verbatim
  </acceptance_criteria>
  <done>content-load.test.ts now includes integration assertions against the real Myco file; all tests green; Wave 2 assertions preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify Myco MDX editorial quality</name>
  <files>content/projects/myco.mdx</files>
  <what-built>
    A complete Myco case study at `content/projects/myco.mdx`: frontmatter passes schema, body follows Problem → Approach → Outcome structure (800–1200 words), all automated tests green. Now the user must verify editorial quality — the parts automation can't check.
  </what-built>
  <how-to-verify>
    1. Open `content/projects/myco.mdx` in your editor (VS Code, Cursor, or whatever).
    2. Read the frontmatter:
       - Tagline rings true — not marketing fluff, not AI-padding.
       - Outcomes bullets are HONEST — no fabricated metrics, no invented numbers. Cross-check against the Myco README.
    3. Read the body end-to-end (should take ~4–5 minutes at 800–1200 words):
       - Voice matches the README — engineer-positioned, thesis-forward.
       - Problem section frames the real problem (persistent memory, autonomy, local-first) — not a generic "AI needs memory" rant.
       - Approach section names actual architectural choices (SQLite, Ollama, MCP, approval-gated writes) with reasoning.
       - Outcome section is honest about what shipped — no "N users / N commits" stats unless directly sourced.
    4. Look for anti-patterns:
       - [ ] No AI-padding phrases ("In today's fast-paced world...", "revolutionizing X...", "passionate developer...")
       - [ ] No fabricated metrics (user counts, perf numbers, adoption stats) unless sourced from README
       - [ ] No generic closing ("Overall, Myco represents...")
    5. Mental check: would you send this link to a peer engineer? If yes, approve. If it reads as templated, revise.

    If revisions needed: type specifically what to change ("rewrite outcome section to tighten the autonomy framing", "shorten Approach section by 100 words", etc.). Claude will revise and loop back.
  </how-to-verify>
  <action>
    PAUSE for human review. This is a checkpoint task — Claude does NOT modify files during this task. Present the `<how-to-verify>` instructions to the user verbatim and wait for the resume signal. If the user requests revisions, apply them to `content/projects/myco.mdx` (rerun `pnpm test:ci` after) and re-present for review.
  </action>
  <verify>
    <automated>test -f content/projects/myco.mdx</automated>
  </verify>
  <done>User has typed "approved" or equivalent, or completed a revision cycle ending with approval.</done>
  <resume-signal>Type "approved" to proceed, or describe specific edits needed and Claude will revise.</resume-signal>
</task>

</tasks>

<verification>
After all three tasks:
1. `test -f content/projects/myco.mdx` → exits 0.
2. `pnpm test tests/content/ --run` → all Wave 1+2+3 content tests green.
3. `pnpm test:ci` → full suite green.
4. `pnpm typecheck && pnpm lint && pnpm build` → all green.
5. Checkpoint Task 3: user approved editorial quality.
</verification>

<success_criteria>
CNT-05 closed:
- [ ] `content/projects/myco.mdx` authored from Myco README
- [ ] Frontmatter matches all CONTEXT.md-locked values (tier, visibility, year, order, status, tags, hero)
- [ ] Body has Problem / Approach / Outcome H2 sections totaling 800–1200 words
- [ ] No fabricated metrics or placeholder text in the body
- [ ] `content-load.test.ts` asserts 6 integration properties of the real file
- [ ] User approved editorial quality via Task 3 checkpoint
- [ ] `pnpm test:ci` and `pnpm build` both green
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pipeline/02-03-SUMMARY.md` documenting:
- Source of the Myco README used (file path or commit SHA of clone)
- Final word count of the body
- Final outcomes bullet list (verbatim)
- Any deviations from CONTEXT.md-locked frontmatter (expected: none)
- User's checkpoint approval note (verbatim or summarized)
- Handoff notes for Phase 3: Myco is the canonical rendering fixture; `/projects/myco` will dynamically import `@/content/projects/myco.mdx`
</output>
