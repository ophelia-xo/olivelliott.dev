---
phase: 03-project-detail-template
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - next.config.ts
  - styles/tokens.css
  - app/globals.css
  - lib/hero-fallback.ts
  - public/og-default.png
  - tests/mdx/pipeline.test.ts
  - tests/projects/hero-fallback.test.ts
autonomous: true
requirements: [PRJ-02, PRJ-04, PRJ-07]

must_haves:
  truths:
    - "MDX build pipeline emits Shiki-tokenized <pre> blocks with Vesper theme and rehype-pretty-code data attributes"
    - "Every MDX H2/H3 gets an autogenerated id (rehype-slug) plus an appended <a class=anchor> with `#` glyph (rehype-autolink-headings, behavior:append)"
    - "isPlaceholderHero(src) returns true for /images/projects/<slug>/hero-placeholder.{png|jpg|jpeg|webp} and false for any real artwork path — RSC-safe regex, no fs probe"
    - "/og-default.png exists in /public so OG fallback chain never 404s for placeholder-hero projects"
    - ".prose / .anchor / [data-rehype-pretty-code-title] / [data-line] / [data-highlighted-line] CSS rules are present in app/globals.css"
    - "@theme block in styles/tokens.css declares --text-h2 / --text-h3 / --font-weight-semibold (additive only — no edits to existing tokens)"
    - "pnpm build succeeds with the extended rehype chain"
  artifacts:
    - path: "next.config.ts"
      provides: "rehype chain: rehype-slug then rehype-autolink-headings then rehype-pretty-code (string-form, Turbopack-safe)"
      contains: "rehype-pretty-code"
    - path: "styles/tokens.css"
      provides: "Phase 3 type tokens"
      contains: "--text-h2"
    - path: "app/globals.css"
      provides: ".prose + .anchor + rehype-pretty-code data-attribute styles"
      contains: ".prose"
    - path: "lib/hero-fallback.ts"
      provides: "isPlaceholderHero(src) regex helper"
      exports: ["isPlaceholderHero"]
    - path: "public/og-default.png"
      provides: "OG fallback asset (1200x630)"
    - path: "tests/mdx/pipeline.test.ts"
      provides: "Source assertion that next.config.ts wires plugins in correct order"
    - path: "tests/projects/hero-fallback.test.ts"
      provides: "isPlaceholderHero regex spec (positive + negative cases)"
  key_links:
    - from: "next.config.ts"
      to: "rehype-pretty-code + shiki + rehype-slug + rehype-autolink-headings"
      via: "withMDX rehypePlugins string-form registration"
      pattern: "rehype-pretty-code"
    - from: "styles/tokens.css"
      to: "Tailwind v4 @theme system"
      via: "--text-h2 / --text-h3 / --font-weight-semibold token names"
      pattern: "--font-weight-semibold"
---

<objective>
Wave 0 infrastructure for Phase 3. Install the four MDX-pipeline dependencies (rehype-pretty-code, shiki, rehype-slug, rehype-autolink-headings), extend next.config.ts with the Turbopack-safe string-form rehype chain, append the H2 / H3 / semibold type tokens, hand-author the `.prose` class + `.anchor` class + rehype-pretty-code data-attribute selectors in globals.css, ship the `isPlaceholderHero` helper + its RED test, stub `/public/og-default.png`, and write the rehype-chain source-assertion test.

Purpose: Wave 1 (MDX components) and Wave 2 (page + hero + next block) can both assume a working MDX-with-Shiki pipeline, the prose-rhythm CSS, the H2/H3 tokens, the placeholder-hero detection helper, and the OG fallback asset.

Output: Extended `next.config.ts`, additive `styles/tokens.css` block, hand-authored `.prose` rules in `app/globals.css`, `lib/hero-fallback.ts`, stub `/public/og-default.png`, two new test files, four new runtime deps in `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-project-detail-template/03-CONTEXT.md
@.planning/phases/03-project-detail-template/03-RESEARCH.md
@.planning/phases/03-project-detail-template/03-UI-SPEC.md
@.planning/phases/03-project-detail-template/03-VALIDATION.md
@next.config.ts
@styles/tokens.css
@app/globals.css
@package.json
@vitest.config.ts

<interfaces>
Existing wiring the executor must preserve, not invent.

next.config.ts (current — Phase 2):
- `withMDX` wraps `nextConfig` via `createMDX({ extension: /\.(md|mdx)$/, options: { remarkPlugins: ['remark-frontmatter'], rehypePlugins: [] } })`
- Turbopack constraint: plugins MUST be string-form `'plugin-name'` or `[name, optionsObject]` tuples. Function references break (#76739 / #84258). Phase 2 already locked this for `'remark-frontmatter'`. Do NOT switch to `next.config.js`.

styles/tokens.css (current — Phase 1):
- `@theme { ... }` block declares: --font-sans, --font-mono, --text-body, --text-label, --text-display, --font-weight-regular (400), --font-weight-medium (500), --tracking-*, --spacing, all colors, all motion, all radii.
- Phase 3 ADDS only: --text-h2, --text-h2--line-height, --text-h3, --text-h3--line-height, --font-weight-semibold. Do NOT edit existing tokens.

app/globals.css (current — Phase 1):
- `@import "tailwindcss"` + `@import "../styles/tokens.css"` at top.
- @media (prefers-reduced-motion: reduce) collapses all transitions to 0.01ms — Phase 3 inherits.
- `:focus-visible` 2px accent outline at 2px offset — Phase 3 inherits (NextProjectBlock overrides offset to 4px in its own component file, not here).

vitest.config.ts:
- jsdom environment, `@/*` alias, `server-only` aliased to `tests/stubs/server-only.ts`. Phase 3 tests inherit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 03-00-01: Install Phase 3 MDX-pipeline dependencies</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (verify current dependencies — must NOT touch existing entries)
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Standard Stack → Phase 3 NEW dependencies" (version pins + rationale for shiki@^3 over ^4)
    - .planning/STATE.md (Phase 02 P00 note: gray-matter + remark-frontmatter were added as runtime deps — same pattern applies here)
  </read_first>
  <action>
    Run exactly:

        pnpm add rehype-pretty-code@^0.14.3 shiki@^3.23.0 rehype-slug@^6.0.0 rehype-autolink-headings@^7.1.0

    All four are runtime dependencies (under `dependencies`, not `devDependencies`) — Next.js resolves rehype/remark plugins from `dependencies` during `next build`. Same convention Phase 2 used for `gray-matter` and `remark-frontmatter` (per STATE.md Phase 02 P00 note).

    Pin shiki to `^3.23.0` (NOT ^4.x). Per RESEARCH § "Version pinning note (Shiki v3 vs v4)": rehype-pretty-code's peer accepts both, Vesper is bundled in both, but ecosystem soak time on shiki 4.x is short. Lower drift risk on 3.23.

    If `pnpm install` surfaces a peer-dependency warning, log it in the commit message but proceed — the four pins are validated against the npm registry on 2026-05-15 (RESEARCH "Verified" column).

    Do NOT add devDependencies, do NOT edit existing `dependencies` entries, do NOT bump unrelated packages.
  </action>
  <verify>
    <automated>pnpm ls rehype-pretty-code shiki rehype-slug rehype-autolink-headings</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` block contains exactly these new entries with caret-pinned versions: `"rehype-pretty-code": "^0.14.3"`, `"shiki": "^3.23.0"`, `"rehype-slug": "^6.0.0"`, `"rehype-autolink-headings": "^7.1.0"`.
    - `pnpm-lock.yaml` updated with resolved versions.
    - `pnpm ls rehype-pretty-code shiki rehype-slug rehype-autolink-headings` reports all four installed at the expected major.minor.
    - No unrelated `dependencies` or `devDependencies` entries changed.
  </acceptance_criteria>
  <done>Four runtime deps present in package.json + pnpm-lock.yaml; existing dependency block untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-00-02: Extend next.config.ts rehype chain + write pipeline source-assertion test</name>
  <files>next.config.ts, tests/mdx/pipeline.test.ts</files>
  <read_first>
    - next.config.ts (current Phase 2 wiring — preserve `'remark-frontmatter'` line and the comment block above it)
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Pattern 3: Rehype plugin chain via string-form (Turbopack-mandatory)"
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Pattern 4: Plugin order MUST be slug → autolink → pretty-code"
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Pitfall 1" (title=, NOT {filename=}), § "Pitfall 3" (keepBackground: false), § "Pitfall 7" (behavior: append, NOT wrap), § "Pitfall 4" (no function-reference plugin imports)
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Heading anchor (rehype-autolink-headings configuration)" + § "Code Block Contract" (filename label uses [data-rehype-pretty-code-title], NOT a hand-rendered div)
    - .planning/phases/03-project-detail-template/03-VALIDATION.md (Wave 0 task 03-00-02 covers PRJ-02)
  </read_first>
  <behavior>
    - Test 1: assert `next.config.ts` source contains the literal substrings `'rehype-slug'`, `'rehype-autolink-headings'`, `'rehype-pretty-code'` in left-to-right order.
    - Test 2: assert `theme: 'vesper'` substring is present.
    - Test 3: assert `keepBackground: false` substring is present.
    - Test 4: assert `behavior: 'append'` is present and `behavior: 'wrap'` is NOT present.
    - Test 5: assert the `{filename=` substring is NOT present (Pitfall 1 — wrong metastring).
    - Test 6: assert no top-level function-reference imports of the four rehype packages (Pitfall 4 — Turbopack constraint).
    - Test 7: assert `'remark-frontmatter'` line from Phase 2 is preserved verbatim.
  </behavior>
  <action>
    Step A — write `tests/mdx/pipeline.test.ts` FIRST (RED):

        // tests/mdx/pipeline.test.ts
        // Source assertions on next.config.ts. Verifies the Turbopack-safe string-form
        // rehype chain is wired in the correct order with the locked options.
        // Refs: 03-RESEARCH § Pattern 3, Pattern 4, Pitfall 1, Pitfall 3, Pitfall 4, Pitfall 7.
        import { readFileSync } from 'node:fs'
        import { join } from 'node:path'
        import { describe, expect, it } from 'vitest'

        const config = readFileSync(join(process.cwd(), 'next.config.ts'), 'utf-8')

        describe('next.config.ts — Phase 3 rehype chain', () => {
          it('registers rehype-slug, rehype-autolink-headings, rehype-pretty-code in order', () => {
            const slugIdx = config.indexOf("'rehype-slug'")
            const autolinkIdx = config.indexOf("'rehype-autolink-headings'")
            const prettyIdx = config.indexOf("'rehype-pretty-code'")
            expect(slugIdx).toBeGreaterThan(-1)
            expect(autolinkIdx).toBeGreaterThan(slugIdx)
            expect(prettyIdx).toBeGreaterThan(autolinkIdx)
          })

          it('uses Vesper Shiki theme by bundled name', () => {
            expect(config).toContain("theme: 'vesper'")
          })

          it('disables Shiki theme background (we paint --color-surface-2 ourselves)', () => {
            expect(config).toMatch(/keepBackground:\s*false/)
          })

          it("uses behavior: 'append' for autolink-headings (NOT 'wrap')", () => {
            expect(config).toMatch(/behavior:\s*'append'/)
            expect(config).not.toMatch(/behavior:\s*'wrap'/)
          })

          it('does NOT use the {filename=...} metastring syntax (Pitfall 1)', () => {
            expect(config).not.toContain('{filename=')
          })

          it('does NOT import rehype plugins as function references (Turbopack constraint)', () => {
            expect(config).not.toMatch(/import\s+rehypePrettyCode/)
            expect(config).not.toMatch(/import\s+rehypeSlug/)
            expect(config).not.toMatch(/import\s+rehypeAutolinkHeadings/)
          })

          it('preserves Phase 2 remark-frontmatter registration', () => {
            expect(config).toContain("'remark-frontmatter'")
          })
        })

    Run `pnpm vitest run tests/mdx/pipeline.test.ts` — expect ALL 7 assertions to FAIL (RED state).

    Step B — extend `next.config.ts` (GREEN). Replace the body with the following exact content. Preserves the Phase 2 remark-frontmatter line + comment, adds three rehype plugins string-form with serializable options:

        import type { NextConfig } from 'next'
        import createMDX from '@next/mdx'

        /** rehype-pretty-code options. Must be JSON-serializable for Turbopack.
         *  - theme: 'vesper' is bundled in shikijs/textmate-grammars-themes; string name is sufficient (no JSON import needed).
         *  - keepBackground: false — we paint --color-surface-2 ourselves via the .prose pre rule (Pitfall 3).
         *  - defaultLang: 'plaintext' — un-tagged code blocks still get tokenized for visual consistency.
         */
        const rehypePrettyCodeOptions = {
          theme: 'vesper',
          keepBackground: false,
          defaultLang: 'plaintext',
        }

        /** rehype-autolink-headings options.
         *  - behavior: 'append' — anchor sits AFTER heading text (UI-SPEC Heading anchor; Pitfall 7 forbids 'wrap').
         *  - properties.className: ['anchor'] — styled in app/globals.css .prose .anchor.
         *  - content: <span aria-hidden="true">#</span> — decorative glyph; the wrapping <a> carries aria-label.
         */
        const rehypeAutolinkHeadingsOptions = {
          behavior: 'append',
          properties: {
            className: ['anchor'],
            ariaLabel: 'Link to section',
          },
          content: {
            type: 'element',
            tagName: 'span',
            properties: { ariaHidden: 'true' },
            children: [{ type: 'text', value: '#' }],
          },
        }

        const withMDX = createMDX({
          extension: /\.(md|mdx)$/,
          options: {
            // Strings (not function refs) are required for Turbopack compatibility in Next 16.
            // Source: https://nextjs.org/docs/app/guides/mdx#using-plugins-with-turbopack
            // remark-frontmatter strips YAML `---` blocks from the MDX AST so they don't render as body text.
            // gray-matter (in lib/content.ts) handles frontmatter parsing out-of-band via fs.
            remarkPlugins: ['remark-frontmatter'],
            // Order MUST be slug → autolink → pretty-code (Pattern 4).
            rehypePlugins: [
              'rehype-slug',
              ['rehype-autolink-headings', rehypeAutolinkHeadingsOptions],
              ['rehype-pretty-code', rehypePrettyCodeOptions],
            ],
          },
        })

        const nextConfig: NextConfig = {
          pageExtensions: ['ts', 'tsx', 'md', 'mdx'],
          poweredByHeader: false,
          reactStrictMode: true,
        }

        export default withMDX(nextConfig)

    Run `pnpm vitest run tests/mdx/pipeline.test.ts` — expect ALL 7 assertions GREEN.

    Then run `pnpm build`. Build MUST succeed end-to-end (Phase 2's myco.mdx will compile through the new chain; existing `## Problem`, `## Approach`, `## Outcome` headings will receive ids and appended `<a class="anchor">#</a>` anchors; any code blocks will tokenize via Vesper). If the build fails with a peer-dependency conflict in shiki, escalate per RESEARCH "Open Question 3" and consider bumping shiki to `^4.x`.

    Do NOT introduce function-reference imports of rehype plugins (Pitfall 4). Do NOT customize `<pre>` scrollbar in code, do NOT add `@tailwindcss/typography` (Don't Hand-Roll table). Do NOT touch the `pageExtensions`, `poweredByHeader`, or `reactStrictMode` keys.
  </action>
  <verify>
    <automated>pnpm vitest run tests/mdx/pipeline.test.ts --reporter=dot && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `tests/mdx/pipeline.test.ts` exists with all 7 assertions green under `pnpm vitest run`.
    - `next.config.ts` body matches the exact structure in <action>.
    - `next.config.ts` `'remark-frontmatter'` line and its preceding comment block are preserved verbatim.
    - `next.config.ts` contains no `import rehypePrettyCode` / `import rehypeSlug` / `import rehypeAutolinkHeadings` lines.
    - `next.config.ts` contains `theme: 'vesper'`, `keepBackground: false`, `behavior: 'append'`.
    - `pnpm build` exits 0 and emits the static `/projects/myco` page.
    - `grep -c "{filename=" next.config.ts` returns `0`.
  </acceptance_criteria>
  <done>Rehype chain wired Turbopack-safe in correct order; pipeline source-assertion tests green; full build succeeds with the new chain processing the existing Myco MDX.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-00-03: Append Phase 3 type tokens + author .prose / .anchor / rehype-pretty-code CSS + ship isPlaceholderHero helper + RED test</name>
  <files>styles/tokens.css, app/globals.css, lib/hero-fallback.ts, tests/projects/hero-fallback.test.ts</files>
  <read_first>
    - styles/tokens.css (current — must APPEND only inside the existing @theme block; do NOT edit existing tokens)
    - app/globals.css (current — must APPEND below the existing :focus-visible block)
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Typography → Type ramp additions for @theme" (exact 5-line addition)
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Heading anchor (rehype-autolink-headings configuration)" (anchor CSS spec)
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Code Block Contract" (pre + inline code styles + filename label data-attribute selector)
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Hero Variants → Detection rule (locked)" (regex source-of-truth)
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "app/globals.css — Phase 3 additions" (verbatim hand-authored CSS block)
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Pitfall 1" (use [data-rehype-pretty-code-title] selector, not a hand-rendered div)
  </read_first>
  <behavior>
    - Test 1 (positive): isPlaceholderHero('/images/projects/myco/hero-placeholder.png') === true.
    - Test 2 (positive): every supported extension — .png / .jpg / .jpeg / .webp — matches; case-insensitive.
    - Test 3 (positive): nested slug works — '/images/projects/trade-bot/hero-placeholder.jpg' === true.
    - Test 4 (negative): real artwork rejected — '/images/projects/myco/hero.png' === false.
    - Test 5 (negative): unsupported extension rejected — '.gif', '.svg', '.avif' all === false.
    - Test 6 (negative): unrelated paths rejected — '/og-default.png', '', '/images/hero-placeholder.png' all === false.
    - Test 7 (negative): different filename rejected — '/images/projects/myco/foo-placeholder.png' === false; '/images/projects/myco/placeholder.png' === false.
  </behavior>
  <action>
    Step A — write `lib/hero-fallback.ts`:

        // lib/hero-fallback.ts
        // RSC-safe placeholder-hero detection by suffix pattern.
        // Source: .planning/phases/03-project-detail-template/03-UI-SPEC.md § Hero Variants → Detection rule (locked).
        // Used by <ProjectHero> (Wave 2) to choose Variant A (image-present) vs Variant B (text-only).
        // No fs probe, no fetch — regex against project.hero.src is the entire detection.
        export const isPlaceholderHero = (src: string): boolean =>
          /\/images\/projects\/[^/]+\/hero-placeholder\.(png|jpe?g|webp)$/i.test(src)

    Step B — write `tests/projects/hero-fallback.test.ts`:

        // tests/projects/hero-fallback.test.ts
        // Validates isPlaceholderHero regex contract — RSC-safe path detection.
        // Refs: 03-UI-SPEC § Hero Variants → Detection rule (locked); 03-VALIDATION row PRJ-04.
        import { describe, expect, it } from 'vitest'
        import { isPlaceholderHero } from '@/lib/hero-fallback'

        describe('isPlaceholderHero', () => {
          it('matches the canonical png placeholder path', () => {
            expect(isPlaceholderHero('/images/projects/myco/hero-placeholder.png')).toBe(true)
          })

          it('matches every supported extension (png, jpg, jpeg, webp), case-insensitive', () => {
            expect(isPlaceholderHero('/images/projects/x/hero-placeholder.png')).toBe(true)
            expect(isPlaceholderHero('/images/projects/x/hero-placeholder.jpg')).toBe(true)
            expect(isPlaceholderHero('/images/projects/x/hero-placeholder.jpeg')).toBe(true)
            expect(isPlaceholderHero('/images/projects/x/hero-placeholder.webp')).toBe(true)
            expect(isPlaceholderHero('/images/projects/X/HERO-PLACEHOLDER.PNG')).toBe(true)
          })

          it('matches nested project slugs with hyphens', () => {
            expect(isPlaceholderHero('/images/projects/trade-bot/hero-placeholder.jpg')).toBe(true)
            expect(isPlaceholderHero('/images/projects/agenda-keeper/hero-placeholder.webp')).toBe(true)
          })

          it('rejects real artwork paths (no -placeholder suffix)', () => {
            expect(isPlaceholderHero('/images/projects/myco/hero.png')).toBe(false)
            expect(isPlaceholderHero('/images/projects/myco/cover.jpg')).toBe(false)
          })

          it('rejects unsupported extensions (gif, svg, avif)', () => {
            expect(isPlaceholderHero('/images/projects/myco/hero-placeholder.gif')).toBe(false)
            expect(isPlaceholderHero('/images/projects/myco/hero-placeholder.svg')).toBe(false)
            expect(isPlaceholderHero('/images/projects/myco/hero-placeholder.avif')).toBe(false)
          })

          it('rejects unrelated paths', () => {
            expect(isPlaceholderHero('/og-default.png')).toBe(false)
            expect(isPlaceholderHero('/images/hero-placeholder.png')).toBe(false)
            expect(isPlaceholderHero('')).toBe(false)
          })

          it('rejects different-named placeholders', () => {
            expect(isPlaceholderHero('/images/projects/myco/foo-placeholder.png')).toBe(false)
            expect(isPlaceholderHero('/images/projects/myco/placeholder.png')).toBe(false)
          })
        })

    Run `pnpm vitest run tests/projects/hero-fallback.test.ts` — expect ALL cases GREEN immediately. (Task-level TDD: the test specifies the contract explicitly. The helper is one line — splitting RED→GREEN across two commits would be ceremonial overhead. Acceptable per the "glue code wiring existing tested components" exception.)

    Step C — append Phase 3 tokens to `styles/tokens.css`. Inside the existing `@theme { ... }` block, append the following at the end (before the closing `}`). Do NOT edit any existing token. Add the comment header above the additions:

        /* Phase 3 additions — Type ramp H2 / H3 + semibold weight.
         * Pre-authorized by Phase 1 UI-SPEC: "Phase 3 MAY introduce 600 for MDX H2/H3 headings".
         * Source: .planning/phases/03-project-detail-template/03-UI-SPEC.md § Typography. */
        --text-h2: 1.5rem;
        --text-h2--line-height: 1.3;
        --text-h3: 1.25rem;
        --text-h3--line-height: 1.4;
        --font-weight-semibold: 600;

    NOTE: the H2 desktop ramp (1.75rem at md+) is applied via media query inside the `.prose h2` rule in globals.css below — NOT as a separate token (no other component consumes a responsive H2 token). Keep the @theme additions minimal.

    Step D — append `.prose` + `.anchor` + rehype-pretty-code CSS to `app/globals.css`. Append the following BLOCK to the end of `app/globals.css` (after the existing `:focus:not(:focus-visible)` rule). Source: RESEARCH § "app/globals.css — Phase 3 additions":

        /* === Phase 3: Project detail prose styling ===
         * Hand-authored .prose class — UI-SPEC § Page Composition + § Heading anchor + § Code Block Contract.
         * Do NOT install @tailwindcss/typography — its defaults override the token discipline.
         */

        .prose h2 {
          font-size: var(--text-h2);
          line-height: var(--text-h2--line-height);
          font-weight: var(--font-weight-semibold);
          letter-spacing: -0.015em;
          color: var(--color-text-primary);
          margin-top: 3rem;
        }
        @media (min-width: 768px) {
          .prose h2 {
            font-size: 1.75rem;
            margin-top: 4rem;
          }
        }

        .prose h3 {
          font-size: var(--text-h3);
          line-height: var(--text-h3--line-height);
          font-weight: var(--font-weight-semibold);
          letter-spacing: -0.01em;
          color: var(--color-text-primary);
          margin-top: 2rem;
        }

        .prose p { margin-top: 1rem; line-height: 1.6; color: var(--color-text-primary); }
        .prose p:first-child { margin-top: 0; }

        .prose a {
          color: var(--color-accent);
          text-decoration: underline;
          text-decoration-thickness: 1px;
          text-underline-offset: 2px;
          transition: text-decoration-color var(--motion-duration-fast) linear,
                      text-underline-offset var(--motion-duration-fast) ease-out;
        }
        .prose a:hover { color: var(--color-accent-hover); text-underline-offset: 3px; }

        /* Inline code (NOT inside <pre>) — surface-2 pill */
        .prose code:not(pre code) {
          background-color: var(--color-surface-2);
          border-radius: var(--radius-sm);
          padding: 0.25rem 0.375rem;
          margin: 0 0.125rem;
          font-family: var(--font-mono);
          font-size: 0.9375em;
          color: var(--color-text-primary);
        }

        /* Code block (<pre>) — surface-2 fill, hairline border, 6px radius */
        .prose pre {
          background-color: var(--color-surface-2);
          border: 1px solid var(--color-hairline);
          border-radius: var(--radius-md);
          padding: 1rem;
          margin: 2rem 0;
          overflow-x: auto;
          line-height: 1.625;
        }

        /* rehype-pretty-code emits these data-attributes — style line + filename label */
        .prose pre [data-line] { display: block; padding: 0 1rem; }
        .prose pre [data-highlighted-line] {
          background-color: rgba(255, 255, 255, 0.05);
          border-left: 2px solid var(--color-accent);
          padding-left: calc(1rem - 2px);
        }
        .prose figure[data-rehype-pretty-code-figure] { margin: 2rem 0; }
        .prose [data-rehype-pretty-code-title] {
          display: block;
          background-color: var(--color-surface-2);
          border: 1px solid var(--color-hairline);
          border-bottom: none;
          border-top-left-radius: var(--radius-md);
          border-top-right-radius: var(--radius-md);
          padding: 0.5rem 1rem;
          font-family: var(--font-mono);
          font-size: var(--text-label);
          color: var(--color-text-secondary);
          margin-bottom: -1px;
        }
        .prose [data-rehype-pretty-code-title] + pre {
          margin-top: 0;
          border-top-left-radius: 0;
          border-top-right-radius: 0;
        }

        /* Heading anchor — invisible at rest, visible on heading hover/focus */
        .prose .anchor {
          margin-left: 0.5rem;
          display: inline-block;
          opacity: 0;
          color: var(--color-text-tertiary);
          font-family: var(--font-mono);
          text-decoration: none;
          transition: opacity var(--motion-duration-base) var(--motion-ease-standard),
                      color var(--motion-duration-fast) linear;
        }
        .prose h2:hover .anchor,
        .prose h3:hover .anchor,
        .prose .anchor:focus-visible {
          opacity: 1;
        }
        .prose .anchor:hover,
        .prose .anchor:focus-visible {
          color: var(--color-accent);
        }

        /* Reduced-motion: anchors permanently visible (preserves affordance without hover) */
        @media (prefers-reduced-motion: reduce) {
          .prose .anchor { opacity: 1; }
        }

        /* Lists — ordered + unordered */
        .prose ul, .prose ol { margin-top: 1rem; padding-left: 1.5rem; }
        .prose li { margin-top: 0.5rem; line-height: 1.6; color: var(--color-text-primary); }
        .prose ul { list-style: disc; }
        .prose ol { list-style: decimal; }

    Do NOT add `.prose { max-width: ... }` here — the prose width measure (`max-w-[65ch]`) is applied by the `<MDXProse>` component (Wave 1) on the wrapping div as a Tailwind arbitrary class. Width composition stays separate from typography rhythm.
  </action>
  <verify>
    <automated>pnpm vitest run tests/projects/hero-fallback.test.ts --reporter=dot && grep -q "^  --text-h2:" styles/tokens.css && grep -q "^  --font-weight-semibold:" styles/tokens.css && grep -q "\.prose h2 {" app/globals.css && grep -q "data-rehype-pretty-code-title" app/globals.css && grep -q "\.prose \.anchor" app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `lib/hero-fallback.ts` exists and exports `isPlaceholderHero` with the regex shown.
    - `tests/projects/hero-fallback.test.ts` exists with 7 `describe`/`it` assertions, all green.
    - `styles/tokens.css` `@theme` block contains `--text-h2`, `--text-h2--line-height`, `--text-h3`, `--text-h3--line-height`, `--font-weight-semibold` AS NEW LINES below the existing tokens. Existing token values unchanged.
    - `app/globals.css` contains `.prose h2`, `.prose h3`, `.prose p`, `.prose a`, `.prose code:not(pre code)`, `.prose pre`, `[data-rehype-pretty-code-title]`, `[data-line]`, `[data-highlighted-line]`, `.prose .anchor`, and the reduced-motion override `.prose .anchor { opacity: 1; }`. All sit BELOW the existing `:focus:not(:focus-visible)` rule.
    - No Tailwind Typography plugin import in `globals.css`; no `.prose { max-width: ... }` rule (width is a Wave 1 component concern).
  </acceptance_criteria>
  <done>Phase 3 tokens additive in tokens.css; .prose CSS authored in globals.css with all rehype-pretty-code data-attribute selectors; isPlaceholderHero helper + 7-case test green.</done>
</task>

<task type="auto">
  <name>Task 03-00-04: Stub /public/og-default.png (1200x630 typographic fallback)</name>
  <files>public/og-default.png</files>
  <read_first>
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "OG image precedence (locked)" (path is `/og-default.png`; used when ogImage is unset AND hero is the placeholder)
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Open Question 1" (Phase 3 should stub this to unblock shareable URLs; Phase 6 may replace with a polished version)
    - .planning/phases/03-project-detail-template/03-CONTEXT.md § "Per-Page Metadata & OG" (site default OG is established here as a Phase 3 dependency)
  </read_first>
  <action>
    Generate a 1200×630 PNG at `/public/og-default.png`. Required visual contract:

    - Background: solid `#0a0a0a` (matches `--color-bg`).
    - Wordmark text "olive elliott" centered horizontally and vertically, in `#f5f5f5` (matches `--color-text-primary`).
    - Wordmark uses Geist Sans 96px weight 500 (closest mono-source approximation acceptable; typography is intentionally restrained).
    - Optional small mono tag below wordmark, e.g., `engineer · autonomy · local-first`, in `#a3a3a3` (`--color-text-secondary`), 28px Geist Mono.
    - No gradient, no glow, no decorative shapes — UI-SPEC anti-patterns apply to the OG too.

    Recommended generation path (any ONE works — pick the cheapest):

    1. **Programmatic via @vercel/og locally.** Create a one-off script `scripts/generate-og-default.ts` that uses `next/og`'s `ImageResponse` with the wordmark layout, run it via `pnpm tsx scripts/generate-og-default.ts > public/og-default.png`. Delete the script after.
    2. **Hand-export from any design tool** (Figma, Sketch, Affinity, GIMP) at 1200×630, save as PNG.
    3. **Headless screenshot.** Author a tiny `app/og-preview/page.tsx` route that renders the wordmark, screenshot via `pnpm dlx playwright screenshot ...` at 1200×630, save and delete the route.

    File MUST be a valid PNG (`file public/og-default.png` reports `PNG image data, 1200 x 630`). Document path in commit message so Phase 6 can replace with a polished version.

    Do NOT commit the generation script (if used) — it's one-shot. The PNG itself is the deliverable.
  </action>
  <verify>
    <automated>test -f public/og-default.png && file public/og-default.png | grep -q "PNG image data, 1200 x 630"</automated>
  </verify>
  <acceptance_criteria>
    - `public/og-default.png` exists.
    - `file public/og-default.png` reports `PNG image data, 1200 x 630` (or equivalent indicating PNG + 1200×630 dimensions).
    - File is binary PNG (not a placeholder text file).
    - Visual contract followed: solid `#0a0a0a` bg, light wordmark, no decorative shapes/gradients (manual visual check at commit; not auto-asserted).
    - No generation script committed alongside the PNG.
  </acceptance_criteria>
  <done>Stub OG default image lives at /public/og-default.png at 1200×630 PNG; OG fallback chain has a real asset to point at; Phase 6 may replace later.</done>
</task>

</tasks>

<verification>
- `pnpm ls rehype-pretty-code shiki rehype-slug rehype-autolink-headings` reports all four installed.
- `pnpm vitest run tests/mdx/pipeline.test.ts tests/projects/hero-fallback.test.ts` — all green.
- `pnpm build` succeeds and emits the static `/projects/myco` page.
- `file public/og-default.png` reports `PNG image data, 1200 x 630`.
- `grep` confirms `--text-h2`, `--font-weight-semibold` in `styles/tokens.css` and `.prose h2`, `data-rehype-pretty-code-title`, `.prose .anchor` in `app/globals.css`.
- Phase 2's `tests/content/content-load.test.ts`, `tests/content/redaction.test.ts`, `tests/content/schema.test.ts` all still green (no regression).
</verification>

<success_criteria>
- Wave 0 unblocks Wave 1 (MDX components can be authored against a working `.prose` rhythm + Vesper code blocks)
- Wave 0 unblocks Wave 2 (`<ProjectHero>` imports `isPlaceholderHero`; `generateMetadata` has a real `/og-default.png` to fall back to)
- All 7 PRJ-XX requirements remain reachable: Plans 01–03 will cover them; Plan 00 specifically lights up PRJ-02 (rehype chain), PRJ-04 (placeholder-hero detection), PRJ-07 (OG asset).
- Zero design-token additions beyond the H2 / H3 / semibold ones pre-authorized by Phase 1.
- No regression in Phase 1 + Phase 2 test suites.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-detail-template/03-00-SUMMARY.md` recording:
- Files created / modified (one row each).
- Test results (`pipeline.test.ts` 7/7 green, `hero-fallback.test.ts` 7/7 green).
- `pnpm build` exit status + size of static `/projects/myco` output.
- Whether shiki landed on `^3.x` or had to bump to `^4.x` (peer-warning escalation per RESEARCH Open Question 3).
- OG-default generation method used (which of the three options).
- Any deviations from the plan, with justification.
</output>
