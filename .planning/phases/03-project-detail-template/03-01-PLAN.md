---
phase: 03-project-detail-template
plan: 01
type: execute
wave: 1
depends_on: [03-00]
files_modified:
  - components/mdx/prose.tsx
  - components/mdx/figure.tsx
  - components/mdx/gallery.tsx
  - components/mdx/callout.tsx
  - mdx-components.tsx
  - tests/mdx/components.test.tsx
  - tests/mdx/prose.test.tsx
  - tests/mdx/registration.test.ts
autonomous: true
requirements: [PRJ-02]

must_haves:
  truths:
    - "<MDXProse> wraps children in mx-auto max-w-[65ch] mt-12 md:mt-16 with the .prose class so the prose CSS rhythm activates only inside this wrapper"
    - "<Figure> renders next/image + optional <figcaption>; default width inherits prose; wide=true bleeds out via the documented mx/max-w arbitrary classes"
    - "<Gallery> renders an N-up grid (default 2-up; columns=3 opts in to 3-up at lg+); gap ramps 4 → 6 → 8 across breakpoints"
    - "<Callout> emits <aside> with bg-surface-2 + 6px radius + 4px left border whose color encodes variant: note=accent, warn=danger, quote=text-tertiary"
    - "mdx-components.tsx registers Figure, Gallery, Callout via useMDXComponents (the @next/mdx global registration channel)"
    - "All four components are RSC-only (no 'use client') and contribute zero client JS to the bundle"
  artifacts:
    - path: "components/mdx/prose.tsx"
      provides: "MDXProse wrapper component"
      exports: ["MDXProse"]
    - path: "components/mdx/figure.tsx"
      provides: "Figure MDX component (image + caption + wide opt-in)"
      exports: ["Figure"]
    - path: "components/mdx/gallery.tsx"
      provides: "Gallery MDX component (2-up / 3-up grid)"
      exports: ["Gallery"]
    - path: "components/mdx/callout.tsx"
      provides: "Callout MDX component (note/warn/quote variants)"
      exports: ["Callout"]
    - path: "mdx-components.tsx"
      provides: "useMDXComponents global registration of Figure/Gallery/Callout"
      contains: "Figure"
    - path: "tests/mdx/components.test.tsx"
      provides: "RTL smoke tests for Figure/Gallery/Callout"
    - path: "tests/mdx/prose.test.tsx"
      provides: "MDXProse wrapper assertion (max-w-[65ch] + .prose class + center)"
    - path: "tests/mdx/registration.test.ts"
      provides: "Source assertion that mdx-components.tsx imports + registers all three components"
  key_links:
    - from: "mdx-components.tsx"
      to: "components/mdx/{figure,gallery,callout}"
      via: "named imports passed into MDXComponents map returned by useMDXComponents"
      pattern: "useMDXComponents"
    - from: "components/mdx/figure.tsx"
      to: "next/image"
      via: "default Image import + width/height/sizes/alt props"
      pattern: "from 'next/image'"
---

<objective>
Wave 1 ships the MDX component library required by PRJ-02. Four RSC components — `<MDXProse>`, `<Figure>`, `<Gallery>`, `<Callout>` — each in its own file under `components/mdx/`. `mdx-components.tsx` is extended from the Phase 2 stub to register Figure / Gallery / Callout via the `useMDXComponents` hook. Three test files lock the contract: RTL smoke tests for the three MDX-callable components, a wrapper assertion for MDXProse, and a source assertion for the registration channel.

Purpose: Wave 2's page composition can drop `<MDXProse>{<MDXBody />}</MDXProse>` without inventing layout, and any future MDX file can use `<Figure>`, `<Gallery>`, `<Callout>` without per-page imports.

Output: 4 component files + 3 test files + 1 extended `mdx-components.tsx`. Zero new dependencies. Zero `'use client'` directives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-project-detail-template/03-CONTEXT.md
@.planning/phases/03-project-detail-template/03-RESEARCH.md
@.planning/phases/03-project-detail-template/03-UI-SPEC.md
@.planning/phases/03-project-detail-template/03-VALIDATION.md
@.planning/phases/03-project-detail-template/03-00-PLAN.md
@mdx-components.tsx
@lib/utils.ts

<interfaces>
Wave 0 has already shipped (depends_on: [03-00]). The executor can rely on:

styles/tokens.css — declares --color-surface-2, --color-hairline, --color-accent, --color-danger, --color-text-primary, --color-text-secondary, --color-text-tertiary, --radius-sm, --radius-md, --text-h2, --text-h3, --font-weight-semibold, --font-mono, --motion-duration-fast, --motion-duration-base, --motion-ease-standard.

app/globals.css — `.prose` class governs typography rhythm inside the wrapper (h2/h3/p/a/code/pre/anchor styles). MDXProse just opens the .prose scope; it does not declare typography itself.

mdx-components.tsx (current Phase 2 stub):
```ts
import type { MDXComponents } from 'mdx/types'
const components: MDXComponents = {}
export function useMDXComponents(): MDXComponents {
  return components
}
```

lib/utils.ts:
- `cn(...inputs: ClassValue[]): string` — clsx + tailwind-merge. Use for conditional class composition.

next/image:
- `import Image from 'next/image'` — props: `src`, `alt`, `width`, `height`, `sizes`, `className`, `priority`. Required: `alt` (non-empty per UI-SPEC discipline).

@next/mdx convention:
- The `useMDXComponents` hook is the global registration point — `@next/mdx` looks it up automatically. A per-page `components` prop is for overrides only (Pitfall in research: don't pass a `components` prop to `<MDXBody />`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-01-01: Build MDXProse wrapper + RTL test</name>
  <files>components/mdx/prose.tsx, tests/mdx/prose.test.tsx</files>
  <read_first>
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Component Inventory #4 (MDXProse)" (markup spec, decision rationale)
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Page Composition → Width discipline" (max-w-[65ch] + mx-auto + 65ch is a typographic measure not a spacing token)
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Page Composition → Vertical rhythm" (Hero block bottom to MDX top: mt-12 mobile, mt-16 desktop)
    - app/globals.css (verify the `.prose` class is shipped in 03-00; this component opens that scope)
  </read_first>
  <behavior>
    - Test 1: <MDXProse><p>x</p></MDXProse> renders a wrapping <div> with classes including `mx-auto`, `max-w-[65ch]`, `mt-12`, `md:mt-16`, `prose`.
    - Test 2: children render as direct DOM descendants (no extra layer between wrapper div and <p>x</p>).
    - Test 3: TypeScript signature accepts `{ children: React.ReactNode }`.
  </behavior>
  <action>
    Step A — write `tests/mdx/prose.test.tsx` FIRST (RED):

        // tests/mdx/prose.test.tsx
        // Refs: 03-UI-SPEC § Component Inventory #4 (MDXProse) + § Page Composition.
        import { render } from '@testing-library/react'
        import { describe, expect, it } from 'vitest'
        import { MDXProse } from '@/components/mdx/prose'

        describe('<MDXProse>', () => {
          it('wraps children in a div with the prose width measure + .prose class + top margin', () => {
            const { container } = render(
              <MDXProse>
                <p>hello</p>
              </MDXProse>
            )
            const wrapper = container.firstElementChild as HTMLElement
            expect(wrapper).not.toBeNull()
            expect(wrapper.tagName).toBe('DIV')
            const cls = wrapper.className
            expect(cls).toMatch(/\bmx-auto\b/)
            expect(cls).toMatch(/max-w-\[65ch\]/)
            expect(cls).toMatch(/\bmt-12\b/)
            expect(cls).toMatch(/md:mt-16/)
            expect(cls).toMatch(/\bprose\b/)
          })

          it('renders children as direct descendants (no extra layer)', () => {
            const { container } = render(
              <MDXProse>
                <p data-testid="child">hello</p>
              </MDXProse>
            )
            const wrapper = container.firstElementChild as HTMLElement
            const child = wrapper.firstElementChild as HTMLElement
            expect(child.tagName).toBe('P')
            expect(child.dataset.testid).toBe('child')
          })
        })

    Run `pnpm vitest run tests/mdx/prose.test.tsx` — RED.

    Step B — write `components/mdx/prose.tsx` (GREEN):

        // components/mdx/prose.tsx
        // RSC wrapper that opens the .prose typographic scope and applies the
        // measure (~65ch). Used by app/(site)/projects/[slug]/page.tsx (Wave 2)
        // around the dynamically-imported MDX body.
        // Source: .planning/phases/03-project-detail-template/03-UI-SPEC.md § Component Inventory #4.

        interface MDXProseProps {
          children: React.ReactNode
        }

        export function MDXProse({ children }: MDXProseProps) {
          return (
            <div className="prose mx-auto max-w-[65ch] mt-12 md:mt-16">
              {children}
            </div>
          )
        }

    Run `pnpm vitest run tests/mdx/prose.test.tsx` — GREEN. Do NOT add `'use client'`. Do NOT include `max-w-prose` (Tailwind's built-in prose width is 65ch but UI-SPEC explicitly uses the arbitrary `max-w-[65ch]` for grep-ability + token discipline).
  </action>
  <verify>
    <automated>pnpm vitest run tests/mdx/prose.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `components/mdx/prose.tsx` exports `MDXProse` matching the signature above.
    - File contains no `'use client'` directive.
    - `tests/mdx/prose.test.tsx` 2 cases green.
    - `MDXProse` className contains `prose`, `mx-auto`, `max-w-[65ch]`, `mt-12`, `md:mt-16` (verifiable via grep on the source).
  </acceptance_criteria>
  <done>MDXProse wrapper shipped, prose-scope opens with the locked width measure, smoke tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-01-02: Build Figure + Gallery + Callout components + RTL smoke tests</name>
  <files>components/mdx/figure.tsx, components/mdx/gallery.tsx, components/mdx/callout.tsx, tests/mdx/components.test.tsx</files>
  <read_first>
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Component Inventory #5 (Figure)" — props, markup, sizes attribute, wide bleed math
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Component Inventory #6 (Gallery)" — items shape, columns prop, breakpoint grid behavior
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Component Inventory #7 (Callout)" — variants, left-border color mapping, no-icon discipline
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Color → Accent reserved-for list" (variant border colors are content-surface accents — DO NOT also add background tints per variant)
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Anti-Patterns" (no decorative icons inside callouts; variant signaled by border color only)
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Pitfall 10" (next/image sizes mismatch causes CLS — Figure default sizes spec'd in UI-SPEC §5)
    - lib/utils.ts (cn helper signature)
  </read_first>
  <behavior>
    - Figure test 1: renders a <figure> containing a next/image (test via `<img>` query — next/image renders to <img>) with src + alt + width 1200 + height 900 by default.
    - Figure test 2: caption prop renders a <figcaption> with the caption text.
    - Figure test 3: caption omitted → no <figcaption> in DOM.
    - Figure test 4: wide=true adds the bleed class string `mx-[calc((100%-100vw)/2+50%)]` and `max-w-[calc(min(100vw,72rem)-2rem)]` to the wrapping figure.
    - Gallery test 1: items=[a,b] renders 2 <figure> children inside a grid div with class `sm:grid-cols-2`.
    - Gallery test 2: items=[a,b,c] columns=3 renders 3 <figure> children inside a grid div with class `lg:grid-cols-3`.
    - Gallery test 3: each item with caption emits a <figcaption>; without caption emits none.
    - Gallery test 4: wide=true on Gallery applies the same bleed classes as Figure wide.
    - Callout test 1: default variant ('note') emits <aside> with class containing `border-l-4` and a class encoding accent border color (`border-l-[color:var(--color-accent)]` or equivalent).
    - Callout test 2: variant='warn' emits border color from --color-danger.
    - Callout test 3: variant='quote' emits border color from --color-text-tertiary AND italic class on the body.
    - Callout test 4: title prop renders an inner element containing the title text; absent → no title element.
    - Callout test 5: aside has bg-surface-2 + rounded-md + the documented padding ramp.
  </behavior>
  <action>
    Step A — write `tests/mdx/components.test.tsx` FIRST (RED). Cover all 13 cases above in three describe blocks:

        // tests/mdx/components.test.tsx
        // RTL smoke tests for Figure / Gallery / Callout MDX components.
        // Refs: 03-UI-SPEC § Component Inventory #5, #6, #7.
        import { render, screen } from '@testing-library/react'
        import { describe, expect, it } from 'vitest'
        import { Callout } from '@/components/mdx/callout'
        import { Figure } from '@/components/mdx/figure'
        import { Gallery } from '@/components/mdx/gallery'

        const WIDE_BLEED_MX = 'mx-[calc((100%-100vw)/2+50%)]'
        const WIDE_BLEED_MAX = 'max-w-[calc(min(100vw,72rem)-2rem)]'

        describe('<Figure>', () => {
          it('renders a <figure> containing an <img> with default 1200x900 + alt + src', () => {
            const { container } = render(<Figure src="/x.png" alt="alt-text" />)
            const fig = container.querySelector('figure')
            expect(fig).not.toBeNull()
            const img = container.querySelector('img')
            expect(img).not.toBeNull()
            expect(img!.getAttribute('alt')).toBe('alt-text')
            // next/image emits the src somewhere in the rendered <img> attribute
            // (src may be wrapped in /_next/image?...&url=); the alt is the stable contract.
          })

          it('renders <figcaption> when caption prop provided', () => {
            const { container } = render(
              <Figure src="/x.png" alt="a" caption="my caption" />
            )
            const cap = container.querySelector('figcaption')
            expect(cap?.textContent).toBe('my caption')
          })

          it('renders no <figcaption> when caption omitted', () => {
            const { container } = render(<Figure src="/x.png" alt="a" />)
            expect(container.querySelector('figcaption')).toBeNull()
          })

          it('applies wide bleed classes when wide=true', () => {
            const { container } = render(<Figure src="/x.png" alt="a" wide />)
            const fig = container.querySelector('figure')!
            expect(fig.className).toContain(WIDE_BLEED_MX)
            expect(fig.className).toContain(WIDE_BLEED_MAX)
          })
        })

        describe('<Gallery>', () => {
          const TWO = [
            { src: '/a.png', alt: 'a' },
            { src: '/b.png', alt: 'b' },
          ]
          const THREE = [
            { src: '/a.png', alt: 'a', caption: 'A' },
            { src: '/b.png', alt: 'b' },
            { src: '/c.png', alt: 'c', caption: 'C' },
          ]

          it('renders one <figure> per item; defaults to 2-up grid at sm+', () => {
            const { container } = render(<Gallery items={TWO} />)
            expect(container.querySelectorAll('figure').length).toBe(2)
            const grid = container.firstElementChild as HTMLElement
            expect(grid.className).toMatch(/sm:grid-cols-2/)
          })

          it('renders 3-up grid at lg+ when columns=3', () => {
            const { container } = render(<Gallery items={THREE} columns={3} />)
            expect(container.querySelectorAll('figure').length).toBe(3)
            const grid = container.firstElementChild as HTMLElement
            expect(grid.className).toMatch(/lg:grid-cols-3/)
          })

          it('renders <figcaption> only for items that provide caption', () => {
            const { container } = render(<Gallery items={THREE} columns={3} />)
            const captions = Array.from(container.querySelectorAll('figcaption'))
            expect(captions.map((c) => c.textContent)).toEqual(['A', 'C'])
          })

          it('applies wide bleed classes when wide=true', () => {
            const { container } = render(<Gallery items={TWO} wide />)
            const grid = container.firstElementChild as HTMLElement
            expect(grid.className).toContain(WIDE_BLEED_MX)
            expect(grid.className).toContain(WIDE_BLEED_MAX)
          })
        })

        describe('<Callout>', () => {
          it('default variant=note emits <aside> with accent left border + surface-2 bg + rounded-md + padding', () => {
            const { container } = render(<Callout>body</Callout>)
            const aside = container.querySelector('aside')!
            const cls = aside.className
            expect(cls).toMatch(/\bborder-l-4\b/)
            expect(cls).toMatch(/border-l-\[color:var\(--color-accent\)\]/)
            expect(cls).toMatch(/bg-\[color:var\(--color-surface-2\)\]/)
            expect(cls).toMatch(/rounded-md/)
            expect(cls).toMatch(/px-4/)
            expect(cls).toMatch(/py-4/)
          })

          it('variant=warn uses --color-danger left border', () => {
            const { container } = render(<Callout variant="warn">body</Callout>)
            const cls = container.querySelector('aside')!.className
            expect(cls).toMatch(/border-l-\[color:var\(--color-danger\)\]/)
          })

          it('variant=quote uses --color-text-tertiary border and italic body', () => {
            const { container } = render(<Callout variant="quote">body</Callout>)
            const aside = container.querySelector('aside')!
            expect(aside.className).toMatch(/border-l-\[color:var\(--color-text-tertiary\)\]/)
            // italic applied to inner body wrapper
            const body = aside.querySelector('div')!
            expect(body.className).toMatch(/\bitalic\b/)
          })

          it('renders title element when title prop set; omits otherwise', () => {
            const { container, rerender } = render(<Callout title="heads up">body</Callout>)
            expect(screen.getByText('heads up')).toBeTruthy()
            rerender(<Callout>body</Callout>)
            expect(screen.queryByText('heads up')).toBeNull()
          })

          it('emits children inside the body wrapper', () => {
            render(<Callout><p>hello</p></Callout>)
            expect(screen.getByText('hello').tagName).toBe('P')
          })
        })

    Run `pnpm vitest run tests/mdx/components.test.tsx` — RED on all 13 (component files don't exist yet).

    Step B — write `components/mdx/figure.tsx`:

        // components/mdx/figure.tsx
        // MDX-callable Figure component. Default width inherits prose; wide opt-in bleeds out to container.
        // Source: .planning/phases/03-project-detail-template/03-UI-SPEC.md § Component Inventory #5.
        import Image from 'next/image'
        import { cn } from '@/lib/utils'

        interface FigureProps {
          src: string
          alt: string
          caption?: string
          wide?: boolean
          width?: number
          height?: number
        }

        const WIDE_BLEED = 'mx-[calc((100%-100vw)/2+50%)] max-w-[calc(min(100vw,72rem)-2rem)]'

        export function Figure({
          src,
          alt,
          caption,
          wide = false,
          width = 1200,
          height = 900,
        }: FigureProps) {
          return (
            <figure className={cn('my-8', wide && WIDE_BLEED)}>
              <Image
                src={src}
                alt={alt}
                width={width}
                height={height}
                sizes={
                  wide
                    ? '(min-width: 768px) 1024px, 100vw'
                    : '(min-width: 768px) 65ch, 100vw'
                }
                className="rounded-none"
              />
              {caption ? (
                <figcaption className="mt-3 text-[color:var(--color-text-secondary)] text-[var(--text-label)] text-center font-sans">
                  {caption}
                </figcaption>
              ) : null}
            </figure>
          )
        }

    Step C — write `components/mdx/gallery.tsx`:

        // components/mdx/gallery.tsx
        // MDX-callable Gallery component. 2-up default; columns=3 opts in to 3-up at lg+.
        // Source: .planning/phases/03-project-detail-template/03-UI-SPEC.md § Component Inventory #6.
        import Image from 'next/image'
        import { cn } from '@/lib/utils'

        interface GalleryItem {
          src: string
          alt: string
          caption?: string
        }

        interface GalleryProps {
          items: ReadonlyArray<GalleryItem>
          columns?: 2 | 3
          wide?: boolean
        }

        const WIDE_BLEED = 'mx-[calc((100%-100vw)/2+50%)] max-w-[calc(min(100vw,72rem)-2rem)]'

        export function Gallery({ items, columns = 2, wide = false }: GalleryProps) {
          const gridCols =
            columns === 3
              ? 'grid-cols-1 sm:grid-cols-2 lg:grid-cols-3'
              : 'grid-cols-1 sm:grid-cols-2'
          return (
            <div
              className={cn(
                'my-8 grid gap-4 md:gap-6 lg:gap-8',
                gridCols,
                wide && WIDE_BLEED
              )}
            >
              {items.map((item) => (
                <figure key={item.src}>
                  <Image
                    src={item.src}
                    alt={item.alt}
                    width={800}
                    height={600}
                    sizes={
                      columns === 3
                        ? '(min-width: 1024px) 320px, (min-width: 640px) 50vw, 100vw'
                        : '(min-width: 640px) 50vw, 100vw'
                    }
                    className="rounded-none"
                  />
                  {item.caption ? (
                    <figcaption className="mt-3 text-[color:var(--color-text-secondary)] text-[var(--text-label)] text-center font-sans">
                      {item.caption}
                    </figcaption>
                  ) : null}
                </figure>
              ))}
            </div>
          )
        }

    Step D — write `components/mdx/callout.tsx`:

        // components/mdx/callout.tsx
        // MDX-callable Callout component. note/warn/quote variants encoded only via 4px left-border color.
        // No icon, no glyph, no per-variant background tint — the border carries the meaning.
        // Source: .planning/phases/03-project-detail-template/03-UI-SPEC.md § Component Inventory #7.
        import { cn } from '@/lib/utils'

        type CalloutVariant = 'note' | 'warn' | 'quote'

        interface CalloutProps {
          variant?: CalloutVariant
          title?: string
          children: React.ReactNode
          wide?: boolean
        }

        const VARIANT_BORDER: Record<CalloutVariant, string> = {
          note: 'border-l-[color:var(--color-accent)]',
          warn: 'border-l-[color:var(--color-danger)]',
          quote: 'border-l-[color:var(--color-text-tertiary)]',
        }

        const WIDE_BLEED = 'mx-[calc((100%-100vw)/2+50%)] max-w-[calc(min(100vw,72rem)-2rem)]'

        export function Callout({
          variant = 'note',
          title,
          children,
          wide = false,
        }: CalloutProps) {
          return (
            <aside
              className={cn(
                'my-8 px-4 py-4 md:px-6 md:py-5',
                'bg-[color:var(--color-surface-2)] rounded-md border-l-4',
                VARIANT_BORDER[variant],
                wide && WIDE_BLEED
              )}
            >
              {title ? (
                <p className="text-[var(--text-label)] font-medium text-[color:var(--color-text-primary)] mb-2 lowercase tracking-[0.02em] font-mono">
                  {title}
                </p>
              ) : null}
              <div
                className={cn(
                  'text-[var(--text-body)] text-[color:var(--color-text-secondary)] leading-relaxed [&>*+*]:mt-3',
                  variant === 'quote' && 'italic'
                )}
              >
                {children}
              </div>
            </aside>
          )
        }

    Run `pnpm vitest run tests/mdx/components.test.tsx` — expect ALL 13 GREEN. If a className-grep test fails because Tailwind v4's arbitrary-value syntax produced a slightly different class string, adjust the test grep to match the actual emitted class — but do NOT change the component to an alternative color path (the documented `bg-[color:var(--color-surface-2)]` syntax is locked).

    Do NOT add `'use client'` to any of the three components. Do NOT add hover transforms (Anti-Patterns: no `transform: scale()` on Figure/Gallery). Do NOT add icons to Callout (Anti-Patterns: no decorative icons inside callouts). Do NOT introduce a per-variant background-fill differentiator — the border-color IS the variant signal.
  </action>
  <verify>
    <automated>pnpm vitest run tests/mdx/components.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `components/mdx/figure.tsx`, `components/mdx/gallery.tsx`, `components/mdx/callout.tsx` exist; each exports the named component.
    - None of the three files contains `'use client'`.
    - All 13 assertions in `tests/mdx/components.test.tsx` green.
    - Each component imports from `@/lib/utils` (`cn`) for class composition; Figure + Gallery import `Image` from `next/image`.
    - Callout has no icon component imports (lucide-react, etc.) and no per-variant background classes.
  </acceptance_criteria>
  <done>Three MDX-callable components shipped per UI-SPEC; 13-case smoke suite green; zero client JS contributed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-01-03: Extend mdx-components.tsx to register Figure/Gallery/Callout + source-assertion test</name>
  <files>mdx-components.tsx, tests/mdx/registration.test.ts</files>
  <read_first>
    - mdx-components.tsx (current Phase 2 stub — empty MDXComponents map)
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Component map (mdx-components.tsx registration)" — exact registration block
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "mdx-components.tsx — Phase 3 extension" (verbatim source)
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Anti-Patterns to Avoid" (Don't pass MDX components as a `components` prop to <MDXBody />)
  </read_first>
  <behavior>
    - Test 1: source assertion that mdx-components.tsx imports `Figure` from `@/components/mdx/figure`.
    - Test 2: source assertion that mdx-components.tsx imports `Gallery` from `@/components/mdx/gallery`.
    - Test 3: source assertion that mdx-components.tsx imports `Callout` from `@/components/mdx/callout`.
    - Test 4: assert `useMDXComponents()` returned object has keys `Figure`, `Gallery`, `Callout` referencing those imports.
    - Test 5: source does NOT add h2/h3/pre/code overrides (those are handled by rehype plugins + .prose CSS at build time, per UI-SPEC).
  </behavior>
  <action>
    Step A — write `tests/mdx/registration.test.ts` FIRST (RED):

        // tests/mdx/registration.test.ts
        // Asserts mdx-components.tsx wires Figure/Gallery/Callout into the @next/mdx
        // global registration channel. Refs: 03-UI-SPEC § Component map.
        import { readFileSync } from 'node:fs'
        import { join } from 'node:path'
        import { describe, expect, it } from 'vitest'
        import { useMDXComponents } from '@/mdx-components'
        import { Callout } from '@/components/mdx/callout'
        import { Figure } from '@/components/mdx/figure'
        import { Gallery } from '@/components/mdx/gallery'

        const source = readFileSync(join(process.cwd(), 'mdx-components.tsx'), 'utf-8')

        describe('mdx-components.tsx', () => {
          it("imports Figure from @/components/mdx/figure", () => {
            expect(source).toMatch(/import\s*\{\s*Figure\s*\}\s*from\s*['"]@\/components\/mdx\/figure['"]/)
          })

          it("imports Gallery from @/components/mdx/gallery", () => {
            expect(source).toMatch(/import\s*\{\s*Gallery\s*\}\s*from\s*['"]@\/components\/mdx\/gallery['"]/)
          })

          it("imports Callout from @/components/mdx/callout", () => {
            expect(source).toMatch(/import\s*\{\s*Callout\s*\}\s*from\s*['"]@\/components\/mdx\/callout['"]/)
          })

          it('useMDXComponents() returns a map containing Figure/Gallery/Callout', () => {
            const components = useMDXComponents()
            expect(components.Figure).toBe(Figure)
            expect(components.Gallery).toBe(Gallery)
            expect(components.Callout).toBe(Callout)
          })

          it('does NOT add h2/h3/pre/code JSX overrides (rehype + .prose handle those)', () => {
            // Negative assertion — these property names should not appear as object keys
            expect(source).not.toMatch(/\bh2\s*:/)
            expect(source).not.toMatch(/\bh3\s*:/)
            expect(source).not.toMatch(/\bpre\s*:/)
            // 'code:' intentionally allowed only as part of larger identifiers (none expected)
          })
        })

    Run `pnpm vitest run tests/mdx/registration.test.ts` — RED on the import + map assertions.

    Step B — extend `mdx-components.tsx`. Replace the entire file with:

        // mdx-components.tsx — REQUIRED by @next/mdx for App Router.
        // Source: https://nextjs.org/docs/app/guides/mdx
        // Phase 3: registers Figure / Gallery / Callout. Headings, paragraphs, code blocks,
        // and inline code are styled via Tailwind in app/globals.css (.prose class) — no JSX
        // overrides needed because rehype plugins handle slug/anchor/syntax tokenization at build time.
        import type { MDXComponents } from 'mdx/types'
        import { Callout } from '@/components/mdx/callout'
        import { Figure } from '@/components/mdx/figure'
        import { Gallery } from '@/components/mdx/gallery'

        const components: MDXComponents = {
          Figure,
          Gallery,
          Callout,
        }

        export function useMDXComponents(): MDXComponents {
          return components
        }

    Run `pnpm vitest run tests/mdx/registration.test.ts` — GREEN on all 5.

    Do NOT add per-page `components` prop wiring elsewhere — `useMDXComponents` is the global channel and `@next/mdx` looks it up automatically (RESEARCH Anti-Pattern).
  </action>
  <verify>
    <automated>pnpm vitest run tests/mdx/registration.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `mdx-components.tsx` imports `Figure`, `Gallery`, `Callout` from `@/components/mdx/{figure,gallery,callout}`.
    - `useMDXComponents()` returns an object whose `Figure`, `Gallery`, `Callout` properties strictly equal the imported components.
    - Source contains no h2/h3/pre property keys in the components object.
    - All 5 registration tests green.
  </acceptance_criteria>
  <done>MDX components registered globally; @next/mdx pickup channel wired; source assertions + runtime equality tests green.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/mdx --reporter=dot` — all Wave 1 tests green (prose 2 + components 13 + registration 5 = 20 cases).
- `pnpm typecheck` — clean (component prop interfaces resolve; no `any` slipped in).
- `pnpm build` — succeeds; the unmodified `content/projects/myco.mdx` still compiles (no MDX components used yet, but the `useMDXComponents` hook now resolves).
- Phase 1 + Phase 2 test suites still green (no regression).
</verification>

<success_criteria>
- PRJ-02 partially satisfied at the component level: Figure/Gallery/Callout exist + are registered globally + render with the correct DOM shape.
- Wave 2 plans (`03-02`, `03-03`) can compose `<MDXProse>{<MDXBody />}</MDXProse>` and depend on the global registration channel automatically picking up Figure/Gallery/Callout in any future MDX file.
- Zero new dependencies installed in this wave.
- Zero `'use client'` directives in the four component files (RSC-only contribution).
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-detail-template/03-01-SUMMARY.md` recording:
- Files created (4 components + 3 tests + 1 mdx-components.tsx extension).
- Test results (20/20 green expected).
- Any deviation from the className strings spec'd in UI-SPEC, with the actual emitted Tailwind v4 class string.
- Confirmation that `pnpm typecheck` is clean and `pnpm build` still succeeds.
</output>
