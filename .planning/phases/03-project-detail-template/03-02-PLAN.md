---
phase: 03-project-detail-template
plan: 02
type: execute
wave: 2
depends_on: [03-00, 03-01]
files_modified:
  - components/projects/project-hero.tsx
  - components/projects/project-meta.tsx
  - components/projects/tag-chip-row.tsx
  - tests/projects/project-hero.test.tsx
  - tests/projects/project-meta.test.tsx
  - tests/projects/tag-chip-row.test.tsx
autonomous: true
requirements: [PRJ-04, PRJ-06]

must_haves:
  truths:
    - "<ProjectHero> renders Variant A (image-present, 2-column grid at md+) when isPlaceholderHero(hero.src) === false"
    - "<ProjectHero> renders Variant B (text-only, no <img> at all) when isPlaceholderHero(hero.src) === true — Myco currently triggers this branch"
    - "Hero <h1> uses the display type ramp; tagline uses --color-text-secondary; year is rendered inside a <time dateTime={String(year)}> element"
    - "<ProjectMeta> renders the metadata row with role=list and aria-label='Project metadata'; year + tag chips + (repo link OR 'code private' label) appear in that order"
    - "Public projects render a `repo ↗` link to project.links.repo with target=_blank and rel=noopener noreferrer; the ↗ glyph is aria-hidden"
    - "Private projects render the literal string 'code private' (lowercase, two words, no glyph) inside a non-interactive <span>; gating is on project.visibility === 'private', NOT on repo === undefined"
    - "When private, NO anchor element on the page references a github.com repo URL for the project — the schema strips it; the meta component must not synthesize one"
    - "<TagChipRow> renders one <a> per tag pointing at /projects?tag={tag}; the chip wrapper uses px-3 py-2 -my-2 + bg-surface-2 + rounded-sm + Geist Mono 14px lowercase"
    - "When the project's tag list includes 'code-private' (auto-added by schema), it renders as a chip identically to other chips — no special highlight"
  artifacts:
    - path: "components/projects/project-hero.tsx"
      provides: "ProjectHero RSC — image-present vs text-only branching via isPlaceholderHero"
      exports: ["ProjectHero"]
    - path: "components/projects/project-meta.tsx"
      provides: "ProjectMeta RSC — year + TagChipRow + repo/private label in one row"
      exports: ["ProjectMeta"]
    - path: "components/projects/tag-chip-row.tsx"
      provides: "TagChipRow RSC — mono lowercase chips linking to /projects?tag=X"
      exports: ["TagChipRow"]
    - path: "tests/projects/project-hero.test.tsx"
      provides: "image-vs-text variant smoke tests + display-type-ramp class assertion"
    - path: "tests/projects/project-meta.test.tsx"
      provides: "Privacy rendering contract — code private label, repo-URL absence on private, repo link presence on public"
    - path: "tests/projects/tag-chip-row.test.tsx"
      provides: "Chip markup + href + code-private chip styled identically"
  key_links:
    - from: "components/projects/project-hero.tsx"
      to: "lib/hero-fallback.ts"
      via: "isPlaceholderHero import; gates next/image rendering"
      pattern: "isPlaceholderHero"
    - from: "components/projects/project-meta.tsx"
      to: "components/projects/tag-chip-row.tsx"
      via: "TagChipRow named import"
      pattern: "TagChipRow"
    - from: "components/projects/project-meta.tsx"
      to: "project.visibility (Project type from lib/schemas)"
      via: "ternary on visibility === 'private' for label rendering"
      pattern: "visibility"
---

<objective>
Wave 2 lane A — hero + metadata + tag chips. Three RSC components live under `components/projects/`. The hero handles image-present and text-only variants by inspecting `isPlaceholderHero(hero.src)` (shipped in Wave 0). The metadata component composes the year + tag chips + repo-link-or-private-label in a single accessible row. The tag chip row renders mono lowercase chips linking to `/projects?tag=X` (Phase 4 will land the destination; Phase 1's 404 handles the gap gracefully). Privacy rendering is the load-bearing PRJ-06 contract — three test files lock the visible behavior.

Purpose: Wave 2's other lane (Plan 03) composes these with the page route + NextProjectBlock; both lanes are independent on disk (different files, both depend only on Waves 0 + 1) and can run in parallel.

Output: 3 component files + 3 test files. Zero new dependencies. Zero `'use client'` directives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-project-detail-template/03-CONTEXT.md
@.planning/phases/03-project-detail-template/03-RESEARCH.md
@.planning/phases/03-project-detail-template/03-UI-SPEC.md
@.planning/phases/03-project-detail-template/03-VALIDATION.md
@.planning/phases/03-project-detail-template/03-00-PLAN.md
@.planning/phases/03-project-detail-template/03-01-PLAN.md
@lib/schemas.ts
@lib/tags.ts
@lib/utils.ts
@lib/hero-fallback.ts
@tests/fixtures/projects/valid-private.mdx
@tests/fixtures/projects/valid-hero.mdx

<interfaces>
Project type (from lib/schemas.ts via lib/content.ts → re-exported through lib/projects.ts):
- `slug: string` (kebab-case)
- `title: string` (non-empty)
- `tagline: string` (max 140)
- `year: number` (2000–2100)
- `tier: 'hero' | 'secondary'`
- `order: number`
- `status: 'active' | 'archived' | 'paused'`
- `visibility: 'public' | 'private'`
- `tags: readonly Tag[]` (min 1)
- `stack: readonly string[]`
- `links: { repo?: string; live?: string; docs?: string; npm?: string }` (private projects: schema strips `repo`)
- `hero: { src: string; alt: string }`
- `gallery: readonly { src; alt; caption? }[]`
- `outcomes: readonly string[]` (max 5)
- `description: string` (max 160)
- `ogImage?: string`

Tag type (from lib/tags.ts):
- `Tag = 'local-first' | 'autonomous' | 'open-source' | 'ai' | 'agents' | 'distributed' | 'typescript' | 'python' | 'saas' | 'cli' | 'code-private'`
- `TAG_LABELS: Record<Tag, string>` available — but UI-SPEC chips use the Tag value verbatim (lowercase, no remapping). Do NOT use TAG_LABELS for chip text.

Wave 0 helper (from lib/hero-fallback.ts):
- `isPlaceholderHero(src: string): boolean` — regex match on `/images/projects/*/hero-placeholder.{png,jpg,jpeg,webp}`. RSC-safe.

Existing fixtures (Phase 2):
- `tests/fixtures/projects/valid-hero.mdx` — public hero project
- `tests/fixtures/projects/valid-private.mdx` — private project, slug `valid-private`. Schema strips `links.repo` and adds `code-private` to tags. Use as the source of expected privacy data shape in tests (do NOT load the file at runtime in unit tests — construct mock Project objects matching the post-transform shape).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-02-01: Build TagChipRow + RTL test</name>
  <files>components/projects/tag-chip-row.tsx, tests/projects/tag-chip-row.test.tsx</files>
  <read_first>
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Component Inventory #3 (TagChipRow)" — markup + states + visited rule + code-private chip discipline
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Spacing Scale → Tag chip touch target" (px-3 py-2 -my-2 hit-area math)
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Color → Accent reserved-for list" (chips do NOT use accent on hover — color shifts secondary→primary)
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Code Examples → Tag chip row example" (verbatim implementation)
    - lib/tags.ts (Tag type)
  </read_first>
  <behavior>
    - Test 1: tags=['local-first', 'typescript'] renders 2 <a> elements with text 'local-first' and 'typescript' respectively.
    - Test 2: each <a> has href `/projects?tag={tag}` (verbatim — don't URL-encode hyphens; they're URL-safe).
    - Test 3: each chip className contains all of: `inline-flex`, `px-3`, `py-2`, `-my-2`, `bg-[color:var(--color-surface-2)]`, `rounded-sm`, `font-mono`, `lowercase`, `text-[color:var(--color-text-secondary)]`, `hover:text-[color:var(--color-text-primary)]`.
    - Test 4: tags=['typescript', 'code-private'] renders a chip for 'code-private' with the same className as 'typescript' (no special highlight) and href `/projects?tag=code-private`.
    - Test 5: tags=[] renders no <a> elements (defensive).
    - Test 6: returns React fragment (no extra wrapping div) so the meta row's flex layout includes chips as direct children.
  </behavior>
  <action>
    Step A — write `tests/projects/tag-chip-row.test.tsx` FIRST (RED):

        // tests/projects/tag-chip-row.test.tsx
        // Refs: 03-UI-SPEC § Component Inventory #3 (TagChipRow); 03-RESEARCH § Tag chip row example.
        import { render } from '@testing-library/react'
        import { describe, expect, it } from 'vitest'
        import { TagChipRow } from '@/components/projects/tag-chip-row'

        describe('<TagChipRow>', () => {
          it('renders one <a> per tag with text matching the tag value', () => {
            const { container } = render(<TagChipRow tags={['local-first', 'typescript']} />)
            const anchors = Array.from(container.querySelectorAll('a'))
            expect(anchors.length).toBe(2)
            expect(anchors[0]?.textContent?.trim()).toBe('local-first')
            expect(anchors[1]?.textContent?.trim()).toBe('typescript')
          })

          it('href points to /projects?tag={tag} verbatim (hyphens are URL-safe)', () => {
            const { container } = render(<TagChipRow tags={['local-first', 'code-private']} />)
            const hrefs = Array.from(container.querySelectorAll('a')).map((a) => a.getAttribute('href'))
            expect(hrefs).toEqual(['/projects?tag=local-first', '/projects?tag=code-private'])
          })

          it('each chip has the locked className stack (surface-2 + rounded-sm + mono lowercase + secondary→primary hover)', () => {
            const { container } = render(<TagChipRow tags={['typescript']} />)
            const chip = container.querySelector('a')!
            const cls = chip.className
            for (const required of [
              'inline-flex',
              'px-3',
              'py-2',
              '-my-2',
              'bg-[color:var(--color-surface-2)]',
              'rounded-sm',
              'font-mono',
              'lowercase',
              'text-[color:var(--color-text-secondary)]',
              'hover:text-[color:var(--color-text-primary)]',
            ]) {
              expect(cls).toContain(required)
            }
          })

          it("renders the auto-added 'code-private' chip identically to other chips (no special highlight)", () => {
            const { container } = render(<TagChipRow tags={['typescript', 'code-private']} />)
            const [first, second] = Array.from(container.querySelectorAll('a'))
            expect(first?.className).toBe(second?.className)
          })

          it('renders nothing when tags is empty (defensive)', () => {
            const { container } = render(<TagChipRow tags={[]} />)
            expect(container.querySelectorAll('a').length).toBe(0)
          })

          it('returns a React fragment — no extra wrapping div around the chips', () => {
            const { container } = render(<TagChipRow tags={['typescript']} />)
            // First element child should be the <a>, not a wrapping div
            expect(container.firstElementChild?.tagName).toBe('A')
          })
        })

    Run `pnpm vitest run tests/projects/tag-chip-row.test.tsx` — RED.

    Step B — write `components/projects/tag-chip-row.tsx`:

        // components/projects/tag-chip-row.tsx
        // RSC tag chip row. Each chip is a plain <a> linking to the (Phase 4) projects-index filter.
        // The href works statically and degrades to Phase 1's custom 404 until Phase 4 ships the destination.
        // Source: .planning/phases/03-project-detail-template/03-UI-SPEC.md § Component Inventory #3.
        import type { Tag } from '@/lib/tags'

        interface TagChipRowProps {
          tags: readonly Tag[]
        }

        export function TagChipRow({ tags }: TagChipRowProps) {
          return (
            <>
              {tags.map((tag) => (
                <a
                  key={tag}
                  href={`/projects?tag=${tag}`}
                  className="
                    inline-flex items-center px-3 py-2 -my-2
                    bg-[color:var(--color-surface-2)] rounded-sm
                    font-mono text-[var(--text-label)] font-medium tracking-[0.02em] lowercase
                    text-[color:var(--color-text-secondary)]
                    hover:text-[color:var(--color-text-primary)]
                    transition-colors duration-[120ms] ease-linear
                  "
                >
                  {tag}
                </a>
              ))}
            </>
          )
        }

    Run `pnpm vitest run tests/projects/tag-chip-row.test.tsx` — GREEN. Do NOT add focus-visible styles inline — `app/globals.css`'s `:where(a, ...)` global focus-visible already covers anchors. Do NOT use `TAG_LABELS` mapping for chip text (UI-SPEC chips render the Tag value verbatim).
  </action>
  <verify>
    <automated>pnpm vitest run tests/projects/tag-chip-row.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `components/projects/tag-chip-row.tsx` exports `TagChipRow` accepting `{ tags: readonly Tag[] }`.
    - File contains no `'use client'`.
    - All 6 tests in `tests/projects/tag-chip-row.test.tsx` green.
    - `code-private` tag's chip className is byte-identical to other chips.
    - Component returns a fragment (no wrapping div).
  </acceptance_criteria>
  <done>TagChipRow shipped per UI-SPEC; 6-case smoke suite green; chip styling locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-02-02: Build ProjectMeta + privacy-rendering RTL test (PRJ-06 load-bearing)</name>
  <files>components/projects/project-meta.tsx, tests/projects/project-meta.test.tsx</files>
  <read_first>
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Component Inventory #2 (ProjectMeta)" — markup, role=list, gap-3, year inside <time>
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Privacy Rendering Contract" — exact `code private` string (lowercase, no glyph), gating on visibility (NOT on repo absence), 3 verification clauses
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Copywriting Contract → Static / locked strings" — `repo` (with aria-hidden ↗ glyph), `code private` exact spelling
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Anti-Patterns to Avoid → Don't render `links.repo` from `project.links.repo` for private projects"
    - lib/schemas.ts (Project shape — visibility, links.repo, tags, year)
    - tests/projects/tag-chip-row.test.tsx (sibling — TagChipRow contract is now stable)
  </read_first>
  <behavior>
    - Test 1 (public): visibility='public' + links.repo set → renders `repo ↗` link with href = repo URL, target=_blank, rel=noopener noreferrer; ↗ span has aria-hidden=true; no `code private` text anywhere.
    - Test 2 (private): visibility='private' + tags includes 'code-private' (post-schema-transform shape) + links.repo undefined → renders the literal text 'code private' inside a non-interactive <span>; no anchor with the project's would-be repo URL; the `code-private` tag still renders as a chip in the chip row.
    - Test 3 (private): private label color class includes `text-[color:var(--color-text-tertiary)]`.
    - Test 4 (year semantic): year renders inside a `<time>` with `dateTime` attribute equal to String(year).
    - Test 5 (a11y row): outer wrapper has `role="list"` and `aria-label="Project metadata"`; uses flex + flex-wrap + items-center + gap-3.
    - Test 6 (negative repo URL): if visibility='private' AND a stale repo URL is passed in (defensive — schema should have stripped it), the component STILL must not render an anchor with that URL. The component branches on visibility.
    - Test 7 (composition): TagChipRow appears between the year and the repo/private label.
  </behavior>
  <action>
    Step A — write `tests/projects/project-meta.test.tsx` FIRST (RED):

        // tests/projects/project-meta.test.tsx
        // PRJ-06 load-bearing: privacy rendering contract.
        // Refs: 03-UI-SPEC § Component Inventory #2 (ProjectMeta) + § Privacy Rendering Contract.
        import { render, within } from '@testing-library/react'
        import { describe, expect, it } from 'vitest'
        import { ProjectMeta } from '@/components/projects/project-meta'

        describe('<ProjectMeta>', () => {
          it('public project renders `repo` link with ↗ glyph, target=_blank, rel=noopener noreferrer', () => {
            const { container } = render(
              <ProjectMeta
                year={2025}
                tags={['typescript']}
                visibility="public"
                repoUrl="https://github.com/olivelliott/myco"
              />
            )
            const repoLink = Array.from(container.querySelectorAll('a')).find(
              (a) => a.getAttribute('href') === 'https://github.com/olivelliott/myco'
            )
            expect(repoLink).toBeDefined()
            expect(repoLink!.getAttribute('target')).toBe('_blank')
            expect(repoLink!.getAttribute('rel')).toBe('noopener noreferrer')
            expect(repoLink!.textContent).toContain('repo')
            const glyph = repoLink!.querySelector('span[aria-hidden="true"]')
            expect(glyph).not.toBeNull()
            expect(glyph!.textContent).toBe('↗')
            // No private label
            expect(container.textContent).not.toContain('code private')
          })

          it('private project renders the literal "code private" label as a non-interactive <span>', () => {
            const { container } = render(
              <ProjectMeta
                year={2025}
                tags={['typescript', 'code-private']}
                visibility="private"
              />
            )
            const labels = Array.from(container.querySelectorAll('span')).filter(
              (s) => s.textContent === 'code private'
            )
            expect(labels.length).toBe(1)
            // Not a link
            expect(labels[0]?.tagName).toBe('SPAN')
            // No anchor element references "code private" via text
            const anchors = Array.from(container.querySelectorAll('a'))
            expect(anchors.find((a) => a.textContent === 'code private')).toBeUndefined()
          })

          it('private label class uses --color-text-tertiary (NOT accent)', () => {
            const { container } = render(
              <ProjectMeta year={2025} tags={['code-private']} visibility="private" />
            )
            const label = Array.from(container.querySelectorAll('span')).find(
              (s) => s.textContent === 'code private'
            )!
            expect(label.className).toContain('text-[color:var(--color-text-tertiary)]')
          })

          it('year renders inside <time dateTime={String(year)}>', () => {
            const { container } = render(
              <ProjectMeta year={2025} tags={['typescript']} visibility="public" repoUrl="https://x" />
            )
            const time = container.querySelector('time')!
            expect(time.getAttribute('dateTime')).toBe('2025')
            expect(time.textContent).toBe('2025')
          })

          it('outer wrapper is role=list with aria-label="Project metadata" and flex layout with gap-3', () => {
            const { container } = render(
              <ProjectMeta year={2025} tags={['typescript']} visibility="public" repoUrl="https://x" />
            )
            const row = container.firstElementChild as HTMLElement
            expect(row.getAttribute('role')).toBe('list')
            expect(row.getAttribute('aria-label')).toBe('Project metadata')
            expect(row.className).toMatch(/\bflex\b/)
            expect(row.className).toMatch(/flex-wrap/)
            expect(row.className).toMatch(/items-center/)
            expect(row.className).toMatch(/\bgap-3\b/)
          })

          it('PRJ-06: even if a stale repoUrl prop sneaks through, visibility=private suppresses the anchor', () => {
            const { container } = render(
              <ProjectMeta
                year={2025}
                tags={['code-private']}
                visibility="private"
                repoUrl="https://github.com/private-org/secret"
              />
            )
            const anchors = Array.from(container.querySelectorAll('a'))
            // No anchor with the stale repo URL
            expect(anchors.find((a) => a.getAttribute('href')?.includes('private-org/secret'))).toBeUndefined()
            // Still has the `code private` label
            const labels = Array.from(container.querySelectorAll('span')).filter((s) => s.textContent === 'code private')
            expect(labels.length).toBe(1)
          })

          it('composition order: year → tag chips → repo-or-private label', () => {
            const { container } = render(
              <ProjectMeta
                year={2025}
                tags={['typescript', 'local-first']}
                visibility="public"
                repoUrl="https://github.com/x/y"
              />
            )
            const row = container.firstElementChild as HTMLElement
            const children = Array.from(row.children)
            // First child = <time>
            expect(children[0]?.tagName).toBe('TIME')
            // Last interactive child = the repo link
            const last = children[children.length - 1]!
            expect(last.tagName).toBe('A')
            expect(last.getAttribute('href')).toBe('https://github.com/x/y')
          })
        })

    Run `pnpm vitest run tests/projects/project-meta.test.tsx` — RED.

    Step B — write `components/projects/project-meta.tsx`:

        // components/projects/project-meta.tsx
        // RSC. Year + tag chips + repo-or-private-label, single accessible row.
        // Privacy gating is on visibility, NOT on repoUrl absence (per UI-SPEC + RESEARCH anti-pattern).
        // Source: .planning/phases/03-project-detail-template/03-UI-SPEC.md § Component Inventory #2 + § Privacy Rendering Contract.
        import type { Tag } from '@/lib/tags'
        import { TagChipRow } from './tag-chip-row'

        interface ProjectMetaProps {
          year: number
          tags: readonly Tag[]
          visibility: 'public' | 'private'
          repoUrl?: string
        }

        export function ProjectMeta({ year, tags, visibility, repoUrl }: ProjectMetaProps) {
          const isPrivate = visibility === 'private'
          return (
            <div
              role="list"
              aria-label="Project metadata"
              className="flex flex-wrap items-center gap-3"
            >
              <time
                dateTime={String(year)}
                className="font-mono text-[var(--text-label)] font-medium tracking-[0.02em] text-[color:var(--color-text-secondary)]"
              >
                {year}
              </time>
              <TagChipRow tags={tags} />
              {isPrivate ? (
                <span className="font-mono text-[var(--text-label)] font-medium tracking-[0.02em] lowercase text-[color:var(--color-text-tertiary)]">
                  code private
                </span>
              ) : repoUrl ? (
                <a
                  href={repoUrl}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="
                    font-mono text-[var(--text-label)] font-medium tracking-[0.02em] lowercase
                    text-[color:var(--color-accent)] hover:text-[color:var(--color-accent-hover)]
                    py-2 -my-2
                    no-underline hover:underline hover:underline-offset-4 hover:decoration-1
                    transition-colors duration-[120ms] ease-linear
                  "
                >
                  repo <span aria-hidden="true">↗</span>
                </a>
              ) : null}
            </div>
          )
        }

    Run `pnpm vitest run tests/projects/project-meta.test.tsx` — GREEN. Confirm by re-reading test 6: even if `repoUrl="https://github.com/private-org/secret"` is passed alongside `visibility="private"`, the component renders the private-label branch (the `isPrivate` check short-circuits), satisfying the privacy verification clause.

    Do NOT add a `<a aria-label="...">` to the private label (UI-SPEC: "no aria-label, no title tooltip — the visible text is sufficient"). Do NOT add an icon to either branch (UI-SPEC: text-only, no lock icon). Do NOT branch on `repoUrl === undefined` (Anti-Pattern: gate on visibility).
  </action>
  <verify>
    <automated>pnpm vitest run tests/projects/project-meta.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `components/projects/project-meta.tsx` exports `ProjectMeta` matching the interface above.
    - File contains no `'use client'`.
    - All 7 tests in `tests/projects/project-meta.test.tsx` green.
    - Component branches on `visibility === 'private'`, not on `repoUrl === undefined`.
    - The string `code private` appears in the source EXACTLY once (lowercase, two words, no punctuation).
    - The string `repo` appears as link text with an `aria-hidden="true"` `↗` glyph.
  </acceptance_criteria>
  <done>ProjectMeta shipped with the privacy rendering contract enforced via visibility-gated branch; 7-case test suite green; PRJ-06 partially satisfied (page composition still required for full coverage).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-02-03: Build ProjectHero (image-vs-text variant branching) + RTL test</name>
  <files>components/projects/project-hero.tsx, tests/projects/project-hero.test.tsx</files>
  <read_first>
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Component Inventory #1 (ProjectHero)" — props, composition, image-vs-text branching
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Hero Variants" — Variant A (image-present, 2-col grid at md+) AND Variant B (text-only, no <img>, no placeholder rectangle, no skeleton)
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Page Composition → Document outline" (semantic <header> as the hero root)
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Page Composition → Width discipline" (hero text col 50% then 7/12 at lg+; image col 50% then 5/12 at lg+)
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Typography → Display" (hero title uses display ramp — 32–48px clamp, weight 500, tracking-tight)
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Pitfall 10" (next/image sizes mismatch — Variant A uses sizes="(min-width: 768px) 50vw, 100vw")
    - lib/hero-fallback.ts (isPlaceholderHero — Wave 0 helper)
    - components/projects/project-meta.tsx (sibling — composed inside the hero's text col)
  </read_first>
  <behavior>
    - Test 1 (Variant A image-present): hero.src='/images/projects/myco/cover.png' → renders an <img> tag (next/image) with the src, alt, width 1200, height 900, sizes attribute matching the locked spec.
    - Test 2 (Variant A): renders <header> with grid layout class `md:grid` AND `md:grid-cols-12`.
    - Test 3 (Variant B text-only — Myco's actual state): hero.src='/images/projects/myco/hero-placeholder.png' → renders <header> with NO <img> element anywhere.
    - Test 4 (Variant B): even text-only renders the <h1> with the project title at display type ramp.
    - Test 5 (Variant B): renders the tagline as a <p> with text-secondary color class.
    - Test 6 (composition): hero contains <ProjectMeta> output — assert by querying for the role=list / aria-label="Project metadata" wrapper.
    - Test 7 (h1 contract): there is exactly ONE <h1> in the hero, containing the project title (UI-SPEC: One H1 per page, only on the hero).
    - Test 8 (a11y): hero root is <header>, not <div>.
  </behavior>
  <action>
    Step A — write `tests/projects/project-hero.test.tsx` FIRST (RED):

        // tests/projects/project-hero.test.tsx
        // Refs: 03-UI-SPEC § Component Inventory #1 (ProjectHero) + § Hero Variants.
        import { render } from '@testing-library/react'
        import { describe, expect, it } from 'vitest'
        import { ProjectHero } from '@/components/projects/project-hero'

        const COMMON = {
          title: 'Myco',
          tagline: 'A persistent cognitive layer.',
          year: 2025,
          tags: ['local-first', 'typescript'] as const,
        }

        describe('<ProjectHero>', () => {
          it('Variant A (image-present): renders <img> with sizes="(min-width: 768px) 50vw, 100vw"', () => {
            const { container } = render(
              <ProjectHero
                {...COMMON}
                visibility="public"
                repoUrl="https://github.com/olivelliott/myco"
                hero={{ src: '/images/projects/myco/cover.png', alt: 'Cover artwork' }}
              />
            )
            const img = container.querySelector('img')
            expect(img).not.toBeNull()
            expect(img!.getAttribute('alt')).toBe('Cover artwork')
            expect(img!.getAttribute('sizes')).toBe('(min-width: 768px) 50vw, 100vw')
          })

          it('Variant A: <header> uses md:grid + md:grid-cols-12', () => {
            const { container } = render(
              <ProjectHero
                {...COMMON}
                visibility="public"
                repoUrl="https://github.com/olivelliott/myco"
                hero={{ src: '/images/projects/myco/cover.png', alt: 'Cover artwork' }}
              />
            )
            const header = container.querySelector('header')!
            expect(header.className).toMatch(/md:grid\b/)
            expect(header.className).toMatch(/md:grid-cols-12/)
          })

          it('Variant B (placeholder hero): renders NO <img> anywhere — Myco currently triggers this', () => {
            const { container } = render(
              <ProjectHero
                {...COMMON}
                visibility="public"
                repoUrl="https://github.com/olivelliott/myco"
                hero={{
                  src: '/images/projects/myco/hero-placeholder.png',
                  alt: 'Myco placeholder',
                }}
              />
            )
            expect(container.querySelector('img')).toBeNull()
          })

          it('Variant B: still renders <h1> with the project title at display ramp', () => {
            const { container } = render(
              <ProjectHero
                {...COMMON}
                visibility="public"
                repoUrl="https://x"
                hero={{ src: '/images/projects/myco/hero-placeholder.png', alt: 'a' }}
              />
            )
            const h1 = container.querySelector('h1')!
            expect(h1.textContent).toBe('Myco')
            const cls = h1.className
            // Display ramp uses --text-display token OR text-display-derived class.
            // We assert the locked CSS variable is referenced by the className stack.
            expect(cls).toMatch(/text-\[var\(--text-display\)\]|text-display/)
          })

          it('Variant B: renders tagline as <p> with text-secondary color class', () => {
            const { container } = render(
              <ProjectHero
                {...COMMON}
                visibility="public"
                repoUrl="https://x"
                hero={{ src: '/images/projects/myco/hero-placeholder.png', alt: 'a' }}
              />
            )
            const p = Array.from(container.querySelectorAll('p')).find(
              (el) => el.textContent === COMMON.tagline
            )!
            expect(p).toBeDefined()
            expect(p.className).toContain('text-[color:var(--color-text-secondary)]')
          })

          it('hero composes <ProjectMeta> (role=list aria-label="Project metadata")', () => {
            const { container } = render(
              <ProjectHero
                {...COMMON}
                visibility="public"
                repoUrl="https://x"
                hero={{ src: '/images/projects/myco/hero-placeholder.png', alt: 'a' }}
              />
            )
            const row = container.querySelector('[role="list"][aria-label="Project metadata"]')
            expect(row).not.toBeNull()
          })

          it('renders exactly one <h1> with the project title', () => {
            const { container } = render(
              <ProjectHero
                {...COMMON}
                visibility="public"
                repoUrl="https://x"
                hero={{ src: '/images/projects/myco/hero-placeholder.png', alt: 'a' }}
              />
            )
            const h1s = container.querySelectorAll('h1')
            expect(h1s.length).toBe(1)
            expect(h1s[0]?.textContent).toBe('Myco')
          })

          it('hero root is a semantic <header> element', () => {
            const { container } = render(
              <ProjectHero
                {...COMMON}
                visibility="public"
                repoUrl="https://x"
                hero={{ src: '/images/projects/myco/hero-placeholder.png', alt: 'a' }}
              />
            )
            expect(container.firstElementChild?.tagName).toBe('HEADER')
          })
        })

    Run `pnpm vitest run tests/projects/project-hero.test.tsx` — RED.

    Step B — write `components/projects/project-hero.tsx`:

        // components/projects/project-hero.tsx
        // RSC hero block. Branches on isPlaceholderHero(hero.src) to pick image-present
        // (Variant A: 2-column on md+) vs text-only (Variant B: single column, no <img>).
        // Source: .planning/phases/03-project-detail-template/03-UI-SPEC.md § Component Inventory #1 + § Hero Variants.
        import Image from 'next/image'
        import { isPlaceholderHero } from '@/lib/hero-fallback'
        import type { Tag } from '@/lib/tags'
        import { ProjectMeta } from './project-meta'

        interface ProjectHeroProps {
          title: string
          tagline: string
          year: number
          tags: readonly Tag[]
          visibility: 'public' | 'private'
          repoUrl?: string
          hero: { src: string; alt: string }
        }

        export function ProjectHero({
          title,
          tagline,
          year,
          tags,
          visibility,
          repoUrl,
          hero,
        }: ProjectHeroProps) {
          const isText = isPlaceholderHero(hero.src)

          const textCol = (
            <div className="flex flex-col gap-6 md:col-span-7">
              <h1 className="text-[var(--text-display)] leading-[1.15] tracking-[-0.02em] font-medium text-[color:var(--color-text-primary)]">
                {title}
              </h1>
              <p className="text-[var(--text-body)] leading-[1.6] text-[color:var(--color-text-secondary)]">
                {tagline}
              </p>
              <ProjectMeta
                year={year}
                tags={tags}
                visibility={visibility}
                repoUrl={repoUrl}
              />
            </div>
          )

          if (isText) {
            return (
              <header className="w-full">
                <div className="flex flex-col gap-6">
                  <h1 className="text-[var(--text-display)] leading-[1.15] tracking-[-0.02em] font-medium text-[color:var(--color-text-primary)]">
                    {title}
                  </h1>
                  <p className="text-[var(--text-body)] leading-[1.6] text-[color:var(--color-text-secondary)]">
                    {tagline}
                  </p>
                  <ProjectMeta
                    year={year}
                    tags={tags}
                    visibility={visibility}
                    repoUrl={repoUrl}
                  />
                </div>
              </header>
            )
          }

          return (
            <header className="w-full md:grid md:grid-cols-12 md:gap-8 items-start">
              {textCol}
              <div className="mt-8 md:mt-0 md:col-span-5">
                <Image
                  src={hero.src}
                  alt={hero.alt}
                  width={1200}
                  height={900}
                  sizes="(min-width: 768px) 50vw, 100vw"
                  priority
                  className="rounded-none w-full h-auto"
                />
              </div>
            </header>
          )
        }

    Run `pnpm vitest run tests/projects/project-hero.test.tsx` — GREEN on all 8.

    Do NOT add `'use client'`. Do NOT add `<FadeIn>` wrapper around the hero (UI-SPEC Anti-Pattern: no transform animations on first-viewport elements). Do NOT render a placeholder rectangle / skeleton in Variant B (UI-SPEC: "no extra spacer, no visible 'image coming' copy, no placeholder rectangle"). Do NOT auto-promote Variant B title to a larger size (UI-SPEC: "Hero title rendered at the same display scale").
  </action>
  <verify>
    <automated>pnpm vitest run tests/projects/project-hero.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `components/projects/project-hero.tsx` exports `ProjectHero` matching the props interface.
    - File contains no `'use client'`.
    - All 8 tests in `tests/projects/project-hero.test.tsx` green.
    - Variant A renders `<Image>` with `sizes="(min-width: 768px) 50vw, 100vw"` and `priority`.
    - Variant B renders zero `<img>` tags (verifiable via `container.querySelector('img') === null`).
    - Hero root tag is `<header>`.
    - ProjectMeta is composed inside the hero (PRJ-06 wiring path complete: hero → meta → privacy label).
  </acceptance_criteria>
  <done>ProjectHero shipped with both variants; 8-case test suite green; PRJ-04 satisfied at the component level.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/projects --reporter=dot` — Plan 02 contributes 21 cases (tag-chip-row 6 + project-meta 7 + project-hero 8); Wave 0 contributes 7 (hero-fallback). All 28 tests green.
- `pnpm typecheck` — clean.
- `pnpm build` — succeeds (no page composition yet, but components compile).
- Phase 1 + Phase 2 + Plan 03-00 + Plan 03-01 tests still green (no regression).
</verification>

<success_criteria>
- PRJ-04 satisfied at component level: hero variant branching works on Myco's actual placeholder path (text-only) AND on the speculative real-artwork path (image-present).
- PRJ-06 satisfied at component level: privacy rendering contract enforced via visibility-gated branch in ProjectMeta; defensive against stale repoUrl props.
- Plan 03-03 (Wave 2 lane B) can `import { ProjectHero }` and pass the schema-shaped Project fields directly; no further glue required.
- Zero new dependencies, zero `'use client'` directives.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-detail-template/03-02-SUMMARY.md` recording:
- Files created (3 components + 3 tests).
- Test results (21/21 expected green).
- Any class-string deviations from UI-SPEC due to Tailwind v4 arbitrary-value emission.
- Confirmation that the privacy rendering contract's three verification clauses (no anchor with repo URL when private; literal `code private` text; `code-private` chip still present in TagChipRow output) are all auto-asserted by the project-meta test suite.
</output>
