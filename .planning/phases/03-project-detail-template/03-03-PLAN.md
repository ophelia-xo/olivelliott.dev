---
phase: 03-project-detail-template
plan: 03
type: execute
wave: 2
depends_on: [03-00, 03-01, 03-02]
files_modified:
  - lib/next-project.ts
  - components/projects/next-project-title.tsx
  - components/projects/next-project-block.tsx
  - app/(site)/projects/[slug]/page.tsx
  - tests/projects/next-project.test.ts
  - tests/projects/next-project-block.test.tsx
  - tests/projects/static-params.test.ts
  - tests/projects/page.test.tsx
  - tests/projects/page-metadata.test.ts
autonomous: true
requirements: [PRJ-01, PRJ-03, PRJ-05, PRJ-07]

must_haves:
  truths:
    - "/projects/[slug] is a static RSC page: dynamicParams = false, generateStaticParams enumerates getAll().map(p => ({ slug: p.slug })), unknown slugs hit Phase 1's app/not-found.tsx"
    - "Page imports the MDX body via dynamic template-literal import: await import(`@/content/projects/${slug}.mdx`) AFTER the notFound() check; the .mdx extension is mandatory"
    - "Page composes <ProjectHero> + <MDXProse>{<MDXBody />}</MDXProse> + <NextProjectBlock> inside a single <article> root"
    - "generateMetadata returns title (project.title — root titleTemplate wraps), description (project.description ?? project.tagline), openGraph + twitter card with the OG image precedence: project.ogImage → project.hero.src (when not placeholder) → /og-default.png"
    - "generateMetadata sets alternates.canonical = /projects/${slug}"
    - "getNextProject(slug) returns top-overlap related project; falls back to cyclic-by-order; returns null for single-project corpus or unknown slug"
    - "<NextProjectBlock> renders multi-project variant when next is a project object; renders single-project variant (links to /projects with 'all projects →' eyebrow) when next === null"
    - "<NextProjectBlock> uses motion/react <m.h2> client island for whileHover translateX(4px); rest of the block is RSC; hover underline appears via CSS group:hover (preserves affordance under reduced-motion even though translateX is suppressed)"
    - "Myco's existing content/projects/myco.mdx renders unmodified through the page (PRJ-03 — Problem/Approach/Outcome H2s already authored, content-load.test.ts already validates)"
    - "Private fixture (tests/fixtures/projects/valid-private.mdx) rendered through the page produces zero <a> elements pointing at the private project's would-be repo URL"
  artifacts:
    - path: "lib/next-project.ts"
      provides: "getNextProject(slug) — top-overlap, cyclic fallback, single-project null"
      exports: ["getNextProject"]
    - path: "components/projects/next-project-title.tsx"
      provides: "Tiny client island wrapping <m.h2> for whileHover translateX"
      exports: ["NextProjectTitle"]
    - path: "components/projects/next-project-block.tsx"
      provides: "NextProjectBlock RSC composing eyebrow + NextProjectTitle + tagline; multi/single-project variants"
      exports: ["NextProjectBlock"]
    - path: "app/(site)/projects/[slug]/page.tsx"
      provides: "RSC page: generateStaticParams + generateMetadata + dynamic MDX import + composition"
      exports: ["default", "generateStaticParams", "generateMetadata", "dynamicParams"]
    - path: "tests/projects/next-project.test.ts"
      provides: "Algorithm — top-overlap, cyclic, single-project null, unknown-slug null"
    - path: "tests/projects/next-project-block.test.tsx"
      provides: "Component — multi-project + single-project variants"
    - path: "tests/projects/static-params.test.ts"
      provides: "generateStaticParams enumerates getAll() slugs (PRJ-01)"
    - path: "tests/projects/page.test.tsx"
      provides: "RSC page render — composition + private-fixture privacy assertion (PRJ-01 + PRJ-06 integration)"
    - path: "tests/projects/page-metadata.test.ts"
      provides: "generateMetadata — 5 cases (PRJ-07)"
  key_links:
    - from: "app/(site)/projects/[slug]/page.tsx"
      to: "@/content/projects/${slug}.mdx"
      via: "dynamic template-literal import (Turbopack template-prefix-resolved at build time)"
      pattern: "@/content/projects/"
    - from: "app/(site)/projects/[slug]/page.tsx"
      to: "lib/projects.ts (getAll, getProject)"
      via: "generateStaticParams + page body lookup"
      pattern: "from '@/lib/projects'"
    - from: "app/(site)/projects/[slug]/page.tsx"
      to: "lib/next-project.ts (getNextProject)"
      via: "page body computes next prop for NextProjectBlock"
      pattern: "getNextProject"
    - from: "app/(site)/projects/[slug]/page.tsx"
      to: "components/projects/{project-hero,next-project-block}"
      via: "page composition"
      pattern: "ProjectHero"
    - from: "components/projects/next-project-block.tsx"
      to: "motion/react"
      via: "<m.h2> client island via NextProjectTitle"
      pattern: "motion/react"
---

<objective>
Wave 2 lane B + integration. Ships PRJ-01 (static rendering with dynamicParams=false), PRJ-03 (Myco's already-authored Problem/Approach/Outcome MDX renders unmodified), PRJ-05 (next-project navigation with cyclic fallback + single-project case), and PRJ-07 (per-route metadata + OG fallback chain).

Five concrete deliverables: the `getNextProject` algorithm helper + its 4-case test, the `<NextProjectBlock>` component with its tiny `<m.h2>` client island + 2-variant test, the page route file `app/(site)/projects/[slug]/page.tsx` with `generateStaticParams` + `generateMetadata` + dynamic MDX import + composition, and three test files for the page (static-params, page render, metadata).

This plan integrates all upstream outputs: Wave 0's pipeline + isPlaceholderHero, Wave 1's Figure/Gallery/Callout/MDXProse, Wave 2 lane A's ProjectHero/ProjectMeta/TagChipRow.

Output: 1 lib helper + 2 components + 1 page route + 5 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-project-detail-template/03-CONTEXT.md
@.planning/phases/03-project-detail-template/03-RESEARCH.md
@.planning/phases/03-project-detail-template/03-UI-SPEC.md
@.planning/phases/03-project-detail-template/03-VALIDATION.md
@.planning/phases/03-project-detail-template/03-00-PLAN.md
@.planning/phases/03-project-detail-template/03-01-PLAN.md
@.planning/phases/03-project-detail-template/03-02-PLAN.md
@app/(site)/layout.tsx
@app/layout.tsx
@app/not-found.tsx
@lib/projects.ts
@lib/content.ts
@lib/schemas.ts
@content/projects/myco.mdx
@components/motion/motion-provider.tsx
@tests/fixtures/projects/valid-private.mdx
@tests/fixtures/projects/valid-hero.mdx

<interfaces>
lib/projects.ts (Phase 2 — already shipped):
- `getAll(): readonly Project[]` — non-archived, sorted by `order` asc
- `getHeroProjects(): readonly Project[]`
- `getProject(slug: string): Project | undefined` — searches allProjects directly (archived slugs still resolvable; consumer pages call notFound() on undefined)
- `getAllTags()`, `getProjectsByTag(tag)`
- `getRelatedProjects(slug: string, limit = 3): readonly Project[]` — overlapping-tag count, excludes target, excludes zero-overlap, returns [] for unknown slug

Phase 1 wiring:
- app/layout.tsx — root metadata: `metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL ?? 'https://olivelliott.dev')`, `title: { default: 'olivelliott.dev', template: '%s · olivelliott.dev' }`
  → per-page `title: project.title` becomes e.g. "Myco · olivelliott.dev"
  → per-page `alternates.canonical: '/projects/${slug}'` resolves against metadataBase
- app/(site)/layout.tsx — MotionProvider → SkipLink → Nav → main(id="main") → Footer. Phase 3 page slots into <main>.
- app/not-found.tsx — Phase 1's custom 404. Phase 3's notFound() + dynamicParams=false route unknown slugs here.

Phase 2 fixtures:
- tests/fixtures/projects/valid-hero.mdx — public hero project. Loadable via gray-matter for fixture-shape data.
- tests/fixtures/projects/valid-private.mdx — private project, slug `valid-private`, schema strips `links.repo`, adds `code-private` tag.

Wave 0 (this phase):
- next.config.ts rehype chain compiles MDX with slug+autolink+pretty-code; this is what makes Myco's `## Problem`, `## Approach`, `## Outcome` get id attributes + appended anchors.
- /og-default.png exists in /public.
- lib/hero-fallback.ts → isPlaceholderHero (used by ProjectHero).

Wave 1 (this phase):
- components/mdx/{prose,figure,gallery,callout}.tsx — MDXProse wraps the dynamically-imported MDX body; Figure/Gallery/Callout registered globally via mdx-components.tsx.

Wave 2 lane A (this phase):
- components/projects/{project-hero,project-meta,tag-chip-row}.tsx — page composition consumes ProjectHero directly.

motion/react (Phase 1 — components/motion/motion-provider.tsx):
- Phase 1's MotionProvider declares `<LazyMotion strict>` + `<MotionConfig reducedMotion="user">`.
- `strict` mode REQUIRES the `m.*` form (NOT `motion.*`) in client components.
- Import path: `import { m } from 'motion/react'`.
- Reduced-motion: `whileHover` props are suppressed automatically by MotionConfig — no manual gating needed.

Pitfall reminders (RESEARCH §):
- Pitfall 5: dynamic import path MUST include the `.mdx` extension.
- Pitfall 6: `params` is a Promise<{slug: string}> in Next 15+ — use `const { slug } = await params`.
- Pitfall 9: `notFound()` MUST be called BEFORE the dynamic import.
- Pitfall 11: dynamic import template literal MUST start with a literal directory prefix (`@/content/projects/`).
- Pitfall 12: Vitest cannot process .mdx imports natively — page tests MUST mock the .mdx import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-03-01: Build getNextProject helper + 4-case algorithm test</name>
  <files>lib/next-project.ts, tests/projects/next-project.test.ts</files>
  <read_first>
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Component Inventory #10 (RelatedProjectFallback)" — verbatim algorithm + return shape
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Component Inventory #9 (NextProjectBlock) → Algorithm" — 4-step logic
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Code Examples → lib/next-project.ts" (verbatim source)
    - lib/projects.ts (consumed: getAll, getRelatedProjects)
  </read_first>
  <behavior>
    - Test 1 (top-overlap): mock @/lib/projects so getRelatedProjects returns one project; getNextProject returns that project's {slug, title, tagline}.
    - Test 2 (cyclic fallback): mock so getRelatedProjects returns []; getAll returns [A, B, C]; getNextProject('B') returns C; getNextProject('C') wraps to A.
    - Test 3 (single-project null): mock so getAll returns one project; getNextProject returns null.
    - Test 4 (unknown slug null): mock so getAll returns [A] but the queried slug isn't 'a'; getNextProject returns null.
    - Test 5 (return shape): the returned object has exactly the keys {slug, title, tagline} — no other Project fields leak.
  </behavior>
  <action>
    Step A — write `tests/projects/next-project.test.ts` FIRST (RED). The mocking pattern follows Phase 2's tag-index test convention (`vi.mock('@/lib/projects')` + dynamic top-level await import for isolation):

        // tests/projects/next-project.test.ts
        // Algorithm spec for getNextProject — top-overlap, cyclic fallback, null cases.
        // Refs: 03-UI-SPEC § Component Inventory #9 + #10; 03-RESEARCH § Code Examples → lib/next-project.ts.
        import { describe, expect, it, vi } from 'vitest'

        const fakeProject = (slug: string, order: number) => ({
          slug,
          title: `Title-${slug}`,
          tagline: `Tagline-${slug}`,
          year: 2025,
          tier: 'secondary' as const,
          order,
          status: 'active' as const,
          visibility: 'public' as const,
          tags: ['typescript' as const],
          stack: [],
          links: {},
          hero: { src: `/x/${slug}/hero-placeholder.png`, alt: 'a' },
          gallery: [],
          outcomes: [],
          description: 'd',
        })

        describe('getNextProject', () => {
          it('returns the top-overlap related project when one is available', async () => {
            vi.resetModules()
            vi.doMock('@/lib/projects', () => ({
              getAll: () => [fakeProject('a', 10), fakeProject('b', 20)],
              getRelatedProjects: () => [fakeProject('b', 20)],
            }))
            const { getNextProject } = await import('@/lib/next-project')
            expect(getNextProject('a')).toEqual({
              slug: 'b',
              title: 'Title-b',
              tagline: 'Tagline-b',
            })
          })

          it('falls back to cyclic-by-order when no overlap; wraps last → first', async () => {
            vi.resetModules()
            const all = [fakeProject('a', 10), fakeProject('b', 20), fakeProject('c', 30)]
            vi.doMock('@/lib/projects', () => ({
              getAll: () => all,
              getRelatedProjects: () => [],
            }))
            const { getNextProject } = await import('@/lib/next-project')
            expect(getNextProject('b')?.slug).toBe('c')
            expect(getNextProject('c')?.slug).toBe('a')
            expect(getNextProject('a')?.slug).toBe('b')
          })

          it('returns null for single-project corpus', async () => {
            vi.resetModules()
            vi.doMock('@/lib/projects', () => ({
              getAll: () => [fakeProject('a', 10)],
              getRelatedProjects: () => [],
            }))
            const { getNextProject } = await import('@/lib/next-project')
            expect(getNextProject('a')).toBeNull()
          })

          it('returns null for unknown slug', async () => {
            vi.resetModules()
            vi.doMock('@/lib/projects', () => ({
              getAll: () => [fakeProject('a', 10), fakeProject('b', 20)],
              getRelatedProjects: () => [],
            }))
            const { getNextProject } = await import('@/lib/next-project')
            expect(getNextProject('does-not-exist')).toBeNull()
          })

          it('returned object has exactly {slug, title, tagline} keys', async () => {
            vi.resetModules()
            vi.doMock('@/lib/projects', () => ({
              getAll: () => [fakeProject('a', 10), fakeProject('b', 20)],
              getRelatedProjects: () => [fakeProject('b', 20)],
            }))
            const { getNextProject } = await import('@/lib/next-project')
            const result = getNextProject('a')
            expect(result).not.toBeNull()
            expect(Object.keys(result!).sort()).toEqual(['slug', 'tagline', 'title'])
          })
        })

    Run `pnpm vitest run tests/projects/next-project.test.ts` — RED.

    Step B — write `lib/next-project.ts`. Use the exact source from RESEARCH § "Code Examples":

        // lib/next-project.ts
        // Computes the "next project" for the bottom-of-page navigation.
        // 1. Top tag-overlap (single related project from getRelatedProjects).
        // 2. Cyclic fallback by order ascending (last project wraps to first).
        // 3. Returns null for single-project corpus or unknown slug — caller renders the
        //    "all projects →" wayfinding variant in <NextProjectBlock>.
        // Source: .planning/phases/03-project-detail-template/03-UI-SPEC.md § Component Inventory #10.
        import { getAll, getRelatedProjects } from '@/lib/projects'

        export function getNextProject(currentSlug: string):
          | { slug: string; title: string; tagline: string }
          | null {
          const related = getRelatedProjects(currentSlug, 1)
          if (related.length === 1 && related[0]) {
            const p = related[0]
            return { slug: p.slug, title: p.title, tagline: p.tagline }
          }
          const all = getAll()
          if (all.length <= 1) return null
          const idx = all.findIndex((p) => p.slug === currentSlug)
          if (idx === -1) return null
          const nextIdx = (idx + 1) % all.length
          const next = all[nextIdx]!
          return { slug: next.slug, title: next.title, tagline: next.tagline }
        }

    Run `pnpm vitest run tests/projects/next-project.test.ts` — GREEN on all 5.
  </action>
  <verify>
    <automated>pnpm vitest run tests/projects/next-project.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `lib/next-project.ts` exports `getNextProject(currentSlug: string)` returning `{ slug, title, tagline } | null`.
    - All 5 algorithm tests green under `pnpm vitest run`.
    - Function imports only `getAll` + `getRelatedProjects` from `@/lib/projects`.
    - Returned object includes only the three documented keys (no other Project fields leak).
  </acceptance_criteria>
  <done>getNextProject ships with the four documented branches (top-overlap, cyclic, single-project null, unknown-slug null); 5-case test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-03-02: Build NextProjectBlock + tiny client island + 2-variant RTL test</name>
  <files>components/projects/next-project-title.tsx, components/projects/next-project-block.tsx, tests/projects/next-project-block.test.tsx</files>
  <read_first>
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Component Inventory #9 (NextProjectBlock)" — markup, props, multi-project + single-project variants, motion island
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Motion → Phase 3 motion inventory" (whileHover x:4 with --motion-duration-base / --motion-ease-standard; reduced-motion suppresses translate but underline still appears via CSS)
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Copywriting Contract → Static / locked strings" — `next →` (multi), `all projects →` (single), `Browse all projects` (single title), single-project tagline string
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Accessibility Contract → Semantic HTML" — <nav aria-label="Next project"> or "Browse all projects"
    - components/motion/motion-provider.tsx (LazyMotion strict + MotionConfig — Phase 1; Phase 3 island uses `m.*`, NOT `motion.*`)
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Anti-Patterns to Avoid → Don't add 'use client' to MDX components" (this rule does NOT apply here — NextProjectBlock's title island MUST be a client component for whileHover)
  </read_first>
  <behavior>
    - Test 1 (multi-project): next={slug, title, tagline} → renders <nav aria-label="Next project"> containing eyebrow text "next →", an <a href="/projects/{slug}"> wrapping the title and tagline, with the title text matching `next.title` and the tagline matching `next.tagline`.
    - Test 2 (multi-project semantic): the entire link is a single <a> (eyebrow + title + tagline are all inside the same anchor — UI-SPEC tab-order assertion).
    - Test 3 (single-project): next=null → renders <nav aria-label="Browse all projects"> with eyebrow "all projects →", an <a href="/projects"> wrapping title "Browse all projects" and the locked tagline string.
    - Test 4 (hairline + spacing): both variants have a top hairline border (border-t + border-hairline) and pt-12.
    - Test 5 (motion island): the title element is rendered (cannot directly assert whileHover prop in jsdom without Motion test util; assert the title text and the wrapping <a> structure remain stable).
    - Test 6 (focus-visible offset): the <a> has the `focus-visible:outline-offset-4` class (per UI-SPEC: 4px offset for the large title link).
  </behavior>
  <action>
    Step A — write `tests/projects/next-project-block.test.tsx` FIRST (RED). Tests do not need to mount MotionProvider because LazyMotion `strict` allows `m.*` to render without the provider in tests (the strict-mode error fires only at runtime in browsers; jsdom mounts cleanly with stub behavior). If the test runner errors, wrap render in a tiny MotionConfig — but try without first.

        // tests/projects/next-project-block.test.tsx
        // Refs: 03-UI-SPEC § Component Inventory #9 (NextProjectBlock) + § Copywriting Contract.
        import { render, within } from '@testing-library/react'
        import { describe, expect, it } from 'vitest'
        import { NextProjectBlock } from '@/components/projects/next-project-block'

        describe('<NextProjectBlock>', () => {
          it('multi-project variant: <nav aria-label="Next project"> with eyebrow + linked title + tagline', () => {
            const { container } = render(
              <NextProjectBlock next={{ slug: 'fathom', title: 'Fathom', tagline: 'Local-first weather app.' }} />
            )
            const nav = container.querySelector('nav')!
            expect(nav.getAttribute('aria-label')).toBe('Next project')
            const link = nav.querySelector('a')!
            expect(link.getAttribute('href')).toBe('/projects/fathom')
            const linkScope = within(link)
            expect(linkScope.getByText('next →')).toBeTruthy()
            expect(linkScope.getByText('Fathom')).toBeTruthy()
            expect(linkScope.getByText('Local-first weather app.')).toBeTruthy()
          })

          it('multi-project variant: eyebrow + title + tagline are all inside the SAME <a>', () => {
            const { container } = render(
              <NextProjectBlock next={{ slug: 'fathom', title: 'Fathom', tagline: 'tag.' }} />
            )
            const anchors = Array.from(container.querySelectorAll('a'))
            expect(anchors.length).toBe(1)
          })

          it('single-project variant (next=null): <nav aria-label="Browse all projects"> linking to /projects', () => {
            const { container } = render(<NextProjectBlock next={null} />)
            const nav = container.querySelector('nav')!
            expect(nav.getAttribute('aria-label')).toBe('Browse all projects')
            const link = nav.querySelector('a')!
            expect(link.getAttribute('href')).toBe('/projects')
            const linkScope = within(link)
            expect(linkScope.getByText('all projects →')).toBeTruthy()
            expect(linkScope.getByText('Browse all projects')).toBeTruthy()
            expect(
              linkScope.getByText('More work, including private case studies and secondary projects.')
            ).toBeTruthy()
          })

          it('hairline divider above + pt-12 vertical padding (both variants)', () => {
            for (const next of [
              { slug: 'fathom', title: 'Fathom', tagline: 't.' },
              null,
            ] as const) {
              const { container } = render(<NextProjectBlock next={next} />)
              const nav = container.querySelector('nav')!
              expect(nav.className).toMatch(/\bborder-t\b/)
              expect(nav.className).toMatch(/border-\[color:var\(--color-hairline\)\]/)
              expect(nav.className).toMatch(/\bpt-12\b/)
            }
          })

          it('the link element has focus-visible:outline-offset-4 (large-title 4px offset override)', () => {
            const { container } = render(<NextProjectBlock next={null} />)
            const link = container.querySelector('a')!
            expect(link.className).toMatch(/focus-visible:outline-offset-4/)
          })

          it('both variants render a title element with the next title text', () => {
            const { container: a } = render(
              <NextProjectBlock next={{ slug: 'fathom', title: 'Fathom', tagline: 't.' }} />
            )
            expect(a.textContent).toContain('Fathom')
            const { container: b } = render(<NextProjectBlock next={null} />)
            expect(b.textContent).toContain('Browse all projects')
          })
        })

    Run `pnpm vitest run tests/projects/next-project-block.test.tsx` — RED.

    Step B — write the client island `components/projects/next-project-title.tsx`. Tiny, focused — only the title is `'use client'` + uses motion/react `m.h2`:

        'use client'
        // components/projects/next-project-title.tsx
        // Tiny client island for the NextProjectBlock title — wraps motion/react <m.h2>
        // for whileHover translateX(4px). Reduced-motion is gated by Phase 1's MotionConfig.
        // Source: .planning/phases/03-project-detail-template/03-UI-SPEC.md § Component Inventory #9.
        // Per Phase 1's LazyMotion strict, MUST use `m.*`, NOT `motion.*`.
        import { m } from 'motion/react'

        interface NextProjectTitleProps {
          children: React.ReactNode
          className?: string
        }

        export function NextProjectTitle({ children, className }: NextProjectTitleProps) {
          return (
            <m.h2
              className={className}
              whileHover={{ x: 4 }}
              transition={{ duration: 0.22, ease: [0.22, 1, 0.36, 1] }}
            >
              {children}
            </m.h2>
          )
        }

    Step C — write `components/projects/next-project-block.tsx` (RSC; consumes the client island for the title):

        // components/projects/next-project-block.tsx
        // RSC. End-of-page navigation block. Renders multi-project variant when `next`
        // is a project, single-project (wayfinding) variant when null.
        // The whileHover translateX is delegated to the tiny <NextProjectTitle> client island.
        // Source: .planning/phases/03-project-detail-template/03-UI-SPEC.md § Component Inventory #9.
        import { NextProjectTitle } from './next-project-title'

        interface NextProjectBlockProps {
          next: { slug: string; title: string; tagline: string } | null
        }

        const TITLE_CLASSES = `
          mt-2 text-[var(--text-h2)] md:text-[var(--text-display)]
          leading-[1.15] tracking-[-0.015em]
          font-[600] text-[color:var(--color-text-primary)]
          group-hover:underline group-hover:decoration-[color:var(--color-accent)]
          group-hover:underline-offset-4
          group-focus-visible:underline group-focus-visible:decoration-[color:var(--color-accent)]
          group-focus-visible:underline-offset-4
        `.trim()

        export function NextProjectBlock({ next }: NextProjectBlockProps) {
          if (next === null) {
            return (
              <nav
                aria-label="Browse all projects"
                className="border-t border-[color:var(--color-hairline)] mt-16 md:mt-24 pt-12"
              >
                <a
                  href="/projects"
                  className="group block focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-[color:var(--color-accent)]"
                >
                  <span className="font-mono text-[var(--text-label)] font-medium tracking-[0.02em] lowercase text-[color:var(--color-text-tertiary)]">
                    all projects →
                  </span>
                  <NextProjectTitle className={TITLE_CLASSES}>Browse all projects</NextProjectTitle>
                  <p className="mt-3 text-[var(--text-body)] leading-[1.6] text-[color:var(--color-text-secondary)] max-w-[65ch]">
                    More work, including private case studies and secondary projects.
                  </p>
                </a>
              </nav>
            )
          }

          return (
            <nav
              aria-label="Next project"
              className="border-t border-[color:var(--color-hairline)] mt-16 md:mt-24 pt-12"
            >
              <a
                href={`/projects/${next.slug}`}
                className="group block focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-[color:var(--color-accent)]"
              >
                <span className="font-mono text-[var(--text-label)] font-medium tracking-[0.02em] lowercase text-[color:var(--color-text-tertiary)]">
                  next →
                </span>
                <NextProjectTitle className={TITLE_CLASSES}>{next.title}</NextProjectTitle>
                <p className="mt-3 text-[var(--text-body)] leading-[1.6] text-[color:var(--color-text-secondary)] max-w-[65ch]">
                  {next.tagline}
                </p>
              </a>
            </nav>
          )
        }

    Run `pnpm vitest run tests/projects/next-project-block.test.tsx` — GREEN on all 6.

    Do NOT add `'use client'` to `next-project-block.tsx` itself — only the title island is client. Do NOT use `motion.*` (Phase 1 LazyMotion strict requires `m.*`). Do NOT decorate the rest state of the title with the underline (UI-SPEC: rest = no underline; underline appears on hover/focus).
  </action>
  <verify>
    <automated>pnpm vitest run tests/projects/next-project-block.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `components/projects/next-project-title.tsx` has `'use client'` directive and exports `NextProjectTitle` wrapping `m.h2` from `motion/react`.
    - `components/projects/next-project-block.tsx` is RSC (no `'use client'`) and exports `NextProjectBlock`.
    - All 6 tests green.
    - `next-project-block.tsx` does not import `motion/react` directly (only via the client-island sub-component).
    - Multi-project variant link href = `/projects/${next.slug}`; single-project variant link href = `/projects`.
    - Locked strings present verbatim: `next →`, `all projects →`, `Browse all projects`, `More work, including private case studies and secondary projects.`
  </acceptance_criteria>
  <done>NextProjectBlock + client island shipped per UI-SPEC; 6-case test green; PRJ-05 satisfied at the component level (page composition in next task closes the integration loop).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-03-03: Build the page route with generateStaticParams + generateMetadata + dynamic MDX import + composition + integration tests</name>
  <files>app/(site)/projects/[slug]/page.tsx, tests/projects/static-params.test.ts, tests/projects/page.test.tsx, tests/projects/page-metadata.test.ts</files>
  <read_first>
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Routing & Static Generation" — exact route file path + dynamicParams=false + generateStaticParams shape
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Per-Page Metadata Contract" — verbatim generateMetadata implementation + OG image precedence
    - .planning/phases/03-project-detail-template/03-UI-SPEC.md § "Page Composition → Document outline" — <article> root, hero, MDXProse, hairline, NextProjectBlock
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Pattern 1: App Router static route" (full page source)
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Pattern 2: Per-route MDX rendering via dynamic import"
    - .planning/phases/03-project-detail-template/03-RESEARCH.md § "Pitfall 5" (.mdx extension), § "Pitfall 6" (params is Promise), § "Pitfall 9" (notFound() before await import), § "Pitfall 11" (literal directory prefix), § "Pitfall 12" (vitest mocks .mdx)
    - lib/projects.ts (getAll, getProject)
    - lib/next-project.ts (getNextProject — just shipped in 03-03-01)
    - components/projects/{project-hero,next-project-block}.tsx (composed in page body)
    - components/mdx/prose.tsx (wraps MDX body)
    - app/not-found.tsx (Phase 1 — notFound() routes here)
  </read_first>
  <behavior>
    - static-params test 1: generateStaticParams returns an array containing { slug: 'myco' } — proves Phase 2's getAll() is enumerated.
    - static-params test 2: every entry has a `slug` string property and no other keys.
    - static-params test 3: dynamicParams export === false.
    - page test 1 (PRJ-01 valid slug): mocking @/lib/projects to return a known project + mocking the dynamic .mdx import to a stub, the default page export renders an <article> containing a <ProjectHero> output (querying for the role=list aria-label="Project metadata" anchor) AND a NextProjectBlock <nav>.
    - page test 2 (PRJ-01 unknown slug): mocking getProject to return undefined, the page calls notFound() — which Vitest exposes via the notFound mock from 'next/navigation'.
    - page test 3 (PRJ-06 integration / private fixture): mocking getProject to return a project shaped like tests/fixtures/projects/valid-private.mdx (post-schema-transform: visibility=private, tags includes 'code-private', links.repo undefined), the rendered page contains zero anchor elements with href starting "https://github.com/example/valid-private" AND contains the literal text "code private".
    - metadata test 1: generateMetadata returns title === project.title, description === project.description.
    - metadata test 2: alternates.canonical === '/projects/${project.slug}'.
    - metadata test 3 (OG precedence #1): project.ogImage set → openGraph.images[0].url === project.ogImage.
    - metadata test 4 (OG precedence #2): project.ogImage unset + hero.src non-placeholder → openGraph.images[0].url === project.hero.src.
    - metadata test 5 (OG precedence #3): project.ogImage unset + hero.src is placeholder → openGraph.images[0].url === '/og-default.png' (Myco's path).
    - metadata test 6: twitter.card === 'summary_large_image' and twitter.images[0] === resolved OG URL.
    - metadata test 7: unknown slug → returns {} (defensive).
  </behavior>
  <action>
    Step A — write `tests/projects/static-params.test.ts` FIRST (RED):

        // tests/projects/static-params.test.ts
        // PRJ-01: generateStaticParams enumerates Phase 2's getAll() slugs.
        import { describe, expect, it, vi } from 'vitest'

        describe('app/(site)/projects/[slug]/page.tsx — generateStaticParams', () => {
          it('returns an entry for every project from getAll()', async () => {
            vi.resetModules()
            vi.doMock('@/lib/projects', () => ({
              getAll: () => [
                { slug: 'myco' },
                { slug: 'fathom' },
                { slug: 'agenda-keeper' },
              ],
              getProject: vi.fn(),
            }))
            // Mock next-project + mdx-import paths so the module loads cleanly
            vi.doMock('@/lib/next-project', () => ({ getNextProject: () => null }))
            const mod = await import('@/app/(site)/projects/[slug]/page')
            const params = mod.generateStaticParams()
            expect(params).toEqual([
              { slug: 'myco' },
              { slug: 'fathom' },
              { slug: 'agenda-keeper' },
            ])
          })

          it('every entry has only a `slug` key', async () => {
            vi.resetModules()
            vi.doMock('@/lib/projects', () => ({
              getAll: () => [{ slug: 'myco' }],
              getProject: vi.fn(),
            }))
            vi.doMock('@/lib/next-project', () => ({ getNextProject: () => null }))
            const mod = await import('@/app/(site)/projects/[slug]/page')
            const params = mod.generateStaticParams()
            for (const entry of params) {
              expect(Object.keys(entry)).toEqual(['slug'])
            }
          })

          it('exports dynamicParams = false', async () => {
            vi.resetModules()
            vi.doMock('@/lib/projects', () => ({ getAll: () => [], getProject: vi.fn() }))
            vi.doMock('@/lib/next-project', () => ({ getNextProject: () => null }))
            const mod = await import('@/app/(site)/projects/[slug]/page')
            expect(mod.dynamicParams).toBe(false)
          })
        })

    Step B — write `tests/projects/page.test.tsx` (RED). Per Pitfall 12, mock the .mdx dynamic import:

        // tests/projects/page.test.tsx
        // PRJ-01 + PRJ-06 integration. Mocks @/lib/projects and the dynamic .mdx import
        // (per Pitfall 12 — Vitest cannot process .mdx natively).
        import { render } from '@testing-library/react'
        import { describe, expect, it, vi } from 'vitest'

        const mycoProject = {
          slug: 'myco',
          title: 'Myco',
          tagline: 'A persistent cognitive layer.',
          year: 2025,
          tier: 'hero' as const,
          order: 10,
          status: 'active' as const,
          visibility: 'public' as const,
          tags: ['local-first', 'typescript'] as const,
          stack: [],
          links: { repo: 'https://github.com/olivelliott/myco' },
          hero: { src: '/images/projects/myco/hero-placeholder.png', alt: 'a' },
          gallery: [],
          outcomes: [],
          description: 'A local-first MCP server.',
        }

        const privateFixture = {
          slug: 'valid-private',
          title: 'Valid Private Fixture',
          tagline: 'Test fixture.',
          year: 2025,
          tier: 'secondary' as const,
          order: 20,
          status: 'active' as const,
          visibility: 'private' as const,
          tags: ['typescript', 'code-private'] as const,
          stack: [],
          links: {},  // schema stripped repo
          hero: { src: '/images/projects/valid-private/hero.png', alt: 'a' },
          gallery: [],
          outcomes: [],
          description: 'Private fixture.',
        }

        // Stub MDX module — content is irrelevant for composition tests.
        const StubMDX = () => null

        describe('app/(site)/projects/[slug]/page.tsx — render', () => {
          it('PRJ-01 valid slug: renders <article> containing <ProjectHero> and <NextProjectBlock>', async () => {
            vi.resetModules()
            vi.doMock('@/lib/projects', () => ({
              getAll: () => [mycoProject],
              getProject: (slug: string) => (slug === 'myco' ? mycoProject : undefined),
            }))
            vi.doMock('@/lib/next-project', () => ({ getNextProject: () => null }))
            vi.doMock('@/content/projects/myco.mdx', () => ({ default: StubMDX }))
            const { default: Page } = await import('@/app/(site)/projects/[slug]/page')
            const ui = await Page({ params: Promise.resolve({ slug: 'myco' }) })
            const { container } = render(ui)
            expect(container.querySelector('article')).not.toBeNull()
            // ProjectHero composition signal — meta row
            expect(container.querySelector('[role="list"][aria-label="Project metadata"]')).not.toBeNull()
            // NextProjectBlock signal (single-project variant since corpus is just Myco)
            const nav = container.querySelector('nav[aria-label="Browse all projects"]')
            expect(nav).not.toBeNull()
          })

          it('PRJ-01 unknown slug: page calls notFound()', async () => {
            const notFoundSpy = vi.fn(() => {
              throw new Error('NEXT_NOT_FOUND')
            })
            vi.resetModules()
            vi.doMock('next/navigation', () => ({ notFound: notFoundSpy }))
            vi.doMock('@/lib/projects', () => ({
              getAll: () => [mycoProject],
              getProject: () => undefined,
            }))
            vi.doMock('@/lib/next-project', () => ({ getNextProject: () => null }))
            const { default: Page } = await import('@/app/(site)/projects/[slug]/page')
            await expect(
              Page({ params: Promise.resolve({ slug: 'does-not-exist' }) })
            ).rejects.toThrow('NEXT_NOT_FOUND')
            expect(notFoundSpy).toHaveBeenCalledOnce()
          })

          it('PRJ-06 integration: private project renders "code private" + zero anchors with the would-be repo URL', async () => {
            vi.resetModules()
            vi.doMock('@/lib/projects', () => ({
              getAll: () => [privateFixture],
              getProject: () => privateFixture,
            }))
            vi.doMock('@/lib/next-project', () => ({ getNextProject: () => null }))
            vi.doMock('@/content/projects/valid-private.mdx', () => ({ default: StubMDX }))
            const { default: Page } = await import('@/app/(site)/projects/[slug]/page')
            const ui = await Page({ params: Promise.resolve({ slug: 'valid-private' }) })
            const { container } = render(ui)
            // Literal "code private" present
            expect(container.textContent).toContain('code private')
            // No anchor pointing at the would-be repo URL
            const anchors = Array.from(container.querySelectorAll('a'))
            expect(anchors.find((a) => a.getAttribute('href')?.includes('example/valid-private'))).toBeUndefined()
            // Tag chip 'code-private' is still present (auto-added by schema; chip row renders)
            expect(anchors.some((a) => a.getAttribute('href') === '/projects?tag=code-private')).toBe(true)
          })
        })

    Step C — write `tests/projects/page-metadata.test.ts` (RED):

        // tests/projects/page-metadata.test.ts
        // PRJ-07: per-route metadata + OG image fallback chain (5 cases).
        import { describe, expect, it, vi } from 'vitest'

        const baseProject = {
          slug: 'myco',
          title: 'Myco',
          tagline: 'A persistent cognitive layer.',
          year: 2025,
          tier: 'hero' as const,
          order: 10,
          status: 'active' as const,
          visibility: 'public' as const,
          tags: ['local-first'] as const,
          stack: [],
          links: { repo: 'https://github.com/x/y' },
          hero: { src: '/images/projects/myco/hero-placeholder.png', alt: 'placeholder hero alt' },
          gallery: [],
          outcomes: [],
          description: 'A local-first MCP server.',
        }

        async function loadGenerateMetadata(getProjectImpl: (slug: string) => unknown) {
          vi.resetModules()
          vi.doMock('@/lib/projects', () => ({
            getAll: () => [],
            getProject: getProjectImpl,
          }))
          vi.doMock('@/lib/next-project', () => ({ getNextProject: () => null }))
          const mod = await import('@/app/(site)/projects/[slug]/page')
          return mod.generateMetadata
        }

        describe('generateMetadata', () => {
          it('returns title and description from frontmatter', async () => {
            const generateMetadata = await loadGenerateMetadata(() => baseProject)
            const meta = await generateMetadata({ params: Promise.resolve({ slug: 'myco' }) })
            expect(meta.title).toBe('Myco')
            expect(meta.description).toBe('A local-first MCP server.')
          })

          it('sets alternates.canonical = /projects/${slug}', async () => {
            const generateMetadata = await loadGenerateMetadata(() => baseProject)
            const meta = await generateMetadata({ params: Promise.resolve({ slug: 'myco' }) })
            expect(meta.alternates?.canonical).toBe('/projects/myco')
          })

          it('OG precedence #1: project.ogImage wins when set', async () => {
            const generateMetadata = await loadGenerateMetadata(() => ({
              ...baseProject,
              ogImage: '/images/projects/myco/og-custom.png',
            }))
            const meta = await generateMetadata({ params: Promise.resolve({ slug: 'myco' }) })
            const og = meta.openGraph?.images as Array<{ url: string }> | undefined
            expect(og?.[0]?.url).toBe('/images/projects/myco/og-custom.png')
          })

          it('OG precedence #2: hero.src wins when ogImage unset AND hero is NOT a placeholder', async () => {
            const generateMetadata = await loadGenerateMetadata(() => ({
              ...baseProject,
              hero: { src: '/images/projects/myco/cover.png', alt: 'cover' },
            }))
            const meta = await generateMetadata({ params: Promise.resolve({ slug: 'myco' }) })
            const og = meta.openGraph?.images as Array<{ url: string }> | undefined
            expect(og?.[0]?.url).toBe('/images/projects/myco/cover.png')
          })

          it('OG precedence #3: /og-default.png falls back when ogImage unset AND hero is placeholder (Myco today)', async () => {
            const generateMetadata = await loadGenerateMetadata(() => baseProject)
            const meta = await generateMetadata({ params: Promise.resolve({ slug: 'myco' }) })
            const og = meta.openGraph?.images as Array<{ url: string }> | undefined
            expect(og?.[0]?.url).toBe('/og-default.png')
          })

          it('twitter card is summary_large_image and reuses the OG image', async () => {
            const generateMetadata = await loadGenerateMetadata(() => baseProject)
            const meta = await generateMetadata({ params: Promise.resolve({ slug: 'myco' }) })
            expect(meta.twitter?.card).toBe('summary_large_image')
            const tw = meta.twitter?.images as string[] | undefined
            expect(tw?.[0]).toBe('/og-default.png')
          })

          it('returns {} for unknown slug', async () => {
            const generateMetadata = await loadGenerateMetadata(() => undefined)
            const meta = await generateMetadata({ params: Promise.resolve({ slug: 'nope' }) })
            expect(meta).toEqual({})
          })
        })

    Run `pnpm vitest run tests/projects/static-params.test.ts tests/projects/page.test.tsx tests/projects/page-metadata.test.ts` — RED on all (page file doesn't exist yet).

    Step D — create `app/(site)/projects/[slug]/page.tsx`. Use the verbatim Pattern 1 source from RESEARCH, adjusted for our component imports:

        // app/(site)/projects/[slug]/page.tsx
        // RSC project detail page — PRJ-01..PRJ-07.
        // - dynamicParams = false: only generateStaticParams slugs render; unknown → 404
        // - generateMetadata: per-route title/description/canonical + OG precedence chain
        // - Body: getProject → notFound() (BEFORE the dynamic .mdx import per Pitfall 9)
        //         then dynamic-import the MDX module (Pitfall 5: .mdx extension MANDATORY;
        //         Pitfall 11: literal directory prefix `@/content/projects/`)
        // - Composition: <article> > <ProjectHero> + <MDXProse>{<MDXBody />}</MDXProse> + <NextProjectBlock>
        // Source: .planning/phases/03-project-detail-template/03-RESEARCH.md § Pattern 1 + 03-UI-SPEC § Routing & Static Generation.
        import type { Metadata } from 'next'
        import { notFound } from 'next/navigation'
        import { MDXProse } from '@/components/mdx/prose'
        import { NextProjectBlock } from '@/components/projects/next-project-block'
        import { ProjectHero } from '@/components/projects/project-hero'
        import { isPlaceholderHero } from '@/lib/hero-fallback'
        import { getNextProject } from '@/lib/next-project'
        import { getAll, getProject } from '@/lib/projects'

        interface PageProps {
          params: Promise<{ slug: string }>
        }

        export const dynamicParams = false

        export function generateStaticParams() {
          return getAll().map((p) => ({ slug: p.slug }))
        }

        export async function generateMetadata({ params }: PageProps): Promise<Metadata> {
          const { slug } = await params
          const project = getProject(slug)
          if (!project) return {}

          const ogImage =
            project.ogImage ??
            (!isPlaceholderHero(project.hero.src) ? project.hero.src : '/og-default.png')
          const description = project.description ?? project.tagline

          return {
            title: project.title,
            description,
            openGraph: {
              title: project.title,
              description,
              url: `/projects/${project.slug}`,
              type: 'article',
              images: [
                { url: ogImage, width: 1200, height: 630, alt: project.hero.alt },
              ],
            },
            twitter: {
              card: 'summary_large_image',
              title: project.title,
              description,
              images: [ogImage],
            },
            alternates: { canonical: `/projects/${project.slug}` },
          }
        }

        export default async function ProjectPage({ params }: PageProps) {
          const { slug } = await params
          const project = getProject(slug)
          if (!project) notFound()
          // notFound() throws — TS narrowing doesn't follow it; use non-null assertion below.

          // Pitfall 5 + 11: .mdx extension + literal directory prefix.
          const { default: MDXBody } = await import(`@/content/projects/${slug}.mdx`)
          const next = getNextProject(slug)

          return (
            <article>
              <ProjectHero
                title={project!.title}
                tagline={project!.tagline}
                year={project!.year}
                tags={project!.tags}
                visibility={project!.visibility}
                repoUrl={project!.links.repo}
                hero={project!.hero}
              />
              <MDXProse>
                <MDXBody />
              </MDXProse>
              <NextProjectBlock next={next} />
            </article>
          )
        }

    Run `pnpm vitest run tests/projects/static-params.test.ts tests/projects/page.test.tsx tests/projects/page-metadata.test.ts` — GREEN on all (3 + 3 + 7 = 13 cases).

    Then run `pnpm build`. Build MUST emit a static `/projects/myco` page. Confirm by listing the build output (the executor's verify step does this).

    Then run `pnpm dev` (background, kill after sanity check) and visit `http://localhost:3000/projects/myco` — confirm visually that:
    1. Hero renders text-only (Myco's hero.src is the placeholder path).
    2. Problem / Approach / Outcome H2s render.
    3. The bottom of the page shows "all projects →" → "Browse all projects" (single-project corpus).

    Do NOT remove the `'remark-frontmatter'` line from next.config.ts — Plan 03-00 added rehype plugins ALONGSIDE it. Do NOT call `notFound()` after the dynamic import (Pitfall 9). Do NOT compute `isPlaceholderHero` inline twice — `generateMetadata` and `<ProjectHero>` both need it; `generateMetadata` re-computes it locally (acceptable; the function is one regex test).
  </action>
  <verify>
    <automated>pnpm vitest run tests/projects/static-params.test.ts tests/projects/page.test.tsx tests/projects/page-metadata.test.ts --reporter=dot && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `app/(site)/projects/[slug]/page.tsx` exports default page component, `generateStaticParams`, `generateMetadata`, and `dynamicParams = false`.
    - All 13 tests across the three new test files green.
    - `pnpm build` exits 0.
    - Build output includes a static page for `/projects/myco` (verifiable via `find .next/server/app -name 'myco*'` or equivalent — leave to executor's confirmation step).
    - Page composition order in source: `<ProjectHero>` → `<MDXProse>{<MDXBody />}</MDXProse>` → `<NextProjectBlock>` inside `<article>`.
    - Page calls `notFound()` BEFORE the `await import()` line (Pitfall 9 honored).
    - Dynamic import path uses `@/content/projects/${slug}.mdx` (literal directory prefix per Pitfall 11; .mdx extension per Pitfall 5).
    - `params` typed as `Promise<{ slug: string }>` and awaited (Pitfall 6).
  </acceptance_criteria>
  <done>Page route shipped; PRJ-01 (static params + dynamicParams=false + notFound), PRJ-03 (Myco renders unmodified), PRJ-05 (NextProjectBlock with single-project fallback for Myco-only corpus), PRJ-06 (private fixture privacy assertion), PRJ-07 (metadata + OG fallback chain) all satisfied; full test suite green; pnpm build succeeds.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run --reporter=dot` — full Phase 3 suite green:
  - tests/mdx/{pipeline, prose, components, registration} → 27 cases (Plans 00 + 01)
  - tests/projects/{hero-fallback, tag-chip-row, project-meta, project-hero, next-project, next-project-block, static-params, page, page-metadata} → 7 + 6 + 7 + 8 + 5 + 6 + 3 + 3 + 7 = 52 cases (Plans 00 + 02 + 03)
- Plus all Phase 1 + Phase 2 tests still green (no regression).
- `pnpm typecheck` — clean.
- `pnpm build` — exits 0; static `/projects/myco` page in `.next/server/app/(site)/projects/myco`.
- Visual sanity in `pnpm dev` of `http://localhost:3000/projects/myco`: text-only hero, three H2 anchors with hover-revealed `#`, "all projects →" wayfinding block at bottom, no broken images, no overflow scroll past `max-w-[65ch]` on prose paragraphs.
</verification>

<success_criteria>
- All 7 PRJ-XX requirements satisfied: PRJ-01 (Plan 03 static params + page), PRJ-02 (Plan 00 pipeline + Plan 01 components + page composition), PRJ-03 (Plan 03 page renders Myco unmodified — Phase 2's content-load.test.ts already validated the H2 anchors + word budget), PRJ-04 (Plan 02 ProjectHero + Plan 00 isPlaceholderHero), PRJ-05 (Plan 03 getNextProject + NextProjectBlock), PRJ-06 (Plan 02 ProjectMeta + Plan 03 page integration test against private fixture shape), PRJ-07 (Plan 03 generateMetadata + Plan 00 /og-default.png stub).
- Myco renders end-to-end at `/projects/myco` with text-only hero, full Problem/Approach/Outcome MDX body, single-project NextProjectBlock variant linking to `/projects` (404 until Phase 4 — acceptable per UI-SPEC).
- Zero `'use client'` outside the tiny NextProjectTitle island. Zero new external dependencies (Plan 00 already shipped the four).
- `getNextProject` algorithm is the only place corpus-traversal logic lives — page composition stays clean.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-detail-template/03-03-SUMMARY.md` recording:
- Files created (1 lib helper + 2 components + 1 page route + 5 test files).
- Test results (13 new + 52 total Phase 3 expected green).
- `pnpm build` exit status + confirmation `/projects/myco` is in static output.
- Result of the visual `pnpm dev` sanity pass on `/projects/myco` (3 bullets per UI-SPEC manual checks).
- Whether the page test pattern (vi.doMock + dynamic top-level await import) needed adjustment beyond what this plan documented.
- Confirmation that PRJ-06 integration test (private fixture flowing through the page renders zero repo-URL anchors) is asserting the correct contract.
</output>
