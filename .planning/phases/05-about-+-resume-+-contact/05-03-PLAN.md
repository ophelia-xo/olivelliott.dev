---
phase: 05-about-+-resume-+-contact
plan: 03
type: execute
wave: 2
depends_on: [05-00]
files_modified:
  - app/(site)/about/page.tsx
  - components/about/about-bio.tsx
  - components/about/project-pill-row.tsx
  - components/about/contact-stack.tsx
  - components/about/values-list.tsx
  - tests/about/about-page.test.tsx
  - tests/about/about-bio.test.tsx
  - tests/about/project-pill-row.test.tsx
  - tests/about/contact-stack.test.tsx
  - tests/about/values-list.test.tsx
autonomous: true
requirements: [ABT-01, ABT-02, ABT-03, CTC-03]

must_haves:
  truths:
    - "/about renders a 3-paragraph bio that names Aktiga + anchors the autonomous-workflows / local-first / open-source-communities thesis (ABT-01 + ABT-02)"
    - "/about includes a ValuesList with 3 items: polymath, autonomous workflows, open-source communities (ABT-03)"
    - "/about includes a ContactStack with github + email (mailto with subject hi%20from%20olivelliott.dev) + linkedin — CTC-03 second surface (footer is the first)"
    - "/about lives at app/(site)/about/page.tsx INSIDE the (site)/ route group — inherits Nav + Footer + MotionProvider chrome (unlike /resume which opts out)"
    - "Banned-words list is absent from every Phase 5 about source file (no 'passionate', 'crafted', 'seamless', etc.)"
    - "ProjectPillRow pulls from getHeroProjects() — auto-updates when Phase 7 adds more hero MDX (no hand-listing project slugs)"
    - "Exactly one <h1> on /about ('about'); three <h2> elements (what I'm working on / how to reach me / values); the 'who I am' eyebrow is intentionally a <p>, NOT an h2"
  artifacts:
    - path: "app/(site)/about/page.tsx"
      provides: "RSC route — composes AboutBio + ProjectPillRow + ContactStack + ValuesList inside <article class='about'>; exports static metadata (title='about')"
      min_lines: 50
      exports: ["default", "metadata"]
    - path: "components/about/about-bio.tsx"
      provides: "RSC — 3-paragraph bio block; hardcoded copy from UI-SPEC § Copywriting Contract; max-w-prose"
      exports: ["AboutBio"]
    - path: "components/about/project-pill-row.tsx"
      provides: "RSC — pill row mapping getHeroProjects() to <a href='/projects/{slug}'>{title}</a>; wrapped in <nav aria-label='Featured projects'>; mirrors Phase 3 TagChipRow visual treatment but with project slugs (Pitfall 3-style separation — do NOT reuse TagChipRow)"
      exports: ["ProjectPillRow"]
    - path: "components/about/contact-stack.tsx"
      provides: "RSC — <ul role='list'> of 3 stacked contact links: github + mailto (with %20 subject — Pitfall 5) + linkedin; mono labels"
      exports: ["ContactStack"]
    - path: "components/about/values-list.tsx"
      provides: "RSC — <dl> with 3 <dt>/<dd> pairs for polymath / autonomous workflows / open-source communities; locked copy from UI-SPEC § Copywriting Contract"
      exports: ["ValuesList"]
  key_links:
    - from: "components/about/project-pill-row.tsx"
      to: "@/lib/projects > getHeroProjects()"
      via: "named import; loops projects to render <a href='/projects/{slug}'>{title}</a>"
      pattern: "getHeroProjects"
    - from: "components/about/contact-stack.tsx mailto link"
      to: "the email subject string 'hi from olivelliott.dev'"
      via: "literal %20-encoded URL string per RFC 6068"
      pattern: "subject=hi%20from%20olivelliott\\.dev"
    - from: "app/(site)/about/page.tsx"
      to: "AboutBio + ProjectPillRow + ContactStack + ValuesList"
      via: "named imports + composition inside <article class='about'>"
      pattern: "<AboutBio|<ProjectPillRow|<ContactStack|<ValuesList"
---

<objective>
Wave 2 (Lane A — parallel to Plan 05-02) — Build the entire `/about` surface: the RSC route, four small RSC components (AboutBio, ProjectPillRow, ContactStack, ValuesList), and the implemented tests covering ABT-01..03 + CTC-03's second surface. Zero motion islands, zero client components, zero icon usage — matches UI-SPEC § Anti-Patterns.

This plan does NOT depend on Plan 05-01 or 05-02 — `/about` does not consume `RESUME`. It depends only on Plan 05-00 (test scaffolds + dependency install) and on Phase 4 having shipped (`getHeroProjects()` already exists from Phase 2; ProjectPillRow consumes it).

Purpose: lock the bio, the values, the project-pill row, and the second contact surface in a single coherent plan. The footer is the FIRST contact surface (Phase 1 already ships it; Plan 05-04 updates it); ContactStack on /about is the SECOND, satisfying CTC-03.

Output: 1 page + 4 components (~150 LOC), 5 implemented test files (replacing Wave 0 skips).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-about-+-resume-+-contact/05-CONTEXT.md
@.planning/phases/05-about-+-resume-+-contact/05-RESEARCH.md
@.planning/phases/05-about-+-resume-+-contact/05-UI-SPEC.md
@.planning/phases/05-about-+-resume-+-contact/05-VALIDATION.md
@.planning/phases/05-about-+-resume-+-contact/05-00-PLAN.md
@app/(site)/layout.tsx
@app/(site)/projects/page.tsx
@components/projects/tag-chip-row.tsx
@components/projects/card-meta.tsx
@lib/projects.ts
@lib/utils.ts
@styles/tokens.css

<interfaces>
<!-- /about consumes existing Phase 2 / Phase 4 surfaces; introduces nothing for downstream. -->

From @/lib/projects (Phase 2; reused):
```typescript
export function getHeroProjects(): readonly Project[]
// Returns hero-tier active projects, order-sorted. Currently: Myco.
// Auto-grows when Phase 7 authors Fathom, Agenda Keeper MDX.
```

From @/lib/content > Project type (transitively):
```typescript
type Project = { slug: string; title: string; tier: 'hero' | 'secondary'; ... }
```

Components exposed by this plan (all RSC; no 'use client'; no motion; no Lucide):
```typescript
// components/about/about-bio.tsx
export function AboutBio(): JSX.Element

// components/about/project-pill-row.tsx
export function ProjectPillRow(): JSX.Element   // calls getHeroProjects() server-side

// components/about/contact-stack.tsx
export function ContactStack(): JSX.Element

// components/about/values-list.tsx
export function ValuesList(): JSX.Element
```

Banned words (Phase 4 list + Phase 5 additions — full list from UI-SPEC § anti-aesthetic prohibitions):
  passionate, award-winning, scalable solutions, cutting-edge, 10x, crafted, seamless,
  leveraging, synergy, rockstar, ninja, results-driven, self-starter, team player,
  go-getter, thought leader, dynamic, innovative, transformative, ecosystem, paradigm,
  next-generation

Phase 4 ProjectPillRow / TagChipRow visual class string (mirror — do NOT import TagChipRow):
  font-mono text-[var(--text-label)] font-medium tracking-[0.02em] lowercase
  text-[color:var(--color-text-secondary)] hover:text-[color:var(--color-text-primary)]
  bg-[color:var(--color-surface-2)] px-3 py-2 -my-2 rounded-sm

Email subject locked across surfaces (UI-SPEC § Copywriting Contract):
  mailto:olivelliott48@gmail.com?subject=hi%20from%20olivelliott.dev
  (Pitfall 5: %20 NOT +; RFC 6068)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-03-01: Create AboutBio + ValuesList components (the two static-copy primitives) + tests</name>
  <files>components/about/about-bio.tsx, components/about/values-list.tsx, tests/about/about-bio.test.tsx, tests/about/values-list.test.tsx</files>
  <read_first>
    - .planning/phases/05-about-+-resume-+-contact/05-UI-SPEC.md § Copywriting Contract > /about page copy (lines ~896–918) — exact bio + values copy to author verbatim
    - .planning/phases/05-about-+-resume-+-contact/05-UI-SPEC.md § Component Inventory § 2 (lines ~451–459) — AboutBio contract
    - .planning/phases/05-about-+-resume-+-contact/05-UI-SPEC.md § Component Inventory § 5 (lines ~500–515) — ValuesList contract
    - .planning/phases/05-about-+-resume-+-contact/05-UI-SPEC.md § anti-aesthetic prohibitions (line ~24) — full banned words list
    - app/globals.css (verify `.prose` rule set is available — it's from Phase 3)
  </read_first>
  <behavior>
    AboutBio:
    - Test 1: renders exactly 3 `<p>` elements (the 3-paragraph bio block)
    - Test 2: paragraph 2 contains the literal substring `'Aktiga'` (ABT-02 — names the current role)
    - Test 3: rendered text mentions `'autonomous workflows'` OR `'autonomous-workflow'` (ABT-01 — anchors the thesis; matches one of the two casings from .docx + UI-SPEC bio paragraph 2 'AI-workflow design' uses hyphen)
    - Test 4: rendered text mentions `'local-first'` OR `'Appalachian'` (ABT-01 — thesis OR education anchor — these are the two main thesis cues per UI-SPEC bio paragraphs)
    - Test 5: outer wrapper has class `prose` AND `max-w-prose` (typographic measure per UI-SPEC § Container + Layout)
    - Test 6: banned-words negative — concatenate all rendered text and assert no match against the banned-words regex (full list per `<interfaces>` block above)
    - Test 7: source-grep — no `'use client'` in `components/about/about-bio.tsx`
    - Test 8: source-grep — no import from `motion/react` or `lucide-react`

    ValuesList:
    - Test 1: renders a `<dl>` with exactly 3 `<dt>`/`<dd>` pairs (UI-SPEC § Component 5)
    - Test 2: the three `<dt>` textContents are exactly `['polymath', 'autonomous workflows', 'open-source communities']` IN THIS ORDER (UI-SPEC § Component 5 locked values)
    - Test 3: each `<dd>` contains the EXACT locked descriptor from UI-SPEC § Copywriting Contract (lines ~913–918):
      - polymath: 'work moves across engineering, anthropology, and creative practice — no one of them is the whole picture.'
      - autonomous workflows: 'tools should do the obvious work so we can spend attention on the work that's actually ours.'
      - open-source communities: 'what gets built in the open belongs to everyone who builds on it.'
    - Test 4: each `<dt>` has primary-text color class AND font-medium (body 16px @ 500 — body-medium activation per UI-SPEC § Component 5)
    - Test 5: banned-words negative on rendered text
    - Test 6: source-grep — no `'use client'` in components/about/values-list.tsx
  </behavior>
  <action>
    Create the two static-copy components. ALL copy is hardcoded verbatim from UI-SPEC § Copywriting Contract — do NOT paraphrase.

    **`components/about/about-bio.tsx`** — 3 paragraphs verbatim from UI-SPEC § Copywriting Contract bio paragraphs 1–3 (lines ~903–905):

    ```tsx
    // components/about/about-bio.tsx
    // 3-paragraph bio block for /about. RSC; hardcoded copy verbatim from
    // UI-SPEC § Copywriting Contract. Names Aktiga (ABT-02) and anchors the
    // autonomous-workflows / local-first / polymath thesis (ABT-01). If
    // Phase 7 wants to source from content/about.ts, it's an additive
    // refactor with no UI change.
    export function AboutBio() {
      return (
        <div className="prose max-w-prose">
          <p className="text-[var(--text-body)] text-[color:var(--color-text-primary)] leading-[1.6]">
            I'm an engineer building tools that let people spend attention on the work that's actually theirs.
            My focus is on autonomous workflows, local-first systems, and the small details of tooling that
            compound into hours saved.
          </p>
          <p className="text-[var(--text-body)] text-[color:var(--color-text-primary)] leading-[1.6] mt-4">
            I'm currently at Aktiga, leading system architecture and AI-workflow design for internal tooling
            and developer platforms. I author and maintain the developer documentation (Starlight on Astro)
            and coordinate engineering priorities across the team.
          </p>
          <p className="text-[var(--text-body)] text-[color:var(--color-text-primary)] leading-[1.6] mt-4">
            Before software, I studied cultural anthropology and sustainable development at Appalachian State.
            That perspective shows up in how I think about the systems I build — who they're for, what they
            pull people toward, and whether they're something we want to live with at scale.
          </p>
        </div>
      )
    }
    ```

    **`components/about/values-list.tsx`** — 3 dt/dd pairs verbatim from UI-SPEC § Copywriting Contract values items 1–3:

    ```tsx
    // components/about/values-list.tsx
    // 3-value definition list. RSC; locked copy from UI-SPEC § Copywriting
    // Contract. Each <dt>/<dd> is wrapped in a <div className="contents">
    // so the flex/grid layout flattens the pair into the dl's flow — same
    // pattern as Phase 3's resume-skills dl.
    export function ValuesList() {
      return (
        <dl className="flex flex-col gap-4">
          <div className="contents">
            <dt className="text-[var(--text-body)] font-medium text-[color:var(--color-text-primary)]">polymath</dt>
            <dd className="text-[var(--text-body)] text-[color:var(--color-text-secondary)] mt-1">
              work moves across engineering, anthropology, and creative practice — no one of them is the whole picture.
            </dd>
          </div>
          <div className="contents">
            <dt className="text-[var(--text-body)] font-medium text-[color:var(--color-text-primary)]">autonomous workflows</dt>
            <dd className="text-[var(--text-body)] text-[color:var(--color-text-secondary)] mt-1">
              tools should do the obvious work so we can spend attention on the work that's actually ours.
            </dd>
          </div>
          <div className="contents">
            <dt className="text-[var(--text-body)] font-medium text-[color:var(--color-text-primary)]">open-source communities</dt>
            <dd className="text-[var(--text-body)] text-[color:var(--color-text-secondary)] mt-1">
              what gets built in the open belongs to everyone who builds on it.
            </dd>
          </div>
        </dl>
      )
    }
    ```

    REPLACE Wave 0 placeholders in `tests/about/about-bio.test.tsx` (8 tests) and `tests/about/values-list.test.tsx` (6 tests) per `<behavior>`. For the banned-words negative tests, define a single shared `BANNED` regex constant at the top of each test file (or `tests/about/banned-words.ts` helper — recommend inline regex per file to keep the test self-contained).

    Commit message format: `feat(05-03): add AboutBio + ValuesList components + 14 tests`
  </action>
  <verify>
    <automated>pnpm vitest run tests/about/about-bio.test.tsx tests/about/values-list.test.tsx --reporter=dot &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Both component files exist with the exact contents above (copy is verbatim from UI-SPEC § Copywriting Contract)
    - [ ] Bio paragraph 2 contains the substring `'Aktiga'` (ABT-02)
    - [ ] ValuesList <dt> values are exactly `['polymath', 'autonomous workflows', 'open-source communities']` in order
    - [ ] All 14 tests pass (8 bio + 6 values)
    - [ ] No banned word appears in either component source
    - [ ] Both components are RSC (no `'use client'`)
    - [ ] No import from motion/react or lucide-react
    - [ ] `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Bio + values copy are locked, banned-word-clean, and tested. /about page can compose them in Task 05-03-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-03-02: Create ProjectPillRow + ContactStack components (the two link primitives) + tests</name>
  <files>components/about/project-pill-row.tsx, components/about/contact-stack.tsx, tests/about/project-pill-row.test.tsx, tests/about/contact-stack.test.tsx</files>
  <read_first>
    - .planning/phases/05-about-+-resume-+-contact/05-UI-SPEC.md § Component Inventory § 3 (lines ~461–472) — ProjectPillRow contract (mirrors Phase 3 TagChipRow VISUAL treatment but does NOT import TagChipRow — Pitfall 3-style separation)
    - .planning/phases/05-about-+-resume-+-contact/05-UI-SPEC.md § Component Inventory § 4 (lines ~474–498) — ContactStack contract; locked email subject + GitHub handle + LinkedIn placeholder
    - .planning/phases/05-about-+-resume-+-contact/05-UI-SPEC.md § Copywriting Contract > /about contact link labels (lines ~909–911) — literal labels
    - .planning/phases/05-about-+-resume-+-contact/05-RESEARCH.md § Anti-Patterns > "Reusing TagChipRow" (line ~476) — explicit warning to hand-author, not refactor
    - .planning/phases/05-about-+-resume-+-contact/05-RESEARCH.md § Pitfall 5 (lines ~568–576) — %20 vs + mailto encoding
    - components/projects/tag-chip-row.tsx (Phase 3 — reference VISUAL class string to mirror; do NOT import)
    - components/projects/card-meta.tsx (Phase 4 — reference for the chip class pattern; same Pitfall 3 separation lesson)
    - lib/projects.ts (verify getHeroProjects exists and is async-free — Phase 2 confirmed it's sync, but re-verify shape)
  </read_first>
  <behavior>
    ProjectPillRow:
    - Test 1: renders inside a `<nav aria-label="Featured projects">` (UI-SPEC § Component 3 — landmark label)
    - Test 2: renders a `<ul>` whose `<li>` count matches `getHeroProjects().length` (must call the real helper, NOT mock — proves the integration; if Phase 7 adds Fathom/Agenda Keeper to hero tier, pill count auto-grows)
    - Test 3: each pill is an `<a>` whose href matches `/projects/{slug}` where slug is the project's slug from getHeroProjects (assert by iterating)
    - Test 4: each pill's text content equals the project's `title` from getHeroProjects (assert by iterating; the lowercase CSS handles visual flattening, but text node is verbatim from data)
    - Test 5: pill classes include the Phase 3 chip class string subset — `font-mono text-[var(--text-label)] lowercase bg-[color:var(--color-surface-2)] px-3 py-2 -my-2 rounded-sm` (mirror — verify by asserting at least these classes are present on each pill anchor; full visual treatment via CSS)
    - Test 6: source-grep — `components/about/project-pill-row.tsx` does NOT import `TagChipRow` (Pitfall 3-style separation lock — explicit hand-author, not refactor)
    - Test 7: source-grep — does NOT contain `'use client'`
    - Test 8: source-grep — no import from motion/react or lucide-react

    ContactStack:
    - Test 1: renders a `<ul role="list">` with exactly 3 `<li>` children (UI-SPEC § Component 4)
    - Test 2: first link href starts with `https://github.com/olivelliott` (NOT 'ophelia-x' — canonical handle per STATE.md)
    - Test 3: first link textContent is the literal `'github.com/olivelliott'`
    - Test 4: second link href === `mailto:olivelliott48@gmail.com?subject=hi%20from%20olivelliott.dev` (CTC-02 EXACT match; Pitfall 5)
    - Test 5: second link textContent is the literal `'olivelliott48@gmail.com'`
    - Test 6: third link href starts with `https://linkedin.com/in/` (LinkedIn handle is PLACEHOLDER per RESEARCH § Open Q 2 — assert the URL prefix, not the full handle)
    - Test 7: github + linkedin links have `target="_blank"` AND `rel="noopener noreferrer"`; mailto link does NOT (internal protocol — no rel needed)
    - Test 8: each <a> has `py-3` class (44×44 touch target via vertical padding per UI-SPEC § Spacing Scale exceptions for /about)
    - Test 9: source-grep — no `'use client'` in components/about/contact-stack.tsx
    - Test 10: source-grep — no Lucide import (no icons in /about contact links — they're plain mono text)
  </behavior>
  <action>
    Create the two link components.

    **`components/about/project-pill-row.tsx`** — calls `getHeroProjects()` at render time (server-side; the call is synchronous, RSC executes server-side):

    ```tsx
    // components/about/project-pill-row.tsx
    // Pill row on /about — links to /projects/{slug} for each hero-tier
    // project. RSC; calls getHeroProjects() at render time so the row
    // auto-updates when Phase 7 adds more hero MDX. DO NOT import
    // TagChipRow — Phase 3's chip is for tag URL state on /projects,
    // this is for project navigation on /about (Pitfall 3-style separation).
    // UI-SPEC § Component 3.
    import { getHeroProjects } from '@/lib/projects'

    export function ProjectPillRow() {
      const projects = getHeroProjects()

      return (
        <nav aria-label="Featured projects" className="mt-4">
          <ul role="list" className="flex flex-wrap gap-3">
            {projects.map((p) => (
              <li key={p.slug}>
                <a
                  href={`/projects/${p.slug}`}
                  className="font-mono text-[var(--text-label)] font-medium tracking-[0.02em] lowercase bg-[color:var(--color-surface-2)] text-[color:var(--color-text-secondary)] hover:text-[color:var(--color-text-primary)] focus-visible:text-[color:var(--color-text-primary)] px-3 py-2 -my-2 rounded-sm"
                >
                  {p.title}
                </a>
              </li>
            ))}
          </ul>
        </nav>
      )
    }
    ```

    **`components/about/contact-stack.tsx`** — three plain mono links:

    ```tsx
    // components/about/contact-stack.tsx
    // Three vertically stacked contact links for /about > how to reach me.
    // RSC; no icons; mono labels. Email subject is the locked Phase 5
    // string (Pitfall 5: %20 not +, per RFC 6068). LinkedIn handle is a
    // PLACEHOLDER (RESEARCH § Open Q 2) — confirm with Olive before Phase 7.
    // UI-SPEC § Component 4; CTC-02 + CTC-03 second surface.
    const GITHUB_URL = 'https://github.com/olivelliott'
    const EMAIL_URL = 'mailto:olivelliott48@gmail.com?subject=hi%20from%20olivelliott.dev'
    // PLACEHOLDER: confirm LinkedIn handle with Olive — currently the Phase 1 placeholder.
    const LINKEDIN_URL = 'https://linkedin.com/in/olive-elliott'

    const LINK_CLASS =
      'font-mono text-[var(--text-label)] font-medium tracking-[0.02em] lowercase ' +
      'text-[color:var(--color-text-secondary)] hover:text-[color:var(--color-text-primary)] ' +
      'focus-visible:text-[color:var(--color-text-primary)] py-3 inline-block'

    export function ContactStack() {
      return (
        <ul role="list" className="flex flex-col gap-4">
          <li>
            <a href={GITHUB_URL} target="_blank" rel="noopener noreferrer" className={LINK_CLASS}>
              github.com/olivelliott
            </a>
          </li>
          <li>
            <a href={EMAIL_URL} className={LINK_CLASS}>
              olivelliott48@gmail.com
            </a>
          </li>
          <li>
            <a href={LINKEDIN_URL} target="_blank" rel="noopener noreferrer" className={LINK_CLASS}>
              {LINKEDIN_URL.replace(/^https?:\/\//, '')}
            </a>
          </li>
        </ul>
      )
    }
    ```

    REPLACE Wave 0 placeholders in `tests/about/project-pill-row.test.tsx` (8 tests) and `tests/about/contact-stack.test.tsx` (10 tests). For ProjectPillRow tests, import the real `getHeroProjects` from `@/lib/projects` so the test verifies the actual data flow (Phase 2 already proved getHeroProjects works — this asserts ProjectPillRow consumes it correctly).

    Commit message format: `feat(05-03): add ProjectPillRow + ContactStack components + 18 tests`
  </action>
  <verify>
    <automated>pnpm vitest run tests/about/project-pill-row.test.tsx tests/about/contact-stack.test.tsx --reporter=dot &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Both component files exist with the exact contents above
    - [ ] ProjectPillRow does NOT import TagChipRow (verify via source-grep test — Pitfall 3-style lock)
    - [ ] ContactStack email href contains literal `%20` (not `+`)
    - [ ] ContactStack github URL uses `olivelliott` handle (NOT `ophelia-x`)
    - [ ] LinkedIn URL has explicit `// PLACEHOLDER` comment in source
    - [ ] All 18 tests pass (8 pill row + 10 contact stack)
    - [ ] No banned word appears in either component source
    - [ ] Both components are RSC (no `'use client'`)
    - [ ] No Lucide import
    - [ ] `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Pill row + contact stack are in place; the /about page can compose all four about components in Task 05-03-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-03-03: Create /about page route + extend about-page.test.tsx</name>
  <files>app/(site)/about/page.tsx, tests/about/about-page.test.tsx</files>
  <read_first>
    - .planning/phases/05-about-+-resume-+-contact/05-UI-SPEC.md § /about document outline (lines ~282–319) — exact markup shape including section/eyebrow/h2 hierarchy
    - .planning/phases/05-about-+-resume-+-contact/05-UI-SPEC.md § Component Inventory § 1 (lines ~431–449) — AboutPage metadata contract
    - .planning/phases/05-about-+-resume-+-contact/05-UI-SPEC.md § Copywriting Contract > /about page copy (lines ~896–908) — page H1 + eyebrows + framing paragraph
    - .planning/phases/05-about-+-resume-+-contact/05-RESEARCH.md § Pattern 5 (lines ~423–448) — per-route metadata example
    - app/(site)/projects/page.tsx (Phase 4 — pattern for static `const metadata: Metadata` + openGraph.images per-route declaration; STATE.md decision: home OMITS title but other routes declare it)
    - components/about/{about-bio,project-pill-row,contact-stack,values-list}.tsx (Tasks 05-03-01 + 05-03-02 outputs — confirm import paths and prop shapes)
  </read_first>
  <behavior>
    - Test 1: route renders without throwing
    - Test 2: exactly one `<h1>` element with textContent `'about'`
    - Test 3: exactly THREE `<h2>` elements with textContents `['what I'm working on', 'how to reach me', 'values']` IN THIS ORDER (UI-SPEC § /about document outline; the 'who I am' eyebrow is intentionally a <p>, NOT h2)
    - Test 4: the 'who I am' eyebrow is a `<p>` with id `who-i-am-eyebrow` (NOT an h2 — Note in UI-SPEC § Heading hierarchy on /about)
    - Test 5: four `<section>` elements, each with the correct aria-labelledby pointing at the eyebrow/h2 ids — `['who-i-am-eyebrow', 'working-on-eyebrow', 'reach-me-eyebrow', 'values-eyebrow']`
    - Test 6: AboutBio is rendered (assert by querying for the Aktiga substring inside a paragraph in the bio's section)
    - Test 7: ProjectPillRow is rendered (assert by querying for `nav[aria-label="Featured projects"]`)
    - Test 8: ContactStack is rendered (assert by querying for the mailto link with the locked subject)
    - Test 9: ValuesList is rendered (assert by querying for one of the locked <dt> values like 'polymath')
    - Test 10: framing paragraph above the project pill row contains the literal string 'Three projects are getting most of my focus right now' (UI-SPEC § Copywriting Contract framing paragraph)
    - Test 11: metadata export exists with `title === 'about'` (composes via root titleTemplate per Phase 4 STATE.md decision)
    - Test 12: metadata.openGraph.images is declared (per Phase 4 STATE.md note: 'openGraph.images MUST be declared per-route because root layout omits them')
    - Test 13: metadata.description contains 'Aktiga' AND ('autonomous workflows' OR 'autonomous-workflows') — confirms the description is the locked thesis line
    - Test 14: source-grep — `app/(site)/about/page.tsx` does NOT contain `'use client'`
    - Test 15: source-grep — does NOT import from motion/react or lucide-react
  </behavior>
  <action>
    Create `app/(site)/about/page.tsx`. The file lives INSIDE the (site)/ route group — it inherits Nav, Footer, MotionProvider, SkipLink from app/(site)/layout.tsx. No chrome to ship in the page itself; just the article and its sections.

    ```tsx
    // app/(site)/about/page.tsx
    // /about RSC route. Composes AboutBio + ProjectPillRow + ContactStack +
    // ValuesList inside a single-column <article>. Lives INSIDE the (site)/
    // group so it inherits Nav + Footer + MotionProvider chrome (contrast
    // with /resume which opts out via app/resume/).
    // UI-SPEC § /about document outline; § Component Inventory § 1.
    // ABT-01 + ABT-02 + ABT-03 + CTC-03 (second surface; footer is first).
    import type { Metadata } from 'next'
    import { AboutBio } from '@/components/about/about-bio'
    import { ContactStack } from '@/components/about/contact-stack'
    import { ProjectPillRow } from '@/components/about/project-pill-row'
    import { ValuesList } from '@/components/about/values-list'

    export const metadata: Metadata = {
      title: 'about',
      description:
        'Olive Elliott is an engineer focused on autonomous workflows, local-first systems, and tools that support open-source communities. Currently building at Aktiga.',
      alternates: { canonical: '/about' },
      openGraph: {
        title: 'about · olivelliott.dev',
        description:
          'Olive Elliott is an engineer focused on autonomous workflows, local-first systems, and tools that support open-source communities. Currently building at Aktiga.',
        url: '/about',
        type: 'website',
        images: [
          {
            url: '/og-default.png',
            width: 1200,
            height: 630,
            alt: 'olivelliott.dev — engineer, autonomy, local-first',
          },
        ],
      },
      twitter: { card: 'summary_large_image', images: ['/og-default.png'] },
    }

    export default function AboutPage() {
      return (
        <article className="about flex flex-col gap-12">
          <h1 className="text-[var(--text-display)] lowercase font-medium tracking-[-0.02em] text-[color:var(--color-text-primary)]">
            about
          </h1>

          <section aria-labelledby="who-i-am-eyebrow">
            <p
              id="who-i-am-eyebrow"
              className="font-mono text-[var(--text-label)] font-medium tracking-[0.02em] lowercase text-[color:var(--color-text-tertiary)]"
            >
              who I am
            </p>
            <div className="mt-4">
              <AboutBio />
            </div>
          </section>

          <section aria-labelledby="working-on-eyebrow">
            <h2
              id="working-on-eyebrow"
              className="text-[var(--text-h2)] font-semibold tracking-[-0.015em] text-[color:var(--color-text-primary)]"
            >
              what I'm working on
            </h2>
            <p className="mt-4 text-[var(--text-body)] text-[color:var(--color-text-secondary)] max-w-prose leading-[1.6]">
              Three projects are getting most of my focus right now — built to test the thesis
              that small, local-first tools earn their keep by staying out of the way.
            </p>
            <ProjectPillRow />
          </section>

          <section aria-labelledby="reach-me-eyebrow">
            <h2
              id="reach-me-eyebrow"
              className="text-[var(--text-h2)] font-semibold tracking-[-0.015em] text-[color:var(--color-text-primary)]"
            >
              how to reach me
            </h2>
            <div className="mt-4">
              <ContactStack />
            </div>
          </section>

          <section aria-labelledby="values-eyebrow">
            <h2
              id="values-eyebrow"
              className="text-[var(--text-h2)] font-semibold tracking-[-0.015em] text-[color:var(--color-text-primary)]"
            >
              values
            </h2>
            <div className="mt-4">
              <ValuesList />
            </div>
          </section>
        </article>
      )
    }
    ```

    REPLACE Wave 0 placeholder in `tests/about/about-page.test.tsx` with the 15 tests from `<behavior>`. Use real component imports (the four about components are already in place from Tasks 01 + 02) — this is an integration test, not unit isolation.

    Commit message format: `feat(05-03): /about route — RSC page composing 4 about components + metadata + 15 tests`
  </action>
  <verify>
    <automated>pnpm vitest run tests/about/ --reporter=dot &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `app/(site)/about/page.tsx` exists INSIDE the (site)/ group
    - [ ] Exports `default` AND `metadata`
    - [ ] Page is RSC (no `'use client'`)
    - [ ] Single H1 ('about'), three H2s in locked order, 'who I am' eyebrow is a <p>
    - [ ] All 15 page tests pass
    - [ ] All 5 about test files now have implemented bodies (no `it.skip` remaining in `tests/about/`)
    - [ ] `pnpm typecheck` exits 0
    - [ ] Manual: `pnpm dev` then load http://localhost:3000/about — sees nav + footer (inherited from (site)/) + the page sections
  </acceptance_criteria>
  <done>/about route ships with all four components composed; bio names Aktiga; pill row pulls from real Phase 2 helper; contact stack provides the second CTC-03 surface; values list locks the 3 thesis values.</done>
</task>

</tasks>

<verification>
After all tasks:

```bash
# All about tests green; the 5 Wave-0 skips are now implemented
pnpm vitest run tests/about/

# Full suite stable
pnpm vitest run

# Typecheck clean
pnpm typecheck

# Manual: load /about
pnpm dev
# Open http://localhost:3000/about
# Expect: site nav top, single-column bio + sections, project pill row showing Myco, contact stack with 3 links, values list with 3 items, footer at bottom
# Verify mailto link in contact stack opens with subject "hi from olivelliott.dev" (NOT "hi+from+olivelliott.dev")

# Confirm path is correct (NOT under app/resume/)
test -f 'app/(site)/about/page.tsx'
test ! -f 'app/about/page.tsx'

# Source-grep guards
grep -l 'use client' components/about/ app/\(site\)/about/page.tsx 2>/dev/null || echo "no client islands in /about — correct"
```
</verification>

<success_criteria>
- [ ] /about renders 3-paragraph bio + project pill row + contact stack + values list
- [ ] Bio names Aktiga (ABT-02)
- [ ] Bio anchors autonomous-workflows / local-first / open-source thesis (ABT-01)
- [ ] ValuesList has 3 items: polymath / autonomous workflows / open-source communities (ABT-03)
- [ ] ContactStack provides second CTC-03 surface; mailto subject hi%20from%20olivelliott.dev (Pitfall 5)
- [ ] ProjectPillRow auto-grows via getHeroProjects() (no hard-coded slugs)
- [ ] Single H1 ('about'), three H2s, 'who I am' eyebrow is <p> not h2
- [ ] Page lives INSIDE (site)/ group (inherits nav + footer + motion provider)
- [ ] Zero client islands, zero motion, zero Lucide imports in any /about source
- [ ] Banned words absent from all /about sources (regression test in Task 01)
- [ ] 5 implemented test files (about-bio, project-pill-row, contact-stack, values-list, about-page) — 47 total tests
- [ ] pnpm typecheck exits 0
- [ ] Full vitest suite stays green
</success_criteria>

<output>
After completion, create `.planning/phases/05-about-+-resume-+-contact/05-03-SUMMARY.md` covering:
- Confirm Pitfall 3-style separation — ProjectPillRow is its own component, did NOT refactor TagChipRow
- Confirm Pitfall 5 — mailto uses %20 (verified by contact-stack.test.tsx Test 4 + about-page.test.tsx Test 8)
- Confirm the canonical github handle 'olivelliott' (NOT 'ophelia-x') in ContactStack — Plan 05-04 will apply the same correction to the footer
- Note LinkedIn handle is still PLACEHOLDER — flag for Phase 7 confirmation step
- Note ABT-01..03 + CTC-03 (second surface) all validated by tests
</output>
