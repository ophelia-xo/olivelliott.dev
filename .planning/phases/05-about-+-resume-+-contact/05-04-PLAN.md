---
phase: 05-about-+-resume-+-contact
plan: 04
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - components/site/footer.tsx
  - tests/site/footer.test.tsx
autonomous: true
requirements: [CTC-01, CTC-02, RES-05]

must_haves:
  truths:
    - "Footer GitHub URL is https://github.com/olivelliott (canonical handle per STATE.md — was 'ophelia-x' Phase 1 placeholder)"
    - "Footer view-source URL is https://github.com/olivelliott/portfolio (same correction — was 'ophelia-x')"
    - "Footer mailto href contains literal `?subject=hi%20from%20olivelliott.dev` (CTC-02 + Pitfall 5: %20 not +; RFC 6068)"
    - "Footer renders a DownloadPdfLink (RES-05 second surface; first is the resume header from Plan 05-02)"
    - "A literal `·` interpunct separator with aria-hidden='true' sits between DownloadPdfLink and 'view source' in the footer right slot"
    - "All four existing footer features still work: copyright text, GitHub/Email/LinkedIn icon row (Phase 1), view-source link, mobile stack layout"
  artifacts:
    - path: "components/site/footer.tsx"
      provides: "Updated Phase 1 footer — canonical github handle, CTC-02 mailto subject, DownloadPdfLink + interpunct + view-source in the right slot"
      contains: "DownloadPdfLink"
    - path: "tests/site/footer.test.tsx"
      provides: "Footer regression test — locks the canonical github handle, the mailto subject, the DownloadPdfLink presence, and verifies the inherited Phase 1 features did NOT break"
  key_links:
    - from: "components/site/footer.tsx"
      to: "components/resume/download-pdf-link"
      via: "named import + render inside the right slot of the footer chrome"
      pattern: "from\\s+['\"]@/components/resume/download-pdf-link['\"]"
    - from: "Footer mailto href"
      to: "the email subject string"
      via: "URL-encoded query parameter (Pitfall 5)"
      pattern: "subject=hi%20from%20olivelliott\\.dev"
---

<objective>
Wave 3 — Apply the three-line footer correction documented in RESEARCH § Example 4 (GitHub handle fix, mailto subject correction, DownloadPdfLink insertion), and add the implemented `tests/site/footer.test.tsx` that locks the corrections + makes the Phase 1 footer features regression-proof against future refactors. This is the SMALLEST plan in Phase 5 — one source file modification, one test file extension, three behavioral changes — but it satisfies three requirements (CTC-01, CTC-02, RES-05's second surface) and makes the site's contact + download story complete across all pages.

Depends on Plan 05-02 because `DownloadPdfLink` from `components/resume/download-pdf-link.tsx` is composed here. No dependency on Plan 05-03 (about) — though both touch the contact story, they touch different files.

Purpose: lock the contact + download affordances on EVERY page (the footer renders inside `app/(site)/layout.tsx` on every (site)/ route). Combined with Plan 05-03's ContactStack on /about, this satisfies CTC-03's "at least two places" requirement (footer + /about).

Output: 1 modified component (~3-line diff + import + 1 JSX insertion), 1 implemented test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-about-+-resume-+-contact/05-CONTEXT.md
@.planning/phases/05-about-+-resume-+-contact/05-RESEARCH.md
@.planning/phases/05-about-+-resume-+-contact/05-UI-SPEC.md
@.planning/phases/05-about-+-resume-+-contact/05-VALIDATION.md
@.planning/phases/05-about-+-resume-+-contact/05-02-SUMMARY.md
@components/site/footer.tsx
@components/resume/download-pdf-link.tsx
@.planning/phases/01-foundation/01-04-SUMMARY.md

<interfaces>
<!-- DownloadPdfLink shape from Plan 05-02 (already implemented). -->

From @/components/resume/download-pdf-link:
```typescript
export function DownloadPdfLink(props?: { className?: string }): JSX.Element
// Renders <a href="/resume.pdf" download class="download-pdf-link ...">download.pdf ↓</a>
// RSC; no client behavior. Optional className merged for placement flexibility.
```

Existing footer (Phase 1; components/site/footer.tsx — current state per repo at planning time):
```typescript
const GITHUB_URL = 'https://github.com/ophelia-x'                  // ← CHANGE to 'olivelliott'
const EMAIL_URL = 'mailto:olivelliott48@gmail.com?subject=olivelliott.dev'  // ← CHANGE subject
const LINKEDIN_URL = 'https://linkedin.com/in/olive-elliott'       // PLACEHOLDER — leave (Phase 7)
const VIEW_SOURCE_URL = 'https://github.com/ophelia-x/portfolio'   // ← CHANGE to 'olivelliott'

// Right slot composition (current):
//   <ul>...3 icon links...</ul>
//   <a>view source</a>
// Target right slot composition (Phase 5):
//   <ul>...3 icon links...</ul>
//   <DownloadPdfLink />
//   <span aria-hidden="true">·</span>
//   <a>view source</a>
```

Banned-word list (already in PHASE 4 anti-pattern test — Plan 05-05 extends the manifest):
  passionate, scalable solutions, cutting-edge, crafted, seamless, leveraging,
  synergy, rockstar, ninja, innovative, transformative, ecosystem, paradigm,
  next-generation, award-winning, results-driven, self-starter, team player,
  go-getter, thought leader, dynamic

(Footer copy contains none of these; verification is via Plan 05-05's manifest extension.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-04-01: Update footer constants (GitHub handle + mailto subject + view-source URL) + insert DownloadPdfLink</name>
  <files>components/site/footer.tsx, tests/site/footer.test.tsx</files>
  <read_first>
    - components/site/footer.tsx (current Phase 1 file — verify the four constants are still where the diff expects them: GITHUB_URL, EMAIL_URL, LINKEDIN_URL, VIEW_SOURCE_URL)
    - .planning/phases/05-about-+-resume-+-contact/05-RESEARCH.md § Example 4 (lines ~854–875) — the EXACT 3-line diff + DownloadPdfLink insertion shape
    - .planning/phases/05-about-+-resume-+-contact/05-UI-SPEC.md § Component Inventory § 11 (lines ~612–635) — footer update contract
    - .planning/phases/05-about-+-resume-+-contact/05-CONTEXT.md § Footer Tweaks (lines ~67–71) — verification of mailto subject + addition of DownloadPdfLink
    - .planning/phases/05-about-+-resume-+-contact/05-RESEARCH.md § Pitfall 5 (lines ~568–576) — %20 vs + encoding rationale
    - .planning/phases/05-about-+-resume-+-contact/05-RESEARCH.md § Pitfall 7 (lines ~586–592) — footer stays RSC; DownloadPdfLink stays RSC
    - components/resume/download-pdf-link.tsx (Plan 05-02 output — verify component is exported and accepts className)
    - tests/site/footer.test.tsx (Wave 0 placeholder — replace its body)
    - .planning/phases/01-foundation/01-04-SUMMARY.md (Phase 1 footer composition — understand what existed BEFORE Phase 5 touches it, so we don't regress)
  </read_first>
  <behavior>
    URL corrections:
    - Test 1: GitHub icon link href === `https://github.com/olivelliott` (NOT 'ophelia-x' — CTC-01 + canonical handle per STATE.md)
    - Test 2: View-source link href === `https://github.com/olivelliott/portfolio` (NOT 'ophelia-x/portfolio')
    - Test 3: Email icon link href === `mailto:olivelliott48@gmail.com?subject=hi%20from%20olivelliott.dev` (CTC-02; Pitfall 5 — literal %20, NOT '+', NOT 'olivelliott.dev' plain)
    - Test 4: LinkedIn icon link href === `https://linkedin.com/in/olive-elliott` (still the Phase 1 placeholder — Phase 7 may confirm/update; Phase 5 leaves the PLACEHOLDER intact per RESEARCH § Open Q 2)

    DownloadPdfLink insertion:
    - Test 5: Footer contains a `<a href="/resume.pdf" download>` (assert by querying the rendered footer for this exact anchor — RES-05 second surface)
    - Test 6: The DownloadPdfLink anchor's textContent contains `'download.pdf'` AND `'↓'` (the literal glyph; Phase 5 anti-pattern: no icon library reach for this)
    - Test 7: There is a `<span aria-hidden="true">` with textContent `'·'` sibling to the DownloadPdfLink (the interpunct separator per RESEARCH § Example 4 + UI-SPEC § Component 11)
    - Test 8: DOM order in the right slot: icon-row UL → DownloadPdfLink → interpunct span → view-source anchor (assert by querying their relative positions)

    Phase 1 regression (do not break what existed):
    - Test 9: Footer renders a `<footer>` element with `border-t` class (Phase 1 baseline)
    - Test 10: Copyright text contains `'© 2026 Olive Elliott'` (Phase 1 baseline)
    - Test 11: Three icon links (GitHub, Email, LinkedIn) all still have their aria-labels (`'GitHub'`, `'Email Olive'`, `'LinkedIn'`)
    - Test 12: Each icon link still has `p-3` (the 44×44 touch target padding from Phase 1)
    - Test 13: View-source link text still says `'view source'` (mono lowercase from Phase 1 — text unchanged, only the URL flipped)

    Source-grep guards:
    - Test 14: `components/site/footer.tsx` does NOT contain `'use client'` (footer stays RSC; Pitfall 7 lock)
    - Test 15: `components/site/footer.tsx` does NOT contain the literal string `'ophelia-x'` anywhere (regression-proof against future paste-back)
    - Test 16: `components/site/footer.tsx` does NOT contain the literal `'subject=olivelliott.dev'` (the Phase 1 mailto subject — flipped to the Phase 5 string)
    - Test 17: `components/site/footer.tsx` contains the literal `'subject=hi%20from%20olivelliott.dev'` exactly once (positive lock for CTC-02; if a future edit drops or mangles the subject, this fails)
  </behavior>
  <action>
    Apply the four-part edit to `components/site/footer.tsx`:

    **Part 1 — Update three constants at the top of the file:**

    ```diff
    -const GITHUB_URL = 'https://github.com/ophelia-x'
    -const EMAIL_URL = 'mailto:olivelliott48@gmail.com?subject=olivelliott.dev'
    +const GITHUB_URL = 'https://github.com/olivelliott'
    +const EMAIL_URL = 'mailto:olivelliott48@gmail.com?subject=hi%20from%20olivelliott.dev'
     const LINKEDIN_URL = 'https://linkedin.com/in/olive-elliott'  // PLACEHOLDER: confirm with Olive — Phase 1 placeholder; resolve in Phase 7
    -const VIEW_SOURCE_URL = 'https://github.com/ophelia-x/portfolio'
    +const VIEW_SOURCE_URL = 'https://github.com/olivelliott/portfolio'
    ```

    **Part 2 — Add the DownloadPdfLink import at the top of the file (after the existing Lucide + brand-icons imports):**

    ```tsx
    import { DownloadPdfLink } from '@/components/resume/download-pdf-link'
    ```

    **Part 3 — Insert DownloadPdfLink + interpunct into the right slot, BEFORE the view-source link:**

    Existing right-slot JSX:
    ```tsx
    <div className="flex items-center gap-4 sm:gap-6">
      <ul className="flex items-center gap-2">{/* ...3 icon links... */}</ul>
      <a href={VIEW_SOURCE_URL} ...>view source</a>
    </div>
    ```

    Updated right-slot JSX:
    ```tsx
    <div className="flex items-center gap-4 sm:gap-6">
      <ul className="flex items-center gap-2">{/* ...3 icon links... */}</ul>
      <DownloadPdfLink />
      <span aria-hidden="true" className="text-[color:var(--color-text-tertiary)]">·</span>
      <a href={VIEW_SOURCE_URL} ...>view source</a>
    </div>
    ```

    The `aria-hidden="true"` is mandatory on the interpunct so screen readers don't announce "middle dot" between the download link and view-source (UI-SPEC § Accessibility Contract — decorative interpuncts are aria-hidden).

    **Part 4 — Update the JSDoc breadcrumb comment in the file header:**

    Replace the existing block:
    ```
    * Placeholder handles: GitHub/LinkedIn URLs and view-source repo use the
    * ophelia-x GitHub username per UI-SPEC acknowledged placeholders; confirm
    * actual handles before Phase 7 launch.
    ```

    With:
    ```
    * GitHub handle + view-source canonicalized to olivelliott (Phase 5; per
    * STATE.md Myco repo URL decision). LinkedIn handle remains a Phase 1
    * placeholder pending Olive's confirmation (RESEARCH § Open Q 2). Mailto
    * subject locked to "hi from olivelliott.dev" per CTC-02 (Pitfall 5 — %20
    * not +; RFC 6068). DownloadPdfLink + interpunct added per RES-05.
    ```

    **Then REPLACE the Wave 0 placeholder** in `tests/site/footer.test.tsx` with the 17 tests from `<behavior>`. Use `@testing-library/react > render` to mount `<Footer />` and query the DOM. Use `node:fs.readFileSync` for the source-grep tests (Tests 14–17), with the comment-stripping pattern (`/* */` then `//`) before identifier match (mirrors `tests/home/anti-patterns.test.ts` Phase 4 precedent).

    Commit message format: `feat(05-04): footer — canonical handle + CTC-02 subject + DownloadPdfLink + 17 tests`
  </action>
  <verify>
    <automated>pnpm vitest run tests/site/footer.test.tsx --reporter=dot &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `components/site/footer.tsx` GitHub URL is `https://github.com/olivelliott`
    - [ ] `components/site/footer.tsx` view-source URL is `https://github.com/olivelliott/portfolio`
    - [ ] `components/site/footer.tsx` email URL has subject `hi%20from%20olivelliott.dev` (literal %20)
    - [ ] No occurrence of `'ophelia-x'` anywhere in `components/site/footer.tsx`
    - [ ] No occurrence of `'subject=olivelliott.dev'` (the Phase 1 subject) anywhere in `components/site/footer.tsx`
    - [ ] `DownloadPdfLink` imported from `@/components/resume/download-pdf-link`
    - [ ] DownloadPdfLink rendered in the footer right slot, between the icon row and the view-source link
    - [ ] Interpunct `·` `<span aria-hidden="true">` sits between DownloadPdfLink and view-source
    - [ ] All 17 tests pass
    - [ ] No `'use client'` in components/site/footer.tsx (footer stays RSC)
    - [ ] `pnpm typecheck` exits 0
    - [ ] Phase 1 footer features (copyright text, icon row, aria-labels, 44×44 touch targets, mobile stack) all preserved — verified by Tests 9–13
  </acceptance_criteria>
  <done>Footer is updated; mailto subject locked; DownloadPdfLink present on every (site)/ route; canonical github handle propagates everywhere; CTC-01 + CTC-02 + RES-05 (second surface) all satisfied with regression locks in place.</done>
</task>

</tasks>

<verification>
After all tasks:

```bash
# Footer test green; Wave 0 skip now implemented
pnpm vitest run tests/site/footer.test.tsx

# Full suite stable
pnpm vitest run

# Typecheck clean
pnpm typecheck

# Manual: load any page, footer shows the new link + canonical handle
pnpm dev
# Open http://localhost:3000/about
# Footer right slot order should be: 3 icons → download.pdf ↓ → · → view source
# Click GitHub icon: should go to https://github.com/olivelliott (NOT ophelia-x)
# Click Email icon: should open mailto with subject "hi from olivelliott.dev"
# Click "download.pdf ↓": should download /resume.pdf (Wave 0 stub for now — Wave 4 replaces with real)

# Regression: source contains the locked strings
grep -c 'olivelliott' components/site/footer.tsx
# expect: ≥ 4 (github, email user, view-source, linkedin path)
! grep -c 'ophelia-x' components/site/footer.tsx
# expect: exit 1 (no matches)
grep -c 'subject=hi%20from%20olivelliott.dev' components/site/footer.tsx
# expect: 1
```
</verification>

<success_criteria>
- [ ] Footer GitHub URL is canonical https://github.com/olivelliott (CTC-01)
- [ ] Footer view-source URL is canonical https://github.com/olivelliott/portfolio
- [ ] Footer mailto subject is hi%20from%20olivelliott.dev (CTC-02 — Pitfall 5)
- [ ] Footer renders DownloadPdfLink (RES-05 second surface)
- [ ] Interpunct separator with aria-hidden="true" between DownloadPdfLink and view-source
- [ ] Footer remains RSC (no `'use client'`)
- [ ] Phase 1 footer features intact (copyright, 3 icons, aria-labels, 44×44 touch targets, mobile stack)
- [ ] 17 tests in tests/site/footer.test.tsx passing
- [ ] No 'ophelia-x' string anywhere in components/site/footer.tsx (regression-proof)
- [ ] pnpm typecheck exits 0
- [ ] Full vitest suite stays green
</success_criteria>

<output>
After completion, create `.planning/phases/05-about-+-resume-+-contact/05-04-SUMMARY.md` covering:
- Confirm the three-line constant diff applied exactly as RESEARCH § Example 4 specified
- Confirm Pitfall 5 lock — Test 17 asserts the literal %20-encoded subject string exists in source
- Note that the LinkedIn URL is still the Phase 1 placeholder — flag for Phase 7 alongside ContactStack's LinkedIn (Plan 05-03 has the same placeholder)
- Note that the Wave 4 (Plan 05-05) anti-pattern manifest extension should INCLUDE components/site/footer.tsx in PHASE_SOURCES (it's already touched by Phase 5 and benefits from the same banned-words / no-motion / no-Lucide-beyond-existing-Mail-icon locks)
- Note CTC-03 is now satisfied: ContactStack on /about (Plan 05-03) + Footer icons on every page = two contact surfaces
</output>
