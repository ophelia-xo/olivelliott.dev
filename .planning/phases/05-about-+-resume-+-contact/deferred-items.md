# Phase 05 — Deferred Items

Discoveries that landed during execution but were out-of-scope for the
current plan. Each item names the plan that discovered it.

## Untracked files discovered during Plan 05-01

- `Olive_Elliott_Resume.docx` — repo-root source for `content/resume.ts`. Pre-extracted into RESEARCH so the .docx itself need not be tracked. Decision deferred: keep local-only or add to git for provenance?
- `scripts/generate-og-default.ts` — pre-existing Phase 3 script that was never committed (per Phase 3 DEFER-02 note). Out of scope for Phase 5; Phase 7 should handle.
