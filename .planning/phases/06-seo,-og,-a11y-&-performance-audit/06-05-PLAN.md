---
phase: 06-seo,-og,-a11y-&-performance-audit
plan: 05
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - public/file.svg
  - public/globe.svg
  - public/next.svg
  - public/vercel.svg
  - public/window.svg
  - tests/home/anti-patterns.test.ts
  - .planning/phases/06-seo,-og,-a11y-&-performance-audit/06-VALIDATION.md
autonomous: true
requirements: [QAL-01, QAL-02, QAL-03, QAL-04, QAL-05, MTA-01, MTA-02, MTA-03, MTA-04]
must_haves:
  truths:
    - "5 orphaned create-next-app scaffolder SVGs deleted from public/ (file, globe, next, vercel, window)"
    - "PHASE_SOURCES manifest extended with Phase 6 source files (sitemap.ts, robots.ts, og-template.tsx, opengraph-image.tsx surfaces)"
    - "`pnpm build` exits 0 end-to-end (Phase 6 files compile + Phase 5 postbuild PDF generates)"
    - "Full vitest suite passes — zero skipped from Phase 6 placeholders, anti-pattern net green"
    - "06-VALIDATION.md frontmatter flipped: status: complete, nyquist_compliant: true, wave_0_complete: true, completed: 2026-05-17"
  artifacts:
    - path: .planning/phases/06-seo,-og,-a11y-&-performance-audit/06-VALIDATION.md
      provides: "Final phase-gate sign-off"
      contains: "nyquist_compliant: true"
    - path: tests/home/anti-patterns.test.ts
      provides: "PHASE_SOURCES extended with Phase 6 source files"
      contains: "Phase 6"
  key_links:
    - from: tests/home/anti-patterns.test.ts PHASE_SOURCES
      to: Phase 6 source files (sitemap, robots, og-template, opengraph-image.tsx surfaces)
      via: "manifest append; invariants stay constant"
      pattern: "app/sitemap.ts|app/robots.ts|lib/og-template.tsx|opengraph-image"
---

<objective>
Phase 6 cleanup + phase-gate sign-off:
1. Delete the 5 orphaned create-next-app scaffolder SVGs from `public/` (file, globe, next, vercel, window) — surfaced by RESEARCH § Runtime State Inventory.
2. Extend `tests/home/anti-patterns.test.ts` PHASE_SOURCES manifest with the Phase 6 source files (sitemap.ts, robots.ts, lib/og-template.tsx, the 5 opengraph-image.tsx surfaces) so future regressions trip the existing 11 invariants.
3. Run `pnpm build` end-to-end to prove all Phase 6 files compile, Phase 5 postbuild PDF still generates, and OG static optimization picks up the new sibling files.
4. Flip `06-VALIDATION.md` frontmatter to `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`, `completed: 2026-05-17`.

Purpose: Phase 6 is the launch gate. This plan is the final pass — orphaned scaffolder leftovers gone, regression net extended to cover the new surfaces, build smoke-gated, validation sign-off attests Nyquist compliance. After this plan, Phase 7 (content pass + launch) is unblocked.

Output: 5 file deletions + 2 file edits (manifest + validation), with `pnpm build` proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-seo,-og,-a11y-&-performance-audit/06-CONTEXT.md
@.planning/phases/06-seo,-og,-a11y-&-performance-audit/06-RESEARCH.md
@.planning/phases/06-seo,-og,-a11y-&-performance-audit/06-VALIDATION.md
@tests/home/anti-patterns.test.ts
@.planning/phases/05-about-+-resume-+-contact/05-05-SUMMARY.md

<interfaces>
<!-- Orphaned SVGs identified by RESEARCH § Runtime State Inventory: -->
<!--   public/file.svg, public/globe.svg, public/next.svg, public/vercel.svg, public/window.svg -->
<!-- Verified by RESEARCH: "no source-grep matches in the repo" — safe to delete. -->
<!-- Before deletion: grep -r "file\.svg\|globe\.svg\|next\.svg\|vercel\.svg\|window\.svg" app/ components/ -->
<!--   If any matches surface, document them in the SUMMARY's Issues Encountered and STOP — do not delete. -->

<!-- PHASE_SOURCES extension precedent: Plan 05-05 SUMMARY § "PHASE_SOURCES (was PHASE4_SOURCES): -->
<!--   single cross-phase anti-pattern manifest in tests/home/anti-patterns.test.ts. Future phases -->
<!--   extend by appending entries; invariants stay constant." -->

<!-- Build smoke: pnpm build → next build → postbuild (tsx scripts/build-resume-pdf.ts → real PDF). -->
<!-- Phase 5 SUMMARY documents the build pipeline. Phase 6 adds OG ImageResponse static optimization -->
<!-- (next/og images compile to static-optimized route handlers at build time) + sitemap/robots route handlers. -->

<!-- 06-VALIDATION.md current frontmatter (Wave 0 state): -->
<!--   status: draft, nyquist_compliant: false, wave_0_complete: false -->
<!-- Final state after this plan: -->
<!--   status: complete, nyquist_compliant: true, wave_0_complete: true, completed: 2026-05-17 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend PHASE_SOURCES manifest in tests/home/anti-patterns.test.ts with Phase 6 source files</name>
  <files>tests/home/anti-patterns.test.ts</files>
  <read_first>
    - tests/home/anti-patterns.test.ts (current PHASE_SOURCES manifest — 24 entries spanning Phase 4 + Phase 5)
    - .planning/phases/05-about-+-resume-+-contact/05-05-SUMMARY.md § "PHASE_SOURCES (was PHASE4_SOURCES)" pattern
    - All Phase 6 new source files: app/sitemap.ts, app/robots.ts, lib/og-template.tsx, app/opengraph-image.tsx, app/(site)/about/opengraph-image.tsx, app/(site)/projects/opengraph-image.tsx, app/resume/opengraph-image.tsx, app/(site)/projects/[slug]/opengraph-image.tsx
  </read_first>
  <action>
    The PHASE_SOURCES manifest currently has 24 entries across Phase 4 + Phase 5. Phase 6 adds 8 new source files that should join the manifest so the 11 invariants (Tests 1-11) extend automatically:

    1. **Append a Phase 6 section** after the existing Phase 5 entries in the `PHASE_SOURCES` array. Add a comment header `// ── Phase 6: SEO + OG + A11y audit surfaces ──`. New entries:

       ```ts
       // ── Phase 6: SEO + OG + A11y audit surfaces ─────────────────
       'app/sitemap.ts',
       'app/robots.ts',
       'lib/og-template.tsx',
       'app/opengraph-image.tsx',
       'app/(site)/about/opengraph-image.tsx',
       'app/(site)/projects/opengraph-image.tsx',
       'app/resume/opengraph-image.tsx',
       'app/(site)/projects/[slug]/opengraph-image.tsx',
       ```

    2. **Update the comment block at the top of `PHASE_SOURCES`** to reflect the Phase 6 extension. Change:
       ```
       // Layout:
       //   - 11 Phase 4 entries (home composer surfaces, project cards, filter UI)
       //   - 13 Phase 5 entries (about route + components, resume route + components,
       //     footer (Phase 1 baseline touched by Plan 05-04), resume content module)
       ```
       to:
       ```
       // Layout:
       //   - 11 Phase 4 entries (home composer surfaces, project cards, filter UI)
       //   - 13 Phase 5 entries (about route + components, resume route + components,
       //     footer (Phase 1 baseline touched by Plan 05-04), resume content module)
       //   -  8 Phase 6 entries (sitemap, robots, og-template + 5 opengraph-image.tsx surfaces)
       ```

    3. **Update the describe block title** (Plan 06-03 changed it from "Phase 4 + 5" to "Phase 4 + 5 + 6"; verify it's still in that form, leave as-is).

    4. **Test 6 (banned-words) extension consideration:** the Plan 06-03 Test 11 added the QAL-04 motion lock. Test 6 (banned-words) currently uses the Phase 4 banned-words pattern. Phase 6 adds OG-card banned phrases per UI-SPEC § Banned words extension. The Test 6 regex SHOULD be extended to include the OG additions: `introducing, presenting, the all-new, transform your, supercharge, next-generation, AI-powered, built with love`.

       **However** — these OG-card additions are ALSO codified in `tests/launch-gate/anti-features.test.ts` Test #8 (Plan 06-04). To avoid duplication, leave Test 6 (anti-patterns.test.ts) as-is and let the launch-gate test catch the OG-card banned words. Document this decision inline:

       ```ts
       // Test 6 banned-words list stays Phase 4 + 5 scope; the OG-card additions
       // (introducing, presenting, the all-new, transform your, supercharge,
       // next-generation, AI-powered, built with love) are codified by
       // tests/launch-gate/anti-features.test.ts Test #8 to avoid duplication.
       ```
       Add this comment ABOVE the `it('Test 6 — banned-word lock ...')` declaration.

    5. **Verify all new manifest entries exist on disk** before running the test. If any are missing, the readAll() helper's `expect(existsSync(abs)).toBe(true)` assertion will fail loudly with the missing path.

    The 11 source-grep invariants automatically extend to the 8 new files. Specifically meaningful:
    - **Test 3** (single 'use client' — only thesis-paragraph.tsx) — verifies no Phase 6 OG file accidentally adds a client directive (none should; OG images are RSC).
    - **Test 5** (no `motion.*` literal — LazyMotion strict) — Phase 6 ships no motion; passes vacuously.
    - **Test 6** (banned words) — should pass; UI-SPEC § Copywriting Contract locked all OG strings against banned words.
    - **Test 11** (motion/react importer must call useReducedMotion) — Phase 6 imports NO motion; passes vacuously.
  </action>
  <verify>
    <automated>grep -c "Phase 6: SEO" tests/home/anti-patterns.test.ts &amp;&amp; grep -q "'app/sitemap.ts'" tests/home/anti-patterns.test.ts &amp;&amp; grep -q "'app/robots.ts'" tests/home/anti-patterns.test.ts &amp;&amp; grep -q "'lib/og-template.tsx'" tests/home/anti-patterns.test.ts &amp;&amp; grep -q "'app/opengraph-image.tsx'" tests/home/anti-patterns.test.ts &amp;&amp; pnpm vitest run tests/home/anti-patterns.test.ts --reporter=verbose 2>&amp;1 | tee /tmp/p6-extend.txt &amp;&amp; ! grep -E "(FAIL|skipped)" /tmp/p6-extend.txt &amp;&amp; grep -E "Tests +11 passed" /tmp/p6-extend.txt</automated>
  </verify>
  <done>
    PHASE_SOURCES manifest extended by 8 Phase 6 entries (now 32 total). Comment block reflects Phase 6 count. All 11 invariants still pass — Phase 6 source files are clean (no `'use client'`, no motion.*, no banned words, no motion/react imports). Test 6 inline comment documents the deduplication with the launch-gate test.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete orphaned create-next-app scaffolder SVGs from public/</name>
  <files>public/file.svg, public/globe.svg, public/next.svg, public/vercel.svg, public/window.svg</files>
  <read_first>
    - .planning/phases/06-seo,-og,-a11y-&-performance-audit/06-RESEARCH.md § Runtime State Inventory (build artifacts row 2 — orphaned `public/{file,globe,next,vercel,window}.svg`)
    - public/ directory listing (confirm all 5 SVGs still exist)
  </read_first>
  <action>
    1. **Safety check — grep the codebase for any references to these SVGs before deletion:**
       ```bash
       grep -rE "(file|globe|next|vercel|window)\.svg" app/ components/ content/ lib/ tests/
       ```
       Expected: zero matches (RESEARCH confirmed at audit time). If ANY match surfaces, STOP and document the reference in the SUMMARY's Issues Encountered — do not delete the referenced file. Audit any false-positive (e.g., a JSDoc mention) by reading the matched line.

    2. **Delete the 5 SVGs:**
       ```bash
       rm public/file.svg public/globe.svg public/next.svg public/vercel.svg public/window.svg
       ```

    3. **Verify deletions:**
       ```bash
       ls public/ | grep -E "^(file|globe|next|vercel|window)\.svg$"
       # Expected: zero output (all deleted)
       ls public/
       # Expected: og-default.png, resume.pdf (these stay — Phase 3 + 5 assets)
       ```

    4. **Re-run the full test suite** to confirm no Phase 4/5/6 test references the deleted SVGs:
       ```bash
       pnpm vitest run --reporter=dot
       ```
       Expected: full suite green.

    Document in the SUMMARY: bytes recovered (each SVG ~1 KB; ~5 KB total). The deletion shrinks the deployed `public/` asset set so visitors don't see stale scaffolder assets at e.g. `https://olivelliott.dev/next.svg`.
  </action>
  <verify>
    <automated>! test -e public/file.svg &amp;&amp; ! test -e public/globe.svg &amp;&amp; ! test -e public/next.svg &amp;&amp; ! test -e public/vercel.svg &amp;&amp; ! test -e public/window.svg &amp;&amp; test -f public/og-default.png &amp;&amp; test -f public/resume.pdf &amp;&amp; ! grep -rE "(file|globe|next|vercel|window)\.svg" app/ components/ content/ lib/ tests/ 2>/dev/null &amp;&amp; pnpm vitest run --reporter=dot 2>&amp;1 | tail -20 | grep -E "Tests +[0-9]+ passed"</automated>
  </verify>
  <done>
    5 orphaned SVGs deleted; public/og-default.png + public/resume.pdf preserved; no source-grep references to the deleted files remain anywhere in app/components/content/lib/tests; full test suite green after deletions.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: pnpm build smoke + flip 06-VALIDATION.md frontmatter to phase-gate sign-off</name>
  <files>.planning/phases/06-seo,-og,-a11y-&-performance-audit/06-VALIDATION.md</files>
  <read_first>
    - .planning/phases/06-seo,-og,-a11y-&-performance-audit/06-VALIDATION.md (current frontmatter — status: draft, nyquist_compliant: false, wave_0_complete: false)
    - .planning/phases/05-about-+-resume-+-contact/05-VALIDATION.md (Phase 5 sign-off shape for reference)
    - Plan 06-04 outputs: lighthouse-report.md (must already be filled with real ≥ 90 scores before this task runs)
  </read_first>
  <action>
    1. **End-to-end build smoke:** run `pnpm build` from the repo root. Expected behavior:
       - Next 16 compiles all Phase 6 new source files (sitemap.ts, robots.ts, OG images, favicons auto-wired)
       - OG ImageResponse routes static-optimized at build time (look for `○ /opengraph-image` and similar in the build log)
       - sitemap + robots route handlers registered (`○ /sitemap.xml`, `○ /robots.txt`)
       - Phase 5 postbuild hook fires (`tsx scripts/build-resume-pdf.ts`) — Puppeteer spawns next start on port 3057, generates /public/resume.pdf, tears down
       - Build exits 0

       If the build fails:
       - Read the error message carefully
       - If it's an OG-image-related failure (Pitfall 1: display:grid, Pitfall 2: edge runtime, Pitfall 3: 500KB cap), surface it and fix in a same-task tweak
       - If it's a Phase 5 postbuild failure (resume.pdf) — that's NOT a Phase 6 regression; document but don't block the gate
       - If it's an OG static-optimization failure (Satori font load failure), check the geist font paths in lib/og-template.tsx exist on disk

    2. **Capture the build log fingerprint** for the SUMMARY: route table from the Next build output (e.g., "Route (app) Size First Load JS ..."), confirming `/opengraph-image`, `/sitemap.xml`, `/robots.txt` all appear.

    3. **Flip 06-VALIDATION.md frontmatter.** Current state (Wave 0):
       ```yaml
       ---
       phase: 6
       slug: seo-og-a11y-performance-audit
       status: draft
       nyquist_compliant: false
       wave_0_complete: false
       created: 2026-05-17
       ---
       ```
       Target state (phase-gate complete):
       ```yaml
       ---
       phase: 6
       slug: seo-og-a11y-performance-audit
       status: complete
       nyquist_compliant: true
       wave_0_complete: true
       created: 2026-05-17
       completed: 2026-05-17
       ---
       ```

    4. **Update the Approval line at the bottom of 06-VALIDATION.md** from `**Approval:** pending` to:
       ```markdown
       **Approval:** ✅ Phase 6 complete (2026-05-17). All 9 requirements met (MTA-01..04 + QAL-01..05).
       - Full test suite: green across {N} tests
       - Lighthouse: ≥ 90 on Performance / A11y / Best Practices / SEO for / and /projects/myco (see lighthouse-report.md)
       - 19-item anti-features launch gate: 19/19 passing
       - vitest-axe a11y: 0 violations on / + /projects + /projects/myco + /about + /resume
       - PHASE_SOURCES anti-pattern net: 32 files × 11 invariants
       - pnpm build end-to-end: success (Phase 5 postbuild PDF still generating)
       ```
       (Replace `{N}` with the actual passing test count from the final `pnpm vitest run` output.)

    5. **Commit message for git_commit step** should reference all 9 requirements:
       ```
       docs(06): phase-gate sign-off + cleanup (MTA-01..04 + QAL-01..05)
       ```

    **Phase 6 closes here.** Phase 7 (content pass + launch) is now unblocked.
  </action>
  <verify>
    <automated>pnpm build 2>&amp;1 | tee /tmp/p6-build.txt &amp;&amp; grep -qE "(Compiled successfully|Build completed)" /tmp/p6-build.txt &amp;&amp; grep -q "status: complete" ".planning/phases/06-seo,-og,-a11y-&amp;-performance-audit/06-VALIDATION.md" &amp;&amp; grep -q "nyquist_compliant: true" ".planning/phases/06-seo,-og,-a11y-&amp;-performance-audit/06-VALIDATION.md" &amp;&amp; grep -q "wave_0_complete: true" ".planning/phases/06-seo,-og,-a11y-&amp;-performance-audit/06-VALIDATION.md" &amp;&amp; grep -q "completed: 2026-05-17" ".planning/phases/06-seo,-og,-a11y-&amp;-performance-audit/06-VALIDATION.md" &amp;&amp; pnpm vitest run --reporter=dot 2>&amp;1 | tail -10 | grep -E "Tests +[0-9]+ passed"</automated>
  </verify>
  <done>
    `pnpm build` exits 0 with all Phase 6 routes present in the route table. 06-VALIDATION.md frontmatter is `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`, `completed: 2026-05-17`. Approval line documents the 9 requirements + supporting evidence. Full test suite green.
  </done>
</task>

</tasks>

<verification>
- `ls public/` shows only `og-default.png` and `resume.pdf` (the 5 scaffolder SVGs are gone)
- `tests/home/anti-patterns.test.ts` PHASE_SOURCES has 32 entries (was 24); all 11 invariants pass
- `pnpm vitest run` reports all Phase 6 tests passing, all Phase 6 placeholders consumed (zero Wave 0 skipped from Phase 6 paths)
- `pnpm build` exits 0; build log shows /opengraph-image, /sitemap.xml, /robots.txt route entries; postbuild PDF generates
- `pnpm typecheck` exits 0
- 06-VALIDATION.md frontmatter: status: complete, nyquist_compliant: true, wave_0_complete: true, completed: 2026-05-17
- Approval line documents all 9 Phase 6 requirements (MTA-01..04 + QAL-01..05)
</verification>

<success_criteria>
Phase 6 fully complete and phase-gate-ready. All 9 requirements validated:
- MTA-01: Per-route metadata audit (Plan 06-02 tests/seo/metadata.test.ts)
- MTA-02: OG images per route (Plan 06-02 — 6 OG files)
- MTA-03: sitemap + robots (Plan 06-02 — sitemap.ts + robots.ts)
- MTA-04: Favicon set (Plan 06-01 — icon.svg + apple-icon.png + favicon.ico OVERWRITE)
- QAL-01: Lighthouse ≥ 90 on /  + /projects/myco (Plan 06-04 manual run + lighthouse-report.md)
- QAL-02: vitest-axe clean on 5 routes (Plan 06-03)
- QAL-03: keyboard reachability (Plan 06-03 keyboard.test.tsx)
- QAL-04: reduced-motion source-grep (Plan 06-03 Test 11 in anti-patterns.test.ts)
- QAL-05: 19-item anti-features launch gate (Plan 06-04 tests/launch-gate/anti-features.test.ts)

Phase 7 (content pass + launch) is unblocked. The launch-gate test set + Lighthouse scores + axe-clean a11y comprise the "ready to ship" gate before Vercel push.
</success_criteria>

<output>
After completion, create `.planning/phases/06-seo,-og,-a11y-&-performance-audit/06-05-SUMMARY.md` documenting:
- 5 orphaned SVG deletions (bytes recovered)
- PHASE_SOURCES manifest expansion (24 → 32 entries)
- pnpm build outcome (route table fingerprint)
- 06-VALIDATION.md flip diff (before/after frontmatter)
- The full Phase 6 cumulative metrics (plans executed, tests added, files created, files modified, deferred items if any)
- Phase 7 readiness note
</output>
