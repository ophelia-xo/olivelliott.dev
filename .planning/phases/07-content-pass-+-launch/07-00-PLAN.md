---
phase: 07-content-pass-+-launch
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - app/layout.tsx
  - .planning/phases/07-content-pass-+-launch/deploy-checklist.md
autonomous: true
requirements: [DPL-03]
must_haves:
  truths:
    - "Vercel Analytics + Speed Insights client islands mount on every route (including /resume) via root app/layout.tsx"
    - "The infrastructure prerequisites for the entire phase (engines pin, redaction scanner armed, Phase 7 phase directory ready) are explicitly verified"
    - "A deploy-checklist.md scaffold exists with all 7 manual smoke steps pre-listed so Plan 07-04 has zero ramp-up"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Mounts <Analytics /> + <SpeedInsights /> inside <body> after <Providers>{children}</Providers>"
      contains: "import { Analytics }"
    - path: ".planning/phases/07-content-pass-+-launch/deploy-checklist.md"
      provides: "Pre-deploy + deploy + post-deploy smoke checklist with 7 verification steps"
      min_lines: 60
  key_links:
    - from: "app/layout.tsx"
      to: "@vercel/analytics/react + @vercel/speed-insights/next"
      via: "client-island import + JSX mount inside <body>"
      pattern: "<Analytics\\s*/>"
    - from: ".planning/phases/07-content-pass-+-launch/deploy-checklist.md"
      to: "Plan 07-04 (consumer — manual checkpoint actor follows it line by line)"
      via: "markdown checklist with [ ] boxes per smoke step"
      pattern: "post-deploy smoke"
---

<objective>
Wire Vercel Analytics + Speed Insights into the root layout (DPL-03), scaffold the deploy-checklist.md that Plan 07-04 will execute against, and verify the Phase 7 prerequisites (engines pin, redaction scanner already armed, deps already installed). This is the foundation that lets the content + deploy waves proceed without infrastructure surprises.

Purpose: One-shot Wave 0 prep — analytics shouldn't be deferred behind content authoring (DPL-03 is a Phase 7 requirement; mount early so dev/preview also start firing telemetry).
Output: app/layout.tsx updated, deploy-checklist.md created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-content-pass-+-launch/07-CONTEXT.md
@.planning/phases/07-content-pass-+-launch/07-VALIDATION.md
@app/layout.tsx
@package.json
@tests/fixtures/banned-terms.ts
@tests/content/redaction.test.ts

<interfaces>
<!-- Vercel Analytics + Speed Insights are client components (under the hood). -->
<!-- They are safe to import from a Server Component layout — Next.js handles -->
<!-- the boundary. Both packages are already installed (package.json:25-26). -->

From @vercel/analytics/react:
```ts
export function Analytics(props?: { mode?: 'production' | 'development' | 'auto'; debug?: boolean }): JSX.Element;
```

From @vercel/speed-insights/next:
```ts
export function SpeedInsights(props?: { route?: string; sampleRate?: number; debug?: boolean }): JSX.Element;
```

Current app/layout.tsx structure (DO NOT BREAK):
- `<html lang="en" className=…>` (Geist Sans + Mono variables)
- `<body style={{…}} className="font-sans antialiased">`
  - `<Providers>{children}</Providers>` ← MotionProvider + next-themes live in here
- (NEW) `<Analytics />` ← sibling of Providers, inside <body>
- (NEW) `<SpeedInsights />` ← sibling of Providers, inside <body>

The two analytics components MUST be siblings of `<Providers>{children}</Providers>` (not children) so they don't accidentally end up under the (site)/ chrome — they must fire on /resume too (which opts out of the (site) chrome but inherits root layout).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount Analytics + SpeedInsights in root app/layout.tsx</name>
  <files>app/layout.tsx</files>
  <read_first>
    - app/layout.tsx (current shape)
    - package.json (confirm @vercel/analytics ^2.0.1 and @vercel/speed-insights ^2.0.0 in dependencies)
    - .planning/phases/07-content-pass-+-launch/07-CONTEXT.md § "Analytics & Post-Deploy Verification" (D-DPL-03 decision: root layout, NOT site layout)
  </read_first>
  <action>
    Per Phase 7 CONTEXT.md "Analytics & Post-Deploy Verification" decision (DPL-03 — root layout chosen so /resume gets analytics too):

    1. Open app/layout.tsx.
    2. Add two imports at the top (after the existing Geist imports):
       ```ts
       import { Analytics } from '@vercel/analytics/react'
       import { SpeedInsights } from '@vercel/speed-insights/next'
       ```
    3. Inside `<body>`, AFTER `<Providers>{children}</Providers>`, add:
       ```tsx
       <Analytics />
       <SpeedInsights />
       ```
    4. Do NOT add `'use client'` to layout.tsx. Both packages handle the client boundary internally.
    5. Do NOT change the existing metadata export, html attributes, font variable wiring, body styles, or Providers placement. Surgery only.
    6. Run `pnpm typecheck` — must exit 0.
    7. Run `pnpm vitest run` — must stay green at 513+ tests (no regressions).
    8. Run `pnpm build` — must complete; static/server pages still generated.

    Why root layout (not (site)/layout.tsx): /resume opts out of the (site)/ chrome but still inherits the root layout. Analytics must fire there too (visitors who land on a shared resume URL still count). Per Plan 05-02 SUMMARY, app/resume/layout.tsx uses `<>{children}</>` for chromeless — analytics in root catches it.

    Why both Analytics + SpeedInsights: DPL-03 requires Analytics; Speed Insights is the perf-monitoring counterpart pre-locked in CLAUDE.md tech stack ("@vercel/speed-insights ... Privacy-respecting analytics + Web Vitals"). Both ship together at no marginal complexity.
  </action>
  <verify>
    <automated>pnpm vitest run --reporter=dot &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx imports Analytics from '@vercel/analytics/react' AND SpeedInsights from '@vercel/speed-insights/next'
    - Both components rendered as siblings of <Providers> inside <body>
    - No 'use client' directive added to app/layout.tsx
    - `pnpm typecheck` exits 0
    - `pnpm vitest run` exits 0 with no regressions (≥513 tests pass, matching Phase 6 baseline)
    - `pnpm build` exits 0; .next/server/app/layout.js exists
  </acceptance_criteria>
  <done>
    Root layout mounts both analytics components; build + typecheck + tests green; DPL-03 partially satisfied (deploy in Plan 07-04 closes it).
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Phase 7 infrastructure prerequisites + author deploy-checklist.md scaffold</name>
  <files>
    .planning/phases/07-content-pass-+-launch/deploy-checklist.md
  </files>
  <read_first>
    - package.json (confirm engines.node pin + scripts: build, start, lhci, postbuild)
    - tests/fixtures/banned-terms.ts (confirm Object.freeze + 3 entries: aktiga, voya, spectra)
    - tests/content/redaction.test.ts (confirm dynamic-describe pattern, scans content/projects/*.mdx)
    - .planning/phases/02-content-pipeline/02-REDACTION-REVIEW.md (template for forked per-project sign-offs in Plan 07-02)
    - .planning/phases/06-seo,-og,-a11y-&-performance-audit/lighthouse-report.md (lhci instructions Plan 07-04 will follow)
    - .planning/phases/07-content-pass-+-launch/07-CONTEXT.md § "Vercel Deploy Process" + § "Analytics & Post-Deploy Verification"
  </read_first>
  <action>
    Two parts: (a) verify pre-existing infrastructure is ready, (b) create the deploy-checklist.md scaffold.

    (a) Verification (no code changes; report findings in commit message):
    - Confirm `engines.node` exists in package.json (current: `">=20.18.0"`). Vercel needs this for build env selection. If absent, fail with explanation; do NOT modify package.json — surface for Olive's call (it's already present, this is belt-and-suspenders).
    - Confirm `tests/fixtures/banned-terms.ts` exists and exports `BANNED_TERMS` as a frozen array (Plan 07-02 will extend this in place — verify the contract before relying on it).
    - Confirm `tests/content/redaction.test.ts` exists with the dynamic-describe pattern (Plan 07-02's new private MDX will auto-fire against it).
    - Confirm `@vercel/analytics` and `@vercel/speed-insights` are in package.json dependencies (NOT devDependencies — they ship to client).
    - Confirm `pnpm vitest run` baseline (no regression from Task 1).

    (b) Author `.planning/phases/07-content-pass-+-launch/deploy-checklist.md`. Use the exact structure below — Plan 07-04's checkpoint:human-action tasks reference this file by section heading:

    ```markdown
    # Phase 7 — Deploy Checklist

    **Owner:** Olive Elliott
    **Target URL:** `https://olivelliott.vercel.app` (Vercel-default subdomain; custom domain deferred per PROJECT.md)
    **First-run date:** _pending — Plan 07-04 execution_

    Single source of truth for the launch sequence. Each box becomes a fact when Olive ticks it during Plan 07-04.

    ---

    ## Pre-Deploy Gate (run locally before pushing to main)

    ### 1. Test suite green
    - [ ] `pnpm vitest run` → all tests pass (target: 513+ baseline carried through Phase 7 additions)
    - [ ] `pnpm typecheck` → exit 0
    - [ ] `pnpm build` → exit 0; `.next/server/app/projects/{myco,fathom,agenda-keeper}.html` all exist; resume.pdf regenerated by postbuild

    ### 2. Lighthouse audit (resolves Phase 6 QAL-01 deferral)
    - [ ] `pnpm lhci` runs cleanly against `/` and `/projects/myco`
    - [ ] All 8 cells in `.planning/phases/06-seo,-og,-a11y-&-performance-audit/lighthouse-report.md` ≥ 0.90
    - [ ] If any axis < 0.90: file a gap-closure micro-plan (likely surfaces: hero image priority/sizes, font-display, motion island bundle size) and re-run before deploy
    - [ ] Score table in `lighthouse-report.md` filled in with actuals; Status flipped from DEFERRED to ✓ PASSED

    ### 3. Content sanity
    - [ ] `/projects/myco`, `/projects/fathom`, `/projects/agenda-keeper` all render the Problem / Approach / Outcome H2 anchors
    - [ ] No "lorem ipsum", no visible "PLACEHOLDER" strings in rendered HTML on any public route (`pnpm build && grep -ri "PLACEHOLDER" .next/server/app/ | grep -v '\\.map'` should be empty for visible content)
    - [ ] Redaction sign-off completed for every `visibility: private` MDX (per `.planning/phases/02-content-pipeline/02-REDACTION-REVIEW.md` sign-off template)

    ### 4. OG card unfurl (local sanity check)
    - [ ] `pnpm build && pnpm start` → visit `http://localhost:3000/opengraph-image` (and 4 per-route variants); confirm each returns a 1200×630 PNG
    - [ ] Paste 3 representative URLs (/, /projects/myco, /about) into Twitter compose; preview shows unique OG image + title + description

    ---

    ## Deploy

    ### 5. Vercel project setup (one-time)
    - [ ] Vercel dashboard → New Project → import GitHub repo `olivelliott/portfolio`
    - [ ] Build command: `pnpm build` (default; honors postbuild)
    - [ ] Output directory: `.next` (default)
    - [ ] Install command: `pnpm install` (default for pnpm projects)
    - [ ] Node version: 20.x (auto-detected from `engines.node` in package.json)
    - [ ] Production branch: `main`
    - [ ] No environment variables required for v1 (no secrets, no API keys)
    - [ ] Subdomain assigned: `olivelliott.vercel.app` (or Vercel-chosen if collision; record actual URL below)

    ### 6. First production deploy
    - [ ] `git push origin main` (auto-triggers Vercel build)
    - [ ] Vercel build log: green; build duration < 3 min target
    - [ ] Deployment URL: `_______________________________` (fill in actual)

    ---

    ## Post-Deploy Smoke (run on the live URL within 30 min of deploy)

    ### 7. Live-URL verification
    - [ ] `https://olivelliott.vercel.app/` — 200 OK, home page renders
    - [ ] `https://olivelliott.vercel.app/about` — 200 OK
    - [ ] `https://olivelliott.vercel.app/projects` — 200 OK; tag chips functional
    - [ ] `https://olivelliott.vercel.app/projects/myco` — 200 OK; Problem/Approach/Outcome render
    - [ ] `https://olivelliott.vercel.app/projects/fathom` — 200 OK; Problem/Approach/Outcome render
    - [ ] `https://olivelliott.vercel.app/projects/agenda-keeper` — 200 OK; "code private" label visible; no repo URL in source
    - [ ] `https://olivelliott.vercel.app/resume` — 200 OK; chromeless layout
    - [ ] `curl -I https://olivelliott.vercel.app/resume.pdf` → 200 OK; Content-Type: application/pdf; size ~240KB
    - [ ] `curl -I https://olivelliott.vercel.app/opengraph-image` → 200 OK; Content-Type: image/png
    - [ ] `https://olivelliott.vercel.app/sitemap.xml` → valid XML; lists all public routes
    - [ ] `https://olivelliott.vercel.app/robots.txt` → present
    - [ ] Favicon loads in browser tab (Chrome + Firefox + Safari quick check)

    ### 8. Analytics confirmation
    - [ ] Visit at least 3 routes on the live URL (creates pageview events)
    - [ ] Vercel dashboard → Analytics tab → confirm pageviews appear within 30 min
    - [ ] Vercel dashboard → Speed Insights tab → confirm Web Vitals data accumulating

    ### 9. Anti-features visual gate (PROJECT.md "high-touch, not template")
    - [ ] No skill bars, gradient blobs, glassmorphism, stagger-on-scroll, bento grid, cookie banner, newsletter signup, testimonials, marquee, "coming soon" copy, or auto-chat widget visible on any live route
    - [ ] Reference: `.planning/phases/06-seo,-og,-a11y-&-performance-audit/lighthouse-report.md § 19-Item Anti-Features Visual Check`

    ---

    ## Sign-off

    **Deploy approved by:** Olive Elliott
    **Date:** _________________
    **Final live URL:** _________________
    **Notes / known issues (if any):**

    _________________________________________________________________

    Once all sections above are ticked: DPL-02 + DPL-03 are satisfied; Phase 7 success criteria #1, #2, #5 are met; the milestone v1.0 verification can run.
    ```

    Final step: Run `pnpm typecheck` (no source touched, confirms Task 1 still clean) and verify the new markdown file exists.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-content-pass-+-launch/deploy-checklist.md &amp;&amp; grep -q "Pre-Deploy Gate" .planning/phases/07-content-pass-+-launch/deploy-checklist.md &amp;&amp; grep -q "Post-Deploy Smoke" .planning/phases/07-content-pass-+-launch/deploy-checklist.md &amp;&amp; grep -q "Analytics confirmation" .planning/phases/07-content-pass-+-launch/deploy-checklist.md &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-content-pass-+-launch/deploy-checklist.md` exists
    - File contains all three section headings: "Pre-Deploy Gate", "Deploy", "Post-Deploy Smoke"
    - File contains "Analytics confirmation" subsection (Plan 07-04 checkpoint references this name)
    - File contains numbered subsections 1–9 (Plan 07-04 references by number)
    - `engines.node` confirmed present in package.json (no change)
    - `tests/fixtures/banned-terms.ts` confirmed present + frozen (no change)
    - `tests/content/redaction.test.ts` confirmed present + dynamic-describe (no change)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    Infrastructure verified ready for Plans 07-01/02/03/04; deploy-checklist.md scaffold exists with all sections Plan 07-04 will execute against; commit message documents the verification findings.
  </done>
</task>

</tasks>

<verification>
- `pnpm vitest run` green (no regressions vs Phase 6 baseline)
- `pnpm typecheck` exit 0
- `pnpm build` exit 0
- `grep -c "Analytics" app/layout.tsx` ≥ 2 (import + JSX)
- `grep -c "SpeedInsights" app/layout.tsx` ≥ 2 (import + JSX)
- `test -f .planning/phases/07-content-pass-+-launch/deploy-checklist.md` succeeds
</verification>

<success_criteria>
1. Vercel Analytics + Speed Insights mount in app/layout.tsx (DPL-03 client-side activation done; runtime confirmation deferred to Plan 07-04 post-deploy).
2. deploy-checklist.md scaffold authored — Plan 07-04 has a line-by-line execution guide.
3. All prerequisites (engines pin, redaction scanner armed, deps installed) verified.
4. Zero test regressions; build still produces resume.pdf; no source touched outside layout.tsx.
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-pass-+-launch/07-00-SUMMARY.md`
</output>
