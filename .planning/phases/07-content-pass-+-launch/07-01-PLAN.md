---
phase: 07-content-pass-+-launch
plan: 01
type: execute
wave: 1
depends_on: [07-00]
files_modified:
  - content/projects/fathom.mdx
autonomous: true
requirements: []
must_haves:
  truths:
    - "A visitor navigating to /projects/fathom reads a Problem → Approach → Outcome case study (800–1200 words) that consumes Phase 3's PRJ-03 template unmodified"
    - "Fathom appears in getAll(), getHeroProjects(), and on home + /projects index (it's hero-tier, status: 'active')"
    - "Schema validates the MDX at module load (Phase 2 ProjectFrontmatterSchema); no fabricated metrics, no banned terms"
  artifacts:
    - path: "content/projects/fathom.mdx"
      provides: "Hero-tier public Fathom case study consumed by the Phase 3 dynamic-import page route"
      contains: "## Problem"
      min_lines: 50
  key_links:
    - from: "content/projects/fathom.mdx"
      to: "lib/schemas.ts ProjectFrontmatterSchema"
      via: "frontmatter YAML parsed by gray-matter, validated at lib/content.ts module load"
      pattern: "visibility: public"
    - from: "content/projects/fathom.mdx"
      to: "app/(site)/projects/[slug]/page.tsx"
      via: "Phase 3 dynamic import `@/content/projects/${slug}.mdx`; getNextProject auto-wires Fathom into Myco's next-project block via tag overlap"
      pattern: "## (Problem|Approach|Outcome)"
---

<objective>
Draft a Fathom hero-tier case study as content/projects/fathom.mdx using the locked Myco template (Problem → Approach → Outcome H2 anchors, 800–1200 words). Public project — no redaction gate required. Once the file lands, Phase 3's dynamic import route auto-renders /projects/fathom, and Phase 4's home + /projects index auto-surface it.

Purpose: Phase 7 success criterion #3 — "All hero and secondary case studies have real content drafted (no lorem ipsum, no placeholder copy left visible)". Fathom is hero-tier #2 (Myco shipped Phase 2; Agenda Keeper ships in Plan 07-02).
Output: One new MDX file, Phase 3 template auto-renders, getHeroProjects() now returns 3 projects (Myco + Fathom + the still-archived Agenda Keeper which lands in Plan 07-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-content-pass-+-launch/07-CONTEXT.md
@.planning/phases/02-content-pipeline/02-CONTEXT.md
@.planning/phases/02-content-pipeline/02-03-SUMMARY.md
@.planning/phases/03-project-detail-template/03-03-SUMMARY.md
@content/projects/myco.mdx
@lib/schemas.ts
@lib/projects.ts
@lib/tags.ts
@content/resume.ts

<interfaces>
<!-- Canonical template: content/projects/myco.mdx (Phase 2 Plan 02-03). -->
<!-- Phase 7 MDX files MUST match this shape so Phase 3's renderer works unmodified. -->

Frontmatter schema (from lib/schemas.ts ProjectFrontmatterRawSchema):
```yaml
slug: string                          # ^[a-z0-9-]+$  → must equal filename slug
title: string
tagline: string                       # max 140 chars (verified at parse time)
year: integer 2000-2100
tier: 'hero' | 'secondary'
order: integer                        # default 100; Myco=10; lower=earlier
status: 'active' | 'archived' | 'paused'   # archived hidden from getAll()
visibility: 'public' | 'private'
tags: array of TAGS enum (≥1)         # see lib/tags.ts for allowed values
stack: array of free-form strings (default [])
links:                                # all keys optional
  repo?: url
  live?: url
  docs?: url
  npm?: url
hero:                                 # required
  src: string                         # /images/projects/{slug}/hero-placeholder.png triggers text-only Variant B
  alt: string
gallery: array of {src, alt, caption?} (default [])
outcomes: array of strings, max 5 (default [])
description: string                   # max 160 chars (SEO meta description; Myco=158)
ogImage?: string                      # optional override; otherwise hero.src→/og-default.png fallback chain
```

Allowed tags (from lib/tags.ts — STRICT ENUM):
- local-first, autonomous, open-source, ai, agents, distributed, typescript, python, saas, cli, code-private
- Any tag NOT in this list fails the build with a Zod error.

Body shape (locked in Phase 2 Plan 02-03 SUMMARY):
- Three H2 sections — EXACT strings: `## Problem`, `## Approach`, `## Outcome`
- Word count: 800–1200 words across all three sections combined
- No fabricated metrics, no invented quotes
- Outcomes bullets in frontmatter are descriptive (e.g., "Apache 2.0 release", not "10× faster"); body prose mirrors that honesty

Hero treatment:
- Phase 3's <ProjectHero> branches on isPlaceholderHero(src) — regex /\/images\/projects\/[^/]+\/hero-placeholder\.(png|jpe?g|webp)$/i
- Match the suffix pattern to get the text-only Variant B (acceptable while no Fathom screenshot exists)
- Real images can drop in later without code changes

Source material to draw from (existing repo + READMEs):
- content/resume.ts § projects[1] (Fathom): "headless CLI and MCP server tracking AI dev-cost intelligence across LLM providers in real time; pluggable store and provider system; Claude Code plugin integrating cost intelligence into developer workflows"
- PROJECT.md § Context: "Fathom (public, TypeScript) — headless AI dev-cost intelligence; CLI + MCP server + Claude Code plugin; pluggable stores and LLM providers"
- 07-CONTEXT.md § specifics: "Public-project Fathom can mention TypeScript / MCP / Claude Code by name (it's open)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft content/projects/fathom.mdx (800–1200 words, Myco template shape)</name>
  <files>content/projects/fathom.mdx</files>
  <read_first>
    - content/projects/myco.mdx (canonical template — exact frontmatter ordering, body shape, prose voice)
    - lib/schemas.ts (frontmatter contract — Zod fails the build if any field is wrong)
    - lib/tags.ts (allowed tag enum)
    - lib/hero-fallback.ts (isPlaceholderHero regex — use /images/projects/fathom/hero-placeholder.png for text-only Variant B)
    - content/resume.ts § projects[Fathom] (existing bullets — source material for Approach + Outcome)
    - .planning/PROJECT.md § "Olive's current projects" (Fathom description)
    - .planning/phases/07-content-pass-+-launch/07-CONTEXT.md § specifics (Fathom may name TypeScript/MCP/Claude Code by name; it's public)
    - .planning/phases/02-content-pipeline/02-03-SUMMARY.md (Myco authoring decisions: 902 words, no fabricated metrics, descriptive outcomes only)
  </read_first>
  <action>
    Create `content/projects/fathom.mdx` with frontmatter + Problem/Approach/Outcome prose.

    **Frontmatter** (match Myco's key order exactly, sub-1200-word budget body):
    ```yaml
    ---
    slug: fathom
    title: Fathom
    tagline: <≤140 chars — one-sentence what-it-is, e.g. "Headless AI dev-cost intelligence — CLI + MCP server + Claude Code plugin with pluggable stores and providers.">
    year: 2025
    tier: hero
    order: 20                           # Myco=10; Fathom comes right after
    status: active
    visibility: public
    tags: [autonomous, open-source, ai, agents, typescript, cli]
    stack: [TypeScript, Node.js, MCP]
    links:
      repo: <PLACEHOLDER — Plan 07-03 will resolve; for now LEAVE OUT this whole `links:` block (or use `links: {}`) so the schema doesn't reject the file. NEVER fabricate a URL.>
    hero:
      src: /images/projects/fathom/hero-placeholder.png
      alt: Fathom dashboard placeholder — real screenshot lands post-launch
    outcomes:
      - <3–5 honest descriptive bullets; e.g. "Open-source CLI + MCP server", "Pluggable store + provider abstraction (any LLM, any backend)", "Claude Code plugin integration", "Real-time cost telemetry across sessions". NO percentages, NO dollar amounts, NO "Xx faster" claims.>
    description: <≤160 chars SEO meta — restate the tagline with verb-first framing. Run `echo -n "..." | wc -c` to verify.>
    ---
    ```

    **CRITICAL on links.repo:**
    - Phase 5 resume.ts has Fathom with NO `link` field (commented as PLACEHOLDER for Plan 07-03).
    - Plan 07-03 will resolve LinkedIn handle + Fathom repo URL + Stemz live URL in one batch — DO NOT preempt that decision here.
    - Acceptable choices for Plan 07-01:
      (a) Omit the `links:` block entirely — schema has `LinksSchema.default({})` so this is valid.
      (b) Write `links: {}` for explicitness.
      DO NOT invent a URL. Do NOT write `links.repo: https://github.com/olivelliott/fathom` unless Olive confirms it exists — and that confirmation is Plan 07-03's job.

    **Body** — three H2 sections, 800–1200 words total:

    `## Problem` (≈250-350 words): Why does AI dev-cost intelligence need to exist? Frame it: developers using Claude / OpenAI / Anthropic across multiple workflows have no honest, real-time picture of what each session costs. Cost dashboards exist at the provider level but not at the workflow/agent/session level. Engineers shipping with autonomous agents are flying blind on the cost-per-task curve. The conventional workarounds (manual spreadsheets, after-the-fact billing reconciliation) don't surface the information at the moment the decision is being made. Connect back to the autonomy thesis: durable, transparent feedback loops matter for systems that act on your behalf.

    `## Approach` (≈300-400 words): Describe the design honestly. Headless = no UI assumption; CLI is the primary surface; MCP server lets Claude Code (and any MCP host) query cost data through standard tooling. Pluggable stores (SQLite by default, but the abstraction lets teams swap to Postgres, DuckDB, or anything). Pluggable LLM providers (Anthropic, OpenAI, local Ollama). TypeScript end-to-end. Discuss WHY headless (composability with other dev workflows), WHY pluggable (different teams have different ground-truth sources for cost data), WHY MCP (matches the autonomy infrastructure pattern Myco established). Cite the Claude Code plugin as the proof-point: developers can install Fathom and immediately have cost intelligence in their daily agent workflows without separate dashboards.

    `## Outcome` (≈200-300 words): What ships. The CLI + MCP server are open. Anyone with a Claude Code workflow can install the plugin and start logging. Pluggable architecture is concretely a store interface + a provider interface, both with at least one shipped implementation each. Connect to the broader thesis: Myco gave agents memory, Fathom gives them an honest accounting of what their work costs. Both pieces point at the same goal — autonomous infrastructure that respects the user's data, budget, and visibility. Frame Fathom as actively evolving; do not invent metrics, user counts, or revenue. Mention licensing only if confirmed open-source.

    **Voice rules** (from PROJECT.md + Phase 2 Plan 02-03 SUMMARY):
    - First-person plural or implicit ("we built", "the system uses") — match Myco's voice
    - No marketing copy ("revolutionary", "best-in-class", "blazing fast")
    - No fabricated metrics, no invented quotes, no claim about shipped features that aren't shipped
    - Honest hedges where appropriate ("Fathom is still young", "the abstraction is intentionally minimal")
    - Banned-words carry-forward from Phase 4 UI-SPEC: do NOT use "ecosystem" (rewrite to "community"). Per Phase 5 Plan 05-01 SUMMARY: this is the active banned-words contract.

    **Honesty placeholders** (if anything cannot be verified from existing source material):
    - Embed an MDX comment: `{/* PLACEHOLDER: needs Olive's confirmation — specific provider list */}`
    - These render to nothing visible but are greppable for Plan 07-04 pre-deploy gate
    - DO NOT use "TODO" or "TBD" in prose; rewrite the sentence to omit unverifiable claims

    **Acceptance checks** (run before declaring done):
    1. `pnpm build` succeeds — Zod schema validates frontmatter; if it fails, fix frontmatter (most likely cause: tag not in TAGS enum or description >160 chars).
    2. `pnpm vitest run tests/content/` — Phase 2 tests still green; Fathom now appears in getAll() and getHeroProjects().
    3. `pnpm vitest run tests/content/redaction.test.ts` — still vacuously passes (Fathom is public; the redaction scanner only fires on `visibility: private`).
    4. Word count: `wc -w content/projects/fathom.mdx` — body alone (frontmatter excluded) should fall in 800–1200. To check body only: `awk '/^---$/{c++; next} c==2' content/projects/fathom.mdx | wc -w`.
    5. Visual sanity: `pnpm build && pnpm start` then visit `http://localhost:3000/projects/fathom` — hero renders Variant B (text-only), three H2 sections render with anchor links, NextProjectBlock shows multi-project variant (Fathom + Myco are now ≥2 active projects), no visible PLACEHOLDER text.

    Commit message: `feat(07-01): author content/projects/fathom.mdx (Phase 7 hero-tier #2)` — body includes word count + outcomes bullet count + confirmation that links block is empty pending Plan 07-03.
  </action>
  <verify>
    <automated>pnpm vitest run --reporter=dot &amp;&amp; pnpm build &amp;&amp; test -f content/projects/fathom.mdx &amp;&amp; grep -q "^## Problem" content/projects/fathom.mdx &amp;&amp; grep -q "^## Approach" content/projects/fathom.mdx &amp;&amp; grep -q "^## Outcome" content/projects/fathom.mdx &amp;&amp; WORDS=$(awk '/^---$/{c++; next} c==2' content/projects/fathom.mdx | wc -w) &amp;&amp; [ "$WORDS" -ge 800 ] &amp;&amp; [ "$WORDS" -le 1200 ]</automated>
  </verify>
  <acceptance_criteria>
    - `content/projects/fathom.mdx` exists with `visibility: public`, `tier: hero`, `status: active`, `slug: fathom`
    - Frontmatter parses cleanly (Zod schema accepts at module load — `pnpm build` exits 0)
    - Body contains the three EXACT H2 strings: `## Problem`, `## Approach`, `## Outcome`
    - Body word count between 800 and 1200 words (inclusive)
    - No banned words: `! grep -iE "\b(ecosystem|passionate|blazing|revolutionary|best-in-class)\b" content/projects/fathom.mdx` (UI-SPEC banned-words carry-forward)
    - No fabricated metrics: manual review — no percentages, no dollar amounts, no "Nx faster" or "Nx more" claims (visual check during commit)
    - `links:` block is either omitted or empty (`{}`) — fabricating a repo URL is forbidden until Plan 07-03 resolves it
    - hero.src uses `/images/projects/fathom/hero-placeholder.png` (triggers Phase 3 Variant B text-only treatment)
    - `pnpm vitest run` still green (no regression); Fathom appears in getAll() — confirm by running an ad-hoc test or via `pnpm build` static-page emit count
    - `.next/server/app/projects/fathom.html` exists after `pnpm build`
    - Redaction scanner still passes (public project doesn't trigger the dynamic-describe assertions)
  </acceptance_criteria>
  <done>
    Fathom MDX shipped; /projects/fathom renders end-to-end with the Phase 3 template unmodified; Phase 4 home + /projects index auto-surface the new card; getHeroProjects() now returns Myco + Fathom; build + tests green.
  </done>
</task>

</tasks>

<verification>
- `pnpm vitest run` exit 0 (no regression vs Plan 07-00 baseline)
- `pnpm build` exit 0
- `pnpm typecheck` exit 0
- `wc -w` on body section returns 800–1200
- `/projects/fathom` static HTML exists in .next/server/app/projects/
- Manual visual pass at `http://localhost:3000/projects/fathom` (during Olive's review at next opportunity — not blocking for Plan 07-02 to start)
</verification>

<success_criteria>
1. Fathom case study shipped at /projects/fathom in the locked Problem/Approach/Outcome template.
2. Frontmatter passes Phase 2 schema validation (no Zod error at build time).
3. Body in 800–1200 word range; no banned words; no fabricated metrics; honest hedges where unverifiable.
4. Phase 3 template + Phase 4 grids auto-consume the new MDX with zero source changes.
5. links.repo deferred to Plan 07-03 (NOT fabricated).
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-pass-+-launch/07-01-SUMMARY.md`
</output>
