---
phase: 07-content-pass-+-launch
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - tests/fixtures/banned-terms.ts
  - content/projects/agenda-keeper.mdx
  - .planning/phases/07-content-pass-+-launch/07-REDACTION-REVIEW-agenda-keeper.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "A visitor navigating to /projects/agenda-keeper reads a Problem → Approach → Outcome case study (800–1200 words) with a visible 'code private' label and ZERO occurrences of a repo URL in the rendered HTML"
    - "tests/fixtures/banned-terms.ts is EXTENDED (not replaced) with any new private-project-specific banned terms required for Agenda Keeper redaction"
    - "Phase 2 redaction.test.ts auto-fires per-file against the new agenda-keeper.mdx and exits green"
    - "A forked redaction-review checklist exists at .planning/phases/07-content-pass-+-launch/07-REDACTION-REVIEW-agenda-keeper.md, signed off by Olive via checkpoint:human-action before commit"
  artifacts:
    - path: "content/projects/agenda-keeper.mdx"
      provides: "Hero-tier PRIVATE Agenda Keeper case study consumed by Phase 3 dynamic-import route + Phase 4 grids"
      contains: "visibility: private"
      min_lines: 50
    - path: "tests/fixtures/banned-terms.ts"
      provides: "Extended frozen BANNED_TERMS array (Phase 2 baseline 3 entries + any Agenda Keeper-specific terms)"
      contains: "Object.freeze"
    - path: ".planning/phases/07-content-pass-+-launch/07-REDACTION-REVIEW-agenda-keeper.md"
      provides: "Per-project sign-off filed using Phase 2's template; locks the audit trail for Phase 7 success criterion #4"
      min_lines: 40
  key_links:
    - from: "content/projects/agenda-keeper.mdx"
      to: "lib/schemas.ts ProjectFrontmatterSchema.transform"
      via: "visibility: private triggers auto-add of code-private tag + strips any links.repo"
      pattern: "visibility: private"
    - from: "content/projects/agenda-keeper.mdx"
      to: "tests/content/redaction.test.ts"
      via: "dynamic-describe per-file assertion auto-fires; case-insensitive whole-word banned-term scan must return []"
      pattern: "BANNED_TERMS"
    - from: ".planning/phases/07-content-pass-+-launch/07-REDACTION-REVIEW-agenda-keeper.md"
      to: "checkpoint:human-action gate (Olive's sign-off before commit)"
      via: "filled-in sign-off block from Phase 2's template"
      pattern: "Approved by"
---

<objective>
Author Agenda Keeper (PRIVATE) as content/projects/agenda-keeper.mdx, extending the Phase 2 banned-terms fixture in place with any new client-specific guardrails, drafting from Phase 5 resume bullets + PROJECT.md context at the level of "meeting-management SaaS for small agencies", then gating on Phase 2's automated redaction scanner + a checkpoint:human-action for Olive's sign-off using the Phase 2 review-checklist template forked per project.

Purpose: Phase 7 success criterion #4 — "Every private-project case study has passed an explicit redaction review before going live". Also closes hero-tier #3 (Myco + Fathom + Agenda Keeper). The redaction gate is mandatory because Agenda Keeper sits behind an NDA-adjacent Aktiga relationship.
Output: New private MDX file rendering at /projects/agenda-keeper with privacy contract intact (no repo link, visible "code private" label), banned-terms fixture extended, sign-off filed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-content-pass-+-launch/07-CONTEXT.md
@.planning/phases/02-content-pipeline/02-CONTEXT.md
@.planning/phases/02-content-pipeline/02-04-SUMMARY.md
@.planning/phases/02-content-pipeline/02-REDACTION-REVIEW.md
@.planning/phases/03-project-detail-template/03-03-SUMMARY.md
@content/projects/myco.mdx
@lib/schemas.ts
@lib/projects.ts
@lib/tags.ts
@tests/fixtures/banned-terms.ts
@tests/content/redaction.test.ts
@content/resume.ts

<interfaces>
<!-- Same frontmatter schema as Plan 07-01 (lib/schemas.ts ProjectFrontmatterRawSchema). -->
<!-- Key difference: visibility: 'private' triggers the schema .transform() which: -->
<!--   1. Auto-adds 'code-private' to tags (deduped) -->
<!--   2. Strips links.repo (silently in prod, warns in dev) -->
<!-- Phase 3 Plan 03-02 SUMMARY established the privacy contract: -->
<!--   - <ProjectMeta> shows literal "code private" label (--color-text-tertiary, non-interactive) -->
<!--   - <TagChipRow> renders code-private chip identically to other chips -->
<!--   - Zero anchors with the would-be repo URL render on the page -->

Phase 2 banned-terms fixture (tests/fixtures/banned-terms.ts):
```ts
export const BANNED_TERMS: readonly string[] = Object.freeze([
  'aktiga',
  'voya',
  'spectra',
])
```

Rules for extending (from the fixture's own comments):
- Lowercase canonical form
- Whole-word boundary enforced by the scanner — no need for variants
- Add ONLY with sign-off via the checklist doc

Redaction scanner contract (tests/content/redaction.test.ts):
- Walks content/projects/*.mdx
- Filters to visibility === 'private'
- Strips frontmatter via gray-matter (only body is scanned, NOT frontmatter)
- Lowercases both haystack and needle, applies /\b{term}\b/ regex
- Fails the per-file `it()` block if any banned term matches

Phase 2 redaction-review checklist template (02-REDACTION-REVIEW.md):
- Banned-term rationale table
- 9-item privacy-sensitive-topics list
- Copy-paste sign-off block (replace <name>, <project-slug>, <YYYY-MM-DD>)

Source material for Agenda Keeper (from PROJECT.md § "Olive's current projects"):
- "Agenda Keeper (private repo `the-real-agenda-keeper`) — meeting-management SaaS for small agencies; Convex + Next.js + TipTap/ProseMirror; OAuth calendar sync"

From content/resume.ts § projects[2] (Agenda Keeper):
- "Developed a full-stack meeting management platform with real-time collaborative editing via TipTap/ProseMirror"
- "Integrated OAuth-based calendar sync to unify scheduling across Google Calendar and Outlook"
- "Built on Convex for reactive, real-time data with zero-config backend infrastructure"

Privacy constraint (Phase 7 CONTEXT.md § specifics):
- "Private-project Agenda Keeper stays at the level of 'meeting-management SaaS for small agencies' without naming clients or specific Aktiga details"
- The product itself (Agenda Keeper, meeting management, TipTap/ProseMirror/Convex stack) is OK to discuss — those are public technical facts. The connection to specific Aktiga clients, internal team members, or proprietary roadmap is NOT.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tests/fixtures/banned-terms.ts (in place, append-only) + draft content/projects/agenda-keeper.mdx</name>
  <files>
    tests/fixtures/banned-terms.ts
    content/projects/agenda-keeper.mdx
  </files>
  <read_first>
    - tests/fixtures/banned-terms.ts (current frozen list: aktiga, voya, spectra)
    - tests/content/redaction.test.ts (dynamic-describe per-file assertion pattern)
    - content/projects/myco.mdx (template shape — frontmatter ordering + body structure)
    - .planning/phases/02-content-pipeline/02-REDACTION-REVIEW.md (banned-term rationale table; 9 privacy-sensitive topics)
    - .planning/phases/07-content-pass-+-launch/07-CONTEXT.md § specifics (redaction rules + voice for Agenda Keeper)
    - .planning/phases/03-project-detail-template/03-03-SUMMARY.md § PRJ-06 contract (3 visible privacy signals)
    - content/resume.ts § projects[Agenda Keeper] (verified-public bullets)
    - PROJECT.md § Olive's current projects (Agenda Keeper description)
  </read_first>
  <action>
    Two sub-steps, committed in a single commit.

    **Step A — Extend banned-terms.ts (APPEND ONLY, NEVER REPLACE):**

    Read the current file. The current array MUST remain `['aktiga', 'voya', 'spectra']`. Append new terms ONLY if Agenda Keeper authoring genuinely needs additional guardrails. Likely candidates (evaluate; do NOT add reflexively):
    - Specific named Aktiga clients you discover in resume/PROJECT.md you might accidentally reference (none currently surface that aren't already in the list).
    - Code-name of the private repo: `the-real-agenda-keeper` — appears in PROJECT.md. Add `the-real-agenda-keeper` to the banned-terms list (rationale: that's an internal naming we shouldn't lean on; the public framing is just "Agenda Keeper").

    Default extension (use unless explicit justification to add more):
    ```ts
    export const BANNED_TERMS: readonly string[] = Object.freeze([
      'aktiga',
      'voya',
      'spectra',
      'the-real-agenda-keeper',
    ])
    ```

    Notes:
    - The word `agenda-keeper` (slug form, no `the-real-` prefix) is NOT banned — the project title is the public-facing brand. Banning it would force the scanner to flag the public title.
    - Convex / TipTap / ProseMirror / OAuth are PUBLIC technical terms — NEVER ban these; they're the legitimate stack discussion.
    - Whole-word boundary means "the-real-agenda-keeper" matches as a hyphenated single token; `\b` treats `-` as a word boundary in JS regex, so for hyphenated strings we may need to scan more carefully. Verify: write a self-test in tests/content/redaction.test.ts? NO — Phase 2's self-test already proves whole-word + case-insensitive; this plan's job is just to confirm `pnpm vitest run tests/content/redaction.test.ts` passes against the new MDX. If the regex misfires on hyphenated terms, that's a Phase 2 scanner bug — file as DEFER and lift the term to just `agenda-keeper-internal` or similar. (Olive's call during the checkpoint.)

    **Step B — Author content/projects/agenda-keeper.mdx:**

    Frontmatter (Myco-mirrored, marked private):
    ```yaml
    ---
    slug: agenda-keeper
    title: Agenda Keeper
    tagline: <≤140 chars — e.g. "Real-time meeting-management SaaS — collaborative agendas, OAuth-synced calendars, built for small agencies.">
    year: 2025
    tier: hero
    order: 30                            # Myco=10, Fathom=20, Agenda Keeper=30
    status: active
    visibility: private                  # ← triggers code-private auto-tag + repo strip
    tags: [saas, typescript]             # schema transform adds 'code-private' automatically; do NOT add it here
    stack: [TypeScript, Next.js, Convex, TipTap, ProseMirror]
    # links: omitted entirely — schema .transform() would strip links.repo anyway,
    # but omission is cleaner and aligns with PROJECT.md "no repo link anywhere"
    hero:
      src: /images/projects/agenda-keeper/hero-placeholder.png
      alt: Agenda Keeper placeholder — real screenshot withheld for privacy
    outcomes:
      - <3–5 honest descriptive bullets — e.g. "Real-time collaborative editing via TipTap/ProseMirror", "OAuth calendar sync across Google Calendar + Outlook", "Convex-backed reactive data layer", "Built for small-agency workflow density". NO client names, NO revenue, NO user counts.>
    description: <≤160 chars SEO meta — restate the tagline.>
    ---
    ```

    Body — three H2 sections, 800–1200 words total, written at PUBLIC framing level:

    `## Problem` (≈250-350 words): What does meeting-management software for small agencies need to do that off-the-shelf tools don't? Generic calendar tools optimize for individual scheduling; agency workflows turn around collaborative agendas, threaded action items, and post-meeting handoffs. The conventional tooling fragments this across Calendar + Doc + Task-tracker, forcing the agency to maintain three separate sources of truth. The Problem section can name TipTap/ProseMirror as exemplars of real-time collaborative editing primitives, and Convex as a reactive-backend pattern; it should NOT name any specific Aktiga client, internal codename, NDA-sensitive product detail, or unreleased feature. Frame the problem-space generically.

    `## Approach` (≈300-400 words): Describe the architecture honestly at a public-technical level. Next.js App Router + Convex backend (reactive data, real-time subscriptions, no manual websocket plumbing). TipTap/ProseMirror for collaborative document editing on the agenda surface. OAuth integration for Google Calendar + Outlook to ingest and reflect calendar state. The collaborative editing model: who can edit which sections, how conflicts resolve. Real-time presence (showing who is currently viewing or editing). Mention WHY Convex over Supabase / Firebase (reactive primitives, TS-first, the developer experience matches the autonomy thesis: less ceremony, more shipped product). Do NOT describe specific client workflows, do NOT name specific agencies, do NOT describe internal Aktiga deployment topology.

    `## Outcome` (≈200-300 words): What the product is and what shipping looks like. It runs production-grade for the target audience. The TipTap/ProseMirror collaborative editing surface is the differentiator; OAuth sync is the connective tissue. Honest hedging: this is a private project; the public-facing case study is necessarily abstract about deployment scale, customer counts, and specific workflows. Connect back to the broader thesis: the same autonomy principles that drove Myco (durable infrastructure that respects the user's data) shape product decisions here too — small-agency operators own their meeting data, not a platform middleman. Do NOT fabricate user counts, revenue, retention, or "X% time saved" metrics.

    **Voice + redaction rules:**
    - First-person plural acceptable ("we built", "the system handles") — match Myco's voice
    - Banned terms (after extension): `aktiga`, `voya`, `spectra`, `the-real-agenda-keeper` — none of these may appear in the body. Aktiga in particular is the most likely accidental slip; vigilance required.
    - Banned-words carry-forward (UI-SPEC § Copywriting Contract): NO "ecosystem", NO "passionate", NO "blazing", NO "revolutionary", NO "best-in-class".
    - No fabricated metrics. If you need a number to make a sentence flow, rewrite the sentence.
    - If anything is uncertain about safety, embed `{/* PLACEHOLDER: needs Olive's confirmation — <topic> */}` and discuss in the checkpoint.

    **Verify before exit:**
    1. `pnpm build` — schema parses; Zod adds code-private tag automatically; links.repo is undefined for private projects (already omitted, so no strip needed).
    2. `pnpm vitest run tests/content/redaction.test.ts` — the new dynamic-describe `it()` block for agenda-keeper.mdx fires and PASSES (empty match array).
    3. `pnpm vitest run` — full suite green; in particular `tests/projects/page.test.tsx` PRJ-06 contract still satisfied (this test routes through slug='myco' with a private-fixture mock, so adding agenda-keeper.mdx doesn't break it).
    4. `wc -w` body section: 800–1200 words.
    5. `! grep -iE "\b(aktiga|voya|spectra|the-real-agenda-keeper)\b" content/projects/agenda-keeper.mdx` — empty match.
    6. `! grep -iE "\b(ecosystem|passionate|blazing|revolutionary|best-in-class)\b" content/projects/agenda-keeper.mdx` — empty match.

    Commit message: `feat(07-02): author content/projects/agenda-keeper.mdx (private hero-tier #3) + extend BANNED_TERMS`. Body documents the new banned term + the redaction-scanner self-test status.
  </action>
  <verify>
    <automated>pnpm vitest run --reporter=dot &amp;&amp; pnpm build &amp;&amp; test -f content/projects/agenda-keeper.mdx &amp;&amp; grep -q "visibility: private" content/projects/agenda-keeper.mdx &amp;&amp; grep -q "^## Problem" content/projects/agenda-keeper.mdx &amp;&amp; grep -q "^## Approach" content/projects/agenda-keeper.mdx &amp;&amp; grep -q "^## Outcome" content/projects/agenda-keeper.mdx &amp;&amp; ! grep -iE "\b(aktiga|voya|spectra)\b" content/projects/agenda-keeper.mdx &amp;&amp; WORDS=$(awk '/^---$/{c++; next} c==2' content/projects/agenda-keeper.mdx | wc -w) &amp;&amp; [ "$WORDS" -ge 800 ] &amp;&amp; [ "$WORDS" -le 1200 ]</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/banned-terms.ts` retains the original 3 entries (aktiga, voya, spectra) AND adds the-real-agenda-keeper (or equivalent — surfaced for sign-off in Task 2)
    - `content/projects/agenda-keeper.mdx` exists with `visibility: private`, `tier: hero`, `status: active`, `slug: agenda-keeper`
    - Frontmatter does NOT include `links:` block (or, if present, has no `repo` key)
    - Frontmatter does NOT include `code-private` in tags (schema auto-adds it)
    - Body contains the three EXACT H2 strings: `## Problem`, `## Approach`, `## Outcome`
    - Body word count between 800 and 1200 words
    - Zero matches for banned terms (case-insensitive whole-word) in body
    - Zero matches for carry-forward banned words (ecosystem, passionate, blazing, revolutionary, best-in-class) anywhere in file
    - `pnpm vitest run tests/content/redaction.test.ts` exits 0 (dynamic describe now has a real per-file it() block for agenda-keeper.mdx, and it passes)
    - `pnpm build` exits 0; `.next/server/app/projects/agenda-keeper.html` exists
    - Static HTML for /projects/agenda-keeper contains the literal "code private" label and contains a code-private tag chip — manual or grep verification: `grep -c "code private" .next/server/app/projects/agenda-keeper.html` ≥ 1
    - Static HTML for /projects/agenda-keeper contains ZERO anchors pointing at any github.com/* URL for this slug
  </acceptance_criteria>
  <done>
    Banned-terms fixture extended in place; agenda-keeper.mdx authored, gated through automated redaction scanner; build emits the static page with the privacy contract intact (visible "code private" label + no repo URL anywhere in the rendered HTML).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Olive reviews + signs off Agenda Keeper redaction (per Phase 2 checklist template)</name>
  <files>
    .planning/phases/07-content-pass-+-launch/07-REDACTION-REVIEW-agenda-keeper.md
  </files>
  <read_first>
    - content/projects/agenda-keeper.mdx (the new MDX, as just authored in Task 1)
    - .planning/phases/02-content-pipeline/02-REDACTION-REVIEW.md (sign-off template — copy + fill)
    - tests/fixtures/banned-terms.ts (updated list)
  </read_first>
  <action>
    Author the per-project review file, then PAUSE for Olive's sign-off.

    **Step 1 — Create the review file** at `.planning/phases/07-content-pass-+-launch/07-REDACTION-REVIEW-agenda-keeper.md`. Use the Phase 2 template's sign-off block (02-REDACTION-REVIEW.md § "Sign-off template") as the structural base. Adapt:

    ```markdown
    # Phase 7 — Redaction Review: Agenda Keeper

    **File reviewed:** `content/projects/agenda-keeper.mdx`
    **Visibility:** private
    **Reviewer:** Olive Elliott
    **Date:** _pending — fill in at sign-off_

    ---

    ## Pre-review automated state

    - [x] `pnpm vitest run tests/content/redaction.test.ts` is green (per-file dynamic-describe it() for agenda-keeper.mdx passes)
    - [x] `tests/fixtures/banned-terms.ts` extended with `the-real-agenda-keeper` (rationale below)
    - [x] Schema transform applied (visibility:private → code-private tag auto-added, no repo link rendered)
    - [x] Build emits static /projects/agenda-keeper page with visible "code private" label, zero repo-URL anchors

    ## Banned-term extension rationale

    | Term added | Why |
    |------------|-----|
    | `the-real-agenda-keeper` | Internal repository name — public framing is just "Agenda Keeper". Banning the slug-form prevents an accidental copy-paste from internal docs or commit history. |

    (Add additional terms here if Olive requests during sign-off.)

    ## Banned-term check (redundant with automated, included for audit trail)

    - [ ] No occurrence of `aktiga`, `voya`, `spectra`, `the-real-agenda-keeper` in body (manually verified by reviewer)

    ## Privacy-sensitive topic check (Phase 2 checklist, applied to Agenda Keeper)

    - [ ] Internal tooling / workflows of Aktiga or any client agency — none referenced
    - [ ] Unreleased features or roadmap items — none referenced
    - [ ] Reverse-engineerable architecture details that could identify a specific client deployment — none exposed
    - [ ] Proprietary code/config copied from the private repo — none copied
    - [ ] Named teammates, clients, agencies, or stakeholders — none (or consent documented inline)
    - [ ] Internal metrics, revenue numbers, user counts, retention figures — none
    - [ ] Security practices / audit findings — none
    - [ ] Client-identifying screenshots — none (hero is placeholder per Phase 3 Variant B)
    - [ ] Undisclosed contractual relationships — none

    ## Public technical surface — explicitly permitted

    These are public technical primitives and may appear in the body:
    - TipTap / ProseMirror (public open-source editing libraries)
    - Convex (public backend platform)
    - Next.js / TypeScript / OAuth / Google Calendar / Outlook (industry-standard tooling)

    ## Approval

    I have read the full MDX body, run the automated scanner, and confirmed each box in the privacy-sensitive-topic checklist above. This content is safe to publish at /projects/agenda-keeper on the public Vercel subdomain.

    **Approved by:** _________________________
    **Signature/Date:** _________________________
    ```

    **Step 2 — Pause for Olive's review.** Surface to the user (human-verify checkpoint):

    > **Pause for redaction sign-off.**
    >
    > I've drafted Agenda Keeper's private case study and extended the banned-terms fixture. Before committing, please read the MDX end-to-end and sign off using the checklist:
    >
    > 1. Open `content/projects/agenda-keeper.mdx` — read the body for any:
    >    - Specific client names you recognize from Aktiga work
    >    - Architecture details that could fingerprint a known deployment
    >    - Unreleased feature mentions
    >    - Anything that feels NDA-adjacent
    >
    > 2. Open `.planning/phases/07-content-pass-+-launch/07-REDACTION-REVIEW-agenda-keeper.md` and tick each privacy-sensitive-topic box (or reply with revisions needed).
    >
    > 3. Confirm the banned-terms extension (`the-real-agenda-keeper` added) is correct, or tell me what else to add.
    >
    > 4. Reply with one of:
    >    - **"approved"** — I commit the MDX + banned-terms + review doc as a single commit, then Plan 07-02 is done.
    >    - **"revise: <feedback>"** — I revise and re-surface for another pass.
    >    - **"add banned terms: <list>"** — I append to BANNED_TERMS, re-run the redaction scanner, and re-surface.

    **Step 3 — After approval:** Commit all three files together: `feat(07-02): ship agenda-keeper private MDX + redaction sign-off`. Body cites the sign-off doc and confirms the banned-terms extension.

    DO NOT commit before approval. DO NOT push to remote (Plan 07-04's job).
  </action>
  <verify>
    <automated>test -f .planning/phases/07-content-pass-+-launch/07-REDACTION-REVIEW-agenda-keeper.md &amp;&amp; grep -q "Approved by:" .planning/phases/07-content-pass-+-launch/07-REDACTION-REVIEW-agenda-keeper.md &amp;&amp; grep -q "Privacy-sensitive topic check" .planning/phases/07-content-pass-+-launch/07-REDACTION-REVIEW-agenda-keeper.md &amp;&amp; pnpm vitest run tests/content/redaction.test.ts</automated>
  </verify>
  <what-built>
    A private hero-tier Agenda Keeper case study + extended banned-terms guardrail + per-project sign-off doc, all gated through Phase 2's automated redaction scanner and awaiting Olive's manual review.
  </what-built>
  <how-to-verify>
    1. Read `content/projects/agenda-keeper.mdx` end-to-end — confirm no client names, no NDA-sensitive details, no fabricated metrics
    2. Open `.planning/phases/07-content-pass-+-launch/07-REDACTION-REVIEW-agenda-keeper.md` — read each privacy-sensitive-topic line
    3. Verify the new banned-term `the-real-agenda-keeper` in `tests/fixtures/banned-terms.ts` matches Olive's understanding of which internal names need protection
    4. Run `pnpm vitest run tests/content/redaction.test.ts` locally to confirm the scanner is green
    5. Optionally run `pnpm dev` and visit `http://localhost:3000/projects/agenda-keeper` to confirm the "code private" label renders and no repo link appears
  </how-to-verify>
  <resume-signal>
    Reply with "approved", "revise: &lt;feedback&gt;", or "add banned terms: &lt;list&gt;"
  </resume-signal>
  <acceptance_criteria>
    - `.planning/phases/07-content-pass-+-launch/07-REDACTION-REVIEW-agenda-keeper.md` exists
    - File includes all 9 privacy-sensitive-topic checkboxes from Phase 2 template
    - File includes banned-term rationale table for any new entries
    - File includes "Approved by" + "Signature/Date" sign-off block (filled in after Olive's approval)
    - User has responded with "approved" (or revisions completed and re-approved)
    - All three files (banned-terms.ts, agenda-keeper.mdx, review doc) committed together in a single `feat(07-02):` commit
  </acceptance_criteria>
  <done>
    Olive has reviewed and approved the Agenda Keeper MDX + banned-terms extension; sign-off doc filed; all three files committed atomically; Phase 7 success criterion #4 (private redaction review) is satisfied for Agenda Keeper.
  </done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/content/redaction.test.ts` exit 0 (now has real per-file assertion for agenda-keeper.mdx)
- `pnpm vitest run` exit 0 (full suite)
- `pnpm build` exit 0
- `pnpm typecheck` exit 0
- `wc -w` body of agenda-keeper.mdx between 800 and 1200
- `grep -c "code private" .next/server/app/projects/agenda-keeper.html` ≥ 1
- `grep -c "github.com" .next/server/app/projects/agenda-keeper.html` consistent with the page chrome (footer link to portfolio repo is acceptable; there must be ZERO link specifically scoped to agenda-keeper)
- Sign-off file `07-REDACTION-REVIEW-agenda-keeper.md` exists with "Approved by:" line filled in
</verification>

<success_criteria>
1. Agenda Keeper PRIVATE hero-tier case study shipped at /projects/agenda-keeper using the locked Problem/Approach/Outcome template.
2. Phase 2 schema's privacy transform exercised on real content for the first time: code-private auto-tag visible, links.repo never rendered.
3. Phase 2 redaction scanner's dynamic-describe block now actively asserts (no longer vacuously passing) and exits green.
4. tests/fixtures/banned-terms.ts extended in place (append-only) per the fixture's documented rules.
5. Per-project sign-off doc filed; Olive's manual approval gate satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-pass-+-launch/07-02-SUMMARY.md`
</output>
