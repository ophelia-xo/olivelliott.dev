---
phase: 07-content-pass-+-launch
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - content/resume.ts
  - components/about/contact-stack.tsx
  - components/site/footer.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "All Phase 5 PLACEHOLDER markers (LinkedIn handle, Fathom repo URL, Stemz live URL, Aktiga role title) are resolved with confirmed values OR explicitly removed with rationale"
    - "Three source files updated atomically in a single commit per resolution batch — no piecemeal drift between resume.ts / contact-stack.tsx / footer.tsx"
    - "If Fathom repo URL is confirmed, content/projects/fathom.mdx is updated in the same commit to add links.repo"
  artifacts:
    - path: "content/resume.ts"
      provides: "Resume source-of-truth with confirmed LinkedIn URL, Fathom repo URL (if public), Stemz live URL (if public)"
      contains: "linkedin:"
    - path: "components/about/contact-stack.tsx"
      provides: "About-page contact stack with confirmed LinkedIn handle (matches resume.ts)"
      contains: "LINKEDIN_URL"
    - path: "components/site/footer.tsx"
      provides: "Footer with confirmed LinkedIn handle (matches resume.ts + contact-stack.tsx)"
      contains: "LINKEDIN_URL"
  key_links:
    - from: "content/resume.ts"
      to: "components/about/contact-stack.tsx + components/site/footer.tsx"
      via: "consistent LinkedIn URL string across all three surfaces"
      pattern: "linkedin\\.com"
    - from: "Plan 07-03 checkpoint output"
      to: "Plan 07-04 pre-deploy grep gate"
      via: "no remaining PLACEHOLDER markers in the 3 source surfaces (verified by source-grep before deploy)"
      pattern: "PLACEHOLDER"
---

<objective>
Resolve all four Phase 5 PLACEHOLDER markers in a single coordinated batch: LinkedIn handle, Fathom repo URL, Stemz live URL, Aktiga role title. Three files modified atomically per resolution so the about / footer / resume surfaces never drift out of sync. Requires checkpoint:human-action — Olive supplies the actual values.

Purpose: Phase 7 success criterion #3 — "any placeholder that remains is explicitly flagged as such". This plan converts soft PLACEHOLDER markers into either confirmed values OR an explicit decision-recorded omission. Either path leaves the codebase honest at deploy time.
Output: content/resume.ts, components/about/contact-stack.tsx, components/site/footer.tsx all carry confirmed values OR explicit empty-string + comment documenting the deferral.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-content-pass-+-launch/07-CONTEXT.md
@.planning/phases/05-about-+-resume-+-contact/05-03-SUMMARY.md
@.planning/phases/05-about-+-resume-+-contact/05-04-SUMMARY.md
@content/resume.ts
@components/about/contact-stack.tsx
@components/site/footer.tsx
@Olive_Elliott_Resume.docx

<interfaces>
<!-- Three files touch placeholders. The LinkedIn URL appears in ALL THREE -->
<!-- and MUST stay consistent or the user sees different handles in different -->
<!-- places. -->

PLACEHOLDER locations (verified via `grep -rn "PLACEHOLDER" content/resume.ts components/about/ components/site/`):

1. content/resume.ts:8 — header comment listing all 4 placeholders
2. content/resume.ts:41 — `linkedin: 'https://linkedin.com/in/olive-elliott'` (assumed slug)
3. content/resume.ts:86 — Fathom project entry: NO `link:` field
4. content/resume.ts:118 — Stemz project entry: NO `link:` field
5. components/about/contact-stack.tsx:4 — header comment
6. components/about/contact-stack.tsx:10 — `LINKEDIN_URL = 'https://linkedin.com/in/olive-elliott'`
7. components/site/footer.tsx:23 — `LINKEDIN_URL = 'https://linkedin.com/in/olive-elliott'` (no inline PLACEHOLDER comment but value is same assumed slug; mentioned in 07-CONTEXT decisions)

Aktiga role title: per Olive_Elliott_Resume.docx + 07-CONTEXT.md, the role at Aktiga needs Olive's confirmation. Currently content/resume.ts § experience[0].role reads "Software Engineer / System Architect / Project Lead" — already populated from .docx, but worth confirming during checkpoint.

Schema constraints (lib/schemas.ts):
- ResumeHeaderSchema.links.linkedin: z.string().url() — must be a valid URL or schema.parse() fails at module load (build fails)
- ResumeProjectSchema.link: z.string().url().optional() — Fathom/Stemz can stay without `link` (current state) OR receive a confirmed URL; cannot be empty string (empty string fails .url())
- ProjectFrontmatterSchema LinksSchema.repo: z.string().url().optional() — same rule for content/projects/fathom.mdx if we add a repo link
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Olive supplies confirmed values for 4 placeholders</name>
  <files>
    .planning/phases/07-content-pass-+-launch/07-03-placeholder-resolutions.md
  </files>
  <read_first>
    - content/resume.ts (all 4 PLACEHOLDER comment lines + their current values)
    - components/about/contact-stack.tsx (LinkedIn URL definition)
    - components/site/footer.tsx (LinkedIn URL definition)
    - .planning/phases/07-content-pass-+-launch/07-CONTEXT.md § "Phase 5 placeholder resolution" (batch resolution decision)
  </read_first>
  <decision>
    For each of the 4 placeholders, Olive provides the resolution:

    **(a) LinkedIn handle** — currently `https://linkedin.com/in/olive-elliott` (assumed slug). Confirm or correct. Per Schema constraint, must be a valid URL.

    **(b) Fathom repo URL** — currently NO `link:` field in resume + NO `links.repo` in content/projects/fathom.mdx (Plan 07-01 deferred to here). Confirm public URL OR confirm it stays without a repo link.

    **(c) Stemz live URL** — currently NO `link:` field in resume. Confirm live URL OR confirm it stays without a link.

    **(d) Aktiga role title** — currently "Software Engineer / System Architect / Project Lead" in resume. Confirm exact title OR provide revised wording.
  </decision>
  <context>
    Per 07-CONTEXT.md "Phase 5 placeholder resolution" decision: single-round resolution, atomic per-confirmation commits. This task gates on Olive supplying the values; Task 2 applies them in one batch.

    DO NOT proceed without explicit values for all 4. DO NOT fabricate URLs. DO NOT guess.

    Author a small resolution-record file at `.planning/phases/07-content-pass-+-launch/07-03-placeholder-resolutions.md` capturing Olive's answers verbatim — Task 2 applies from this file; future audit trail.
  </context>
  <options>
    <option id="resolve-with-values">
      <name>Olive supplies all 4 values</name>
      <pros>Plan completes cleanly; all surfaces consistent; no remaining placeholders at deploy</pros>
      <cons>Requires Olive's attention now (~5 min)</cons>
    </option>
    <option id="resolve-with-omissions">
      <name>Olive marks some as "omit, ship without"</name>
      <pros>Honest — no fabricated URL or title</pros>
      <cons>Must replace PLACEHOLDER comments with explicit "intentionally omitted" rationale + remove from .ts file's header comment list; surfaces (footer, contact-stack) need the URL constant removed entirely or restructured</cons>
    </option>
    <option id="defer-to-v1.1">
      <name>Olive defers one or more to v1.1 (post-launch)</name>
      <pros>Doesn't block launch on missing values</pros>
      <cons>Per 07-CONTEXT.md "Graceful degradation": placeholders that remain at deploy time MUST be tracked in PROJECT.md Active + flagged as HTML comments in rendered source. Increases launch-day documentation work.</cons>
    </option>
  </options>
  <action>
    PAUSE for Olive's input. Surface the decision block above to the user with the resume-signal example below. After Olive replies, write the verbatim answers to `.planning/phases/07-content-pass-+-launch/07-03-placeholder-resolutions.md` (Task 2 reads from this file). DO NOT proceed to Task 2 without explicit answers for all 4 items — fabricated URLs / guessed handles are forbidden.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-content-pass-+-launch/07-03-placeholder-resolutions.md &amp;&amp; grep -q "(a) LinkedIn" .planning/phases/07-content-pass-+-launch/07-03-placeholder-resolutions.md &amp;&amp; grep -q "(b) Fathom" .planning/phases/07-content-pass-+-launch/07-03-placeholder-resolutions.md &amp;&amp; grep -q "(c) Stemz" .planning/phases/07-content-pass-+-launch/07-03-placeholder-resolutions.md &amp;&amp; grep -q "(d) Aktiga" .planning/phases/07-content-pass-+-launch/07-03-placeholder-resolutions.md</automated>
  </verify>
  <resume-signal>
    Reply with a JSON-shaped or list-shaped block giving each of (a) LinkedIn handle, (b) Fathom repo URL, (c) Stemz live URL, (d) Aktiga role title — values OR "omit" OR "defer to v1.1". Example:

    ```
    (a) LinkedIn: https://linkedin.com/in/olive-elliott  ← confirm assumed slug
    (b) Fathom repo: https://github.com/olivelliott/fathom  ← confirmed public
    (c) Stemz live: omit (no current live URL; site is local WordPress)
    (d) Aktiga role: "Software Engineer / System Architect / Project Lead" ← keep as-is
    ```
  </resume-signal>
  <acceptance_criteria>
    - User provides explicit resolution (value OR "omit" OR "defer to v1.1") for each of 4 items
    - `.planning/phases/07-content-pass-+-launch/07-03-placeholder-resolutions.md` exists and records each answer verbatim
    - No fabrication: if Olive can't confirm, the answer is "omit" or "defer", NOT a guessed URL
  </acceptance_criteria>
  <done>
    Olive has answered all 4 questions; the resolutions doc is filed; Task 2 has everything it needs to apply the values atomically.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply confirmed resolutions across resume.ts + contact-stack.tsx + footer.tsx (+ fathom.mdx if applicable)</name>
  <files>
    content/resume.ts
    components/about/contact-stack.tsx
    components/site/footer.tsx
    content/projects/fathom.mdx
  </files>
  <read_first>
    - .planning/phases/07-content-pass-+-launch/07-03-placeholder-resolutions.md (Olive's answers from Task 1)
    - content/resume.ts (current state — locate exact line numbers; surgery only)
    - components/about/contact-stack.tsx (LINKEDIN_URL constant)
    - components/site/footer.tsx (LINKEDIN_URL constant)
    - content/projects/fathom.mdx (current frontmatter — if Fathom repo URL confirmed, add `links.repo: <url>`)
    - lib/schemas.ts § ResumeHeaderSchema + ResumeProjectSchema + LinksSchema (validate any edits against the schema BEFORE saving)
  </read_first>
  <action>
    Apply each resolution. For each of the 4 placeholders:

    **(a) LinkedIn handle:**
    - If CONFIRMED with a value: update in 3 places — `content/resume.ts` line ~42 (`linkedin: '<url>'`), `components/about/contact-stack.tsx` line ~12 (`const LINKEDIN_URL = '<url>'`), `components/site/footer.tsx` line ~23 (`const LINKEDIN_URL = '<url>'`). Remove the PLACEHOLDER comments above each constant.
    - If "omit": remove the entire LinkedIn entry from all three places. In contact-stack.tsx, remove the third `<li>` block. In footer.tsx, remove the third `<li>` block in the icon row. In resume.ts, change schema (lib/schemas.ts ResumeHeaderSchema.links.linkedin) to `.optional()` THEN remove the field from data. Document why in commit message.
    - If "defer to v1.1": leave URL value as-is (Phase 1 placeholder slug) but REPLACE the `PLACEHOLDER:` comment with an explicit `DEFERRED-V1.1:` comment naming the v1.1 owner. Add a todo line to PROJECT.md § Active.

    **(b) Fathom repo URL:**
    - If CONFIRMED: in `content/resume.ts` line ~86 (Fathom project), add `link: '<url>',` after `tagline:`. ALSO add to `content/projects/fathom.mdx` frontmatter (currently has empty links): change `# links: omitted` (or `links: {}`) to:
      ```yaml
      links:
        repo: <url>
      ```
    - If "omit": remove the PLACEHOLDER comment from resume.ts line 86 (the Fathom block has no `link` currently — confirm that's the intentional state) and add a `// Fathom is public but repo URL intentionally not surfaced — Olive's call` comment.
    - If "defer": replace PLACEHOLDER with DEFERRED-V1.1 comment.

    **(c) Stemz live URL:**
    - Symmetric to Fathom — add `link: '<url>'` to resume.ts § projects[Stemz] OR remove placeholder + add intentional-omission comment OR DEFERRED-V1.1.

    **(d) Aktiga role title:**
    - If CONFIRMED unchanged: just remove any PLACEHOLDER comment near experience[0].role.
    - If REVISED: update the `role:` string in `content/resume.ts` § experience[0].

    **After all edits:**
    1. Search for remaining "PLACEHOLDER" in the 3 (or 4) files: `grep -n "PLACEHOLDER" content/resume.ts components/about/contact-stack.tsx components/site/footer.tsx content/projects/fathom.mdx`. Result should be empty unless the user chose "defer to v1.1" — those become `DEFERRED-V1.1:` comments (allowed; grep filter for Plan 07-04 will distinguish).
    2. Run `pnpm typecheck` — exit 0 (catches schema mismatches at compile time).
    3. Run `pnpm vitest run` — full suite green. Particularly:
       - `tests/resume/*.test.ts` (Plan 05-01/02 resume content tests — may grep for specific URL strings; if so, update those tests too in the same commit)
       - `tests/about/*.test.tsx` (Plan 05-03 about page tests — may grep for LinkedIn URL)
       - `tests/site/footer.test.tsx` (Plan 05-04 footer tests — same)
       If any test fails on a hardcoded URL string, update the test to match the new URL (the tests assert presence, not specific slug) — but ONLY if it was a placeholder URL the test was locking in. Document in commit.
    4. Run `pnpm build` — exit 0; resume.pdf regenerated via postbuild; static pages emit.

    Commit message format: `feat(07-03): resolve Phase 5 placeholders (LinkedIn / Fathom URL / Stemz URL / Aktiga role)`. Body lists each of the 4 with the resolution (value confirmed / omitted / deferred) for git-archeology clarity.

    Note: if the resolutions include changes to resume bullets or schema-level edits (rare but possible if Aktiga role is significantly revised), update content/resume.ts only — the schema in lib/schemas.ts is locked unless Olive's "omit" answer requires LinkedIn to become `.optional()`.
  </action>
  <verify>
    <automated>pnpm vitest run --reporter=dot &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; PLACEHOLDER_COUNT=$(grep -c "PLACEHOLDER:" content/resume.ts components/about/contact-stack.tsx components/site/footer.tsx 2>/dev/null | awk -F: '{s+=$2} END {print s+0}') &amp;&amp; [ "$PLACEHOLDER_COUNT" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - All 4 placeholders resolved per Olive's answers (confirmed-with-value, omitted-with-rationale, or deferred-with-comment)
    - Zero `PLACEHOLDER:` comments remain in content/resume.ts, components/about/contact-stack.tsx, components/site/footer.tsx (DEFERRED-V1.1 comments allowed)
    - If LinkedIn URL changed: identical string appears in all 3 files (resume.ts, contact-stack.tsx, footer.tsx)
    - If Fathom repo confirmed: content/projects/fathom.mdx links.repo updated in same commit
    - `pnpm typecheck` exits 0
    - `pnpm vitest run` exits 0 (any test that asserted the old placeholder URL is updated to match the new value)
    - `pnpm build` exits 0; postbuild produces updated resume.pdf
    - Single commit `feat(07-03):` contains all related file changes (no split commits)
  </acceptance_criteria>
  <done>
    All Phase 5 placeholders resolved consistently across surfaces; surfaces stay in sync; build + tests + typecheck green; no surprise placeholders remain at deploy time.
  </done>
</task>

</tasks>

<verification>
- `grep -rn "PLACEHOLDER:" content/resume.ts components/about/contact-stack.tsx components/site/footer.tsx` returns 0 lines (DEFERRED-V1.1 OK)
- `pnpm typecheck` exit 0
- `pnpm vitest run` exit 0
- `pnpm build` exit 0; resume.pdf regenerated
- If LinkedIn URL changed: identical string in 3 files (grep + diff check)
- 07-03-placeholder-resolutions.md filed in phase directory (audit trail)
</verification>

<success_criteria>
1. All 4 Phase 5 placeholders have explicit resolutions (value, omission, or v1.1 deferral) — no soft PLACEHOLDER markers remain.
2. Three surfaces (resume.ts + contact-stack.tsx + footer.tsx) stay consistent on any URL that appears across them.
3. If Fathom repo URL confirmed, fathom.mdx frontmatter updated in same commit (no drift between resume + project page).
4. Schema validations pass at module load; build + tests + typecheck all green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-pass-+-launch/07-03-SUMMARY.md`
</output>
