---
phase: 07-content-pass-+-launch
plan: 04
type: execute
wave: 4
depends_on: [07-00, 07-01, 07-02, 07-03]
files_modified:
  - .planning/phases/06-seo,-og,-a11y-&-performance-audit/lighthouse-report.md
  - .planning/phases/07-content-pass-+-launch/deploy-checklist.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [DPL-02, DPL-03]
must_haves:
  truths:
    - "QAL-01 closed: pnpm lhci passes ≥ 0.90 on all 8 cells (4 axes × 2 routes) and lighthouse-report.md is filled in"
    - "DPL-02 closed: olivelliott.vercel.app (or actual assigned URL) returns 200 OK on all 7 public routes over HTTPS"
    - "DPL-03 closed: Vercel Analytics + Speed Insights dashboards show smoke-test pageviews + Web Vitals data within 30 min"
    - "deploy-checklist.md is fully ticked through; sign-off block filled with date + final URL"
    - "REQUIREMENTS.md updated: QAL-01, DPL-02, DPL-03 marked [x] Complete"
  artifacts:
    - path: ".planning/phases/06-seo,-og,-a11y-&-performance-audit/lighthouse-report.md"
      provides: "Filled-in score tables (8 cells), Status flipped from DEFERRED to PASSED"
      contains: "Performance"
    - path: ".planning/phases/07-content-pass-+-launch/deploy-checklist.md"
      provides: "Fully-ticked checklist with sign-off date + final URL recorded"
      contains: "Deploy approved by"
    - path: ".planning/REQUIREMENTS.md"
      provides: "QAL-01 + DPL-02 + DPL-03 marked complete (mirrored in traceability table)"
      contains: "[x] **QAL-01**"
  key_links:
    - from: "pnpm lhci local run"
      to: ".planning/phases/06-seo,-og,-a11y-&-performance-audit/lighthouse-report.md"
      via: "Olive transcribes scores into score tables; Status: DEFERRED → ✓ PASSED"
      pattern: "Status:.*PASSED"
    - from: "git push origin main"
      to: "Vercel GitHub integration → production deploy → olivelliott.vercel.app"
      via: "Vercel dashboard build (auto-triggered); first-deploy URL recorded in checklist"
      pattern: "olivelliott\\.vercel\\.app"
    - from: "Vercel dashboard Analytics + Speed Insights tabs"
      to: ".planning/phases/07-content-pass-+-launch/deploy-checklist.md § Analytics confirmation"
      via: "Olive ticks boxes after 30-min observation window"
      pattern: "Analytics confirmation"
---

<objective>
The launch plan. Three checkpoint:human-action gates execute in sequence: (1) Olive runs pnpm lhci locally and fills in lighthouse-report.md — resolves Phase 6's deferred QAL-01; (2) Olive triggers the Vercel production deploy via git push to main + GitHub-integration flow; (3) Olive performs the post-deploy smoke checklist on the live URL and confirms Analytics + Speed Insights are firing. Each gate is blocking — Plan 07-04 only completes when all 3 are signed off. After all three close, REQUIREMENTS.md is updated to mark QAL-01 + DPL-02 + DPL-03 complete.

Purpose: Phase 7 success criteria #1, #2, #5 — "reachable at final Vercel subdomain URL", "Vercel Analytics enabled and recording", "shareable URL publicly without caveats". Also closes Phase 6's QAL-01 deferral (the single outstanding requirement from the prior phase).
Output: A live site at olivelliott.vercel.app (or actual assigned URL); lighthouse-report.md filled in; deploy-checklist.md ticked through with sign-off; REQUIREMENTS.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-content-pass-+-launch/07-CONTEXT.md
@.planning/phases/07-content-pass-+-launch/deploy-checklist.md
@.planning/phases/06-seo,-og,-a11y-&-performance-audit/lighthouse-report.md
@.planning/phases/06-seo,-og,-a11y-&-performance-audit/06-04-SUMMARY.md
@package.json

<interfaces>
<!-- This plan is almost entirely human-action gated. The 3 checkpoints are -->
<!-- sequential and each pauses for Olive's interactive run on local Chrome / -->
<!-- Vercel dashboard / live URL. -->

Pre-deploy gate (Task 1):
- Command: `pnpm lhci` (defined in package.json scripts; wraps `pnpm build && pnpm dlx @lhci/cli@0.15.1 autorun --config=./lighthouserc.json`)
- Routes audited: `/` and `/projects/myco` (per lighthouserc.json)
- Acceptance threshold: ≥ 0.90 on all 4 categories × 2 routes = 8 cells
- Report destination: `.planning/phases/06-seo,-og,-a11y-&-performance-audit/lighthouse-report.md` § Scores
- Fallback path if any cell < 0.90: file a gap-closure micro-plan (likely surfaces per Phase 6 SUMMARY: hero image priority/sizes, font-display, motion island bundle) and re-run before deploy

Deploy gate (Task 2):
- Mechanism: Vercel GitHub integration (Olive sets up in Vercel dashboard if first time, then `git push origin main` triggers production build)
- Build command (default): `pnpm install && pnpm build` — honors `postbuild` hook that regenerates resume.pdf via Puppeteer
- Subdomain: `olivelliott.vercel.app` (Vercel default; falls back to Vercel-assigned slug if collision)
- Expected outputs: green Vercel build log, deployment URL accessible over HTTPS

Post-deploy gate (Task 3):
- Live-URL smoke per `.planning/phases/07-content-pass-+-launch/deploy-checklist.md § Post-Deploy Smoke` (7 routes, OG endpoint, resume.pdf, sitemap.xml, robots.txt, favicon)
- Analytics confirmation: Vercel dashboard → Analytics tab + Speed Insights tab, within 30 min of first smoke pageviews

CRITICAL: Claude does NOT push to remote. Claude does NOT run lhci autonomously (per 07-CONTEXT.md decision: "lhci needs a real Chrome instance + spawned production server, more reliable to run interactively on Olive's local machine"). Claude does NOT click in the Vercel dashboard. All three are Olive's actions, surfaced via blocking checkpoints.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Olive runs pnpm lhci locally + fills in lighthouse-report.md (resolves Phase 6 QAL-01 deferral)</name>
  <files>
    .planning/phases/06-seo,-og,-a11y-&-performance-audit/lighthouse-report.md
    .planning/phases/07-content-pass-+-launch/deploy-checklist.md
  </files>
  <read_first>
    - .planning/phases/06-seo,-og,-a11y-&-performance-audit/lighthouse-report.md (current state — Status: DEFERRED; 8 empty score cells; manual checklists)
    - .planning/phases/06-seo,-og,-a11y-&-performance-audit/06-04-SUMMARY.md § "Lighthouse audit (QAL-01) — DEFERRED to Phase 7 launch-week" (rationale + acceptance criteria)
    - .planning/phases/07-content-pass-+-launch/deploy-checklist.md § "Pre-Deploy Gate — 2. Lighthouse audit"
    - package.json § scripts.lhci (`pnpm build && pnpm dlx @lhci/cli@0.15.1 autorun --config=./lighthouserc.json`)
  </read_first>
  <action>
    PAUSE. This is an Olive-action — Claude cannot run lhci reliably.

    Surface to the user:

    > **Pre-deploy gate: Lighthouse audit (QAL-01)**
    >
    > Phase 6 deferred this run to Phase 7 launch-week. Infrastructure is ready — your action:
    >
    > **Step 1 — Run lhci locally:**
    > ```bash
    > # If Chrome isn't on PATH, prefix with:
    > # CHROME_PATH=$(node -e 'console.log(require("puppeteer").executablePath())') \
    > pnpm lhci
    > ```
    > This runs `pnpm build` then spawns `pnpm start` on port 3000 and audits `/` + `/projects/myco` against the 4 categories at the 0.90 threshold (per `lighthouserc.json`).
    >
    > **Step 2 — Read the output.** Outputs go to `./lighthouse-reports/` (gitignored). If lhci exits 0, all 8 cells are ≥ 0.90 — you're good. If it exits non-zero, at least one cell is below.
    >
    > **Step 3 — Fill in `.planning/phases/06-seo,-og,-a11y-&-performance-audit/lighthouse-report.md`:**
    > - Update `Status:` line: `⚠️ DEFERRED` → `✓ PASSED — <date>` (or `✗ FAILED — see gap-closure plan`)
    > - Fill the 8 score cells under § Scores → `/` and `/projects/myco`
    > - Update `Run date:` and `Build SHA:` (the build SHA is the `git rev-parse HEAD` at the time of run)
    > - Optionally tick the supplementary keyboard, reduced-motion, and anti-features visual checklists
    >
    > **Step 4 — Tick `.planning/phases/07-content-pass-+-launch/deploy-checklist.md § 2. Lighthouse audit`:**
    > - [ ] `pnpm lhci` runs cleanly
    > - [ ] All 8 cells ≥ 0.90
    > - [ ] Score table filled; Status flipped
    >
    > **If any cell < 0.90:**
    > Pause launch. Reply with the failing axis (e.g. "Performance on / = 0.85; LCP issue") and we'll file a gap-closure micro-plan before deploy. Likely surfaces per Phase 6 SUMMARY: hero image priority/sizes, font-display, motion island bundle size.
    >
    > **Reply:**
    > - **"lhci passed"** + paste the 8 score values → I commit the report + checklist update, then proceed to Task 2 (deploy).
    > - **"lhci failed: <details>"** → I file a gap-closure plan and we re-run.
    > - **"skip"** → I record this as accepted-with-deferral; QAL-01 stays unchecked in REQUIREMENTS.md (NOT recommended — violates Phase 7 success criterion).

    **After Olive replies with passing scores:** Commit `.planning/phases/06-seo,-og,-a11y-&-performance-audit/lighthouse-report.md` + `.planning/phases/07-content-pass-+-launch/deploy-checklist.md` with message: `docs(07-04): close QAL-01 — lhci passed (Perf=X, A11y=Y, BP=Z, SEO=W on /; ... on /projects/myco)`.
  </action>
  <verify>
    <automated>grep -q "Status:.*PASSED" .planning/phases/06-seo,-og,-a11y-\&-performance-audit/lighthouse-report.md &amp;&amp; grep -q "✓ PASSED\|✗ FAILED" .planning/phases/06-seo,-og,-a11y-\&-performance-audit/lighthouse-report.md</automated>
  </verify>
  <instructions>
    Olive: this is the launch-week task that Phase 6 deferred. Plan 06-04 SUMMARY explains why deferred (lhci needs interactive local Chrome). Infrastructure is fully ready; only the empirical run is pending. Estimated time: 5-15 min (3 min for the lhci run + a couple minutes to fill in the report).

    If any score falls below 0.90, we DO NOT proceed to deploy. We file a gap-closure plan and re-run. The 0.90 threshold is the locked acceptance criterion from Phase 6 CONTEXT.md.
  </instructions>
  <resume-signal>
    Reply with "lhci passed" + the 8 scores, OR "lhci failed: &lt;details&gt;", OR "skip"
  </resume-signal>
  <acceptance_criteria>
    - `pnpm lhci` was executed on Olive's local machine
    - All 8 score cells in `lighthouse-report.md` are populated AND all ≥ 0.90 (per locked threshold)
    - `lighthouse-report.md` Status line updated from DEFERRED to ✓ PASSED with date + build SHA
    - `deploy-checklist.md` § 2 Lighthouse audit boxes ticked
    - Both files committed: `docs(07-04): close QAL-01 — lhci passed`
  </acceptance_criteria>
  <done>
    Lighthouse audit complete and passing; report filled in; QAL-01 satisfied. (REQUIREMENTS.md update batched into Task 4 at end of plan.)
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Olive triggers Vercel production deploy via git push origin main (DPL-02)</name>
  <files>
    .planning/phases/07-content-pass-+-launch/deploy-checklist.md
  </files>
  <read_first>
    - .planning/phases/07-content-pass-+-launch/deploy-checklist.md § "Deploy — 5. Vercel project setup" + "6. First production deploy"
    - .planning/phases/07-content-pass-+-launch/07-CONTEXT.md § "Vercel Deploy Process"
    - package.json § scripts.build + scripts.postbuild (default-honored by Vercel)
  </read_first>
  <action>
    PAUSE. This is an Olive-action — Claude does not push to remote, does not click in Vercel dashboard.

    Surface to the user:

    > **Deploy gate (DPL-02): Vercel production deploy**
    >
    > Pre-deploy gate (Task 1) is closed. Time to ship.
    >
    > **Step 1 — Vercel project setup** (skip if already done):
    > - Vercel dashboard → New Project → import `olivelliott/portfolio`
    > - Build command: `pnpm build` (default; honors `postbuild` for resume.pdf)
    > - Output directory: `.next` (default)
    > - Install command: `pnpm install`
    > - Production branch: `main`
    > - No environment variables required for v1 (no secrets, no API keys)
    > - Subdomain: accept default `olivelliott.vercel.app` (or note the actual assigned slug)
    >
    > Tick: deploy-checklist.md § 5.
    >
    > **Step 2 — Push to main:**
    > ```bash
    > git status                  # confirm clean tree (all Phase 7 commits landed)
    > git push origin main        # triggers Vercel build automatically
    > ```
    >
    > **Step 3 — Watch the Vercel build log:**
    > - Build should complete in < 3 min
    > - Look for: `pnpm install` success, `pnpm build` success (including postbuild PDF generation), static page emit count matches local build, no warning/error spikes
    >
    > **Step 4 — Record actual URL** in `deploy-checklist.md § 6 — Deployment URL: _______________________________`
    >
    > Tick: deploy-checklist.md § 6.
    >
    > **Reply:**
    > - **"deployed: &lt;url&gt;"** — record URL, I commit the checklist update, then proceed to Task 3 (post-deploy smoke).
    > - **"build failed: &lt;details&gt;"** — pause, file a gap-closure plan, re-push.
    > - **"abort"** — pause launch; we re-evaluate.

    **After Olive replies with the URL:** Commit `deploy-checklist.md` with message: `docs(07-04): record Vercel deploy at <url> (DPL-02 closed)`.
  </action>
  <verify>
    <automated>grep -qE "Deployment URL:\s*\S+vercel\.app" .planning/phases/07-content-pass-+-launch/deploy-checklist.md || grep -qE "Final live URL:\s*\S+" .planning/phases/07-content-pass-+-launch/deploy-checklist.md</automated>
  </verify>
  <instructions>
    Olive: this is the one-way door. After `git push origin main`, Vercel auto-deploys. Confirm pre-deploy gate (Task 1) is genuinely closed before pushing. Estimated time: 2 min for the push + 3 min for the build to complete.

    NEVER use `--force` or `--no-verify` on the push. NEVER skip the build. If the Vercel build fails, the auto-deploy doesn't promote — but resolving the failure becomes a Phase 7 gap-closure plan.
  </instructions>
  <resume-signal>
    Reply with "deployed: &lt;url&gt;", "build failed: &lt;details&gt;", or "abort"
  </resume-signal>
  <acceptance_criteria>
    - Vercel project exists and is connected to `olivelliott/portfolio` GitHub repo
    - `git push origin main` succeeded
    - Vercel production build completed without errors
    - Live URL recorded in `deploy-checklist.md § 6`
    - `deploy-checklist.md` § 5 + § 6 boxes ticked
    - Commit `docs(07-04): record Vercel deploy at <url>` filed
  </acceptance_criteria>
  <done>
    Site is live at the recorded Vercel URL; build artifacts (including resume.pdf via postbuild) are served. (DPL-02 will be marked complete in Task 4 after post-deploy smoke confirms reachability.)
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Olive runs post-deploy smoke + confirms Analytics + Speed Insights firing (DPL-02 + DPL-03 close)</name>
  <files>
    .planning/phases/07-content-pass-+-launch/deploy-checklist.md
  </files>
  <read_first>
    - .planning/phases/07-content-pass-+-launch/deploy-checklist.md § "Post-Deploy Smoke — 7. Live-URL verification" + "8. Analytics confirmation" + "9. Anti-features visual gate" + "Sign-off"
    - The recorded live URL from Task 2
  </read_first>
  <action>
    PAUSE. This is an Olive-action — Claude cannot drive a browser session against the live URL or inspect the Vercel dashboard.

    Surface to the user:

    > **Post-deploy gate (DPL-02 confirm + DPL-03 close): Live-URL smoke**
    >
    > Live URL: `<url from Task 2>`
    >
    > Execute `deploy-checklist.md § Post-Deploy Smoke` end-to-end. Three sub-sections:
    >
    > **Step 1 — § 7 Live-URL verification** (~5 min):
    > Walk all 7 routes (`/`, `/about`, `/projects`, `/projects/myco`, `/projects/fathom`, `/projects/agenda-keeper`, `/resume`) + 5 endpoint smoke (`/resume.pdf`, `/opengraph-image`, `/sitemap.xml`, `/robots.txt`, favicon).
    >
    > Useful curl checks:
    > ```bash
    > URL="<live URL>"
    > for path in / /about /projects /projects/myco /projects/fathom /projects/agenda-keeper /resume; do
    >   echo "== $path =="
    >   curl -sI "$URL$path" | head -1
    > done
    > curl -sI "$URL/resume.pdf" | head -3       # expect 200 + Content-Type: application/pdf + ~240KB
    > curl -sI "$URL/opengraph-image"            # expect 200 + image/png
    > curl -s   "$URL/sitemap.xml" | head -20    # expect <?xml + <urlset
    > curl -sI "$URL/robots.txt"                 # expect 200
    > ```
    >
    > Manual visual checks:
    > - `/projects/agenda-keeper` shows visible "code private" label AND no repo URL in source (View Source → search for "github.com" — should not appear scoped to agenda-keeper)
    >
    > Tick each box in deploy-checklist.md § 7.
    >
    > **Step 2 — § 8 Analytics confirmation** (~30 min wait):
    > - Visit 3+ live routes to generate pageview events
    > - Vercel dashboard → your project → Analytics tab → confirm pageviews appear (typical lag: 1-30 min)
    > - Vercel dashboard → Speed Insights tab → confirm Web Vitals data accumulating (typical lag: 1-30 min)
    >
    > Tick deploy-checklist.md § 8.
    >
    > **Step 3 — § 9 Anti-features visual gate** (~3 min):
    > Visual sanity over live `/`, `/projects`, `/projects/myco`, `/about`, `/resume`. Reference list in `lighthouse-report.md § 19-Item Anti-Features Visual Check`.
    >
    > Tick deploy-checklist.md § 9.
    >
    > **Step 4 — Sign-off block:**
    > Fill in deploy-checklist.md § Sign-off:
    > - **Deploy approved by:** Olive Elliott
    > - **Date:** YYYY-MM-DD
    > - **Final live URL:** `<url>`
    > - **Notes / known issues:** (anything not blocking the public-share announcement)
    >
    > **Reply:**
    > - **"smoke passed"** + brief note (any minor known issues OK) → I commit the final checklist + run Task 4 (mark requirements complete).
    > - **"smoke failed: &lt;blocker&gt;"** → file a fix-forward gap-closure plan (per 07-CONTEXT.md "Fix-forward policy": small fixes go via main push; anything that breaks the home page or 404s a key route blocks the public-share announcement).
  </action>
  <verify>
    <automated>grep -q "Deploy approved by:" .planning/phases/07-content-pass-+-launch/deploy-checklist.md &amp;&amp; grep -qE "Date:\s*[0-9]{4}" .planning/phases/07-content-pass-+-launch/deploy-checklist.md</automated>
  </verify>
  <instructions>
    Olive: this is the validation that the deploy actually worked end-to-end. Two important nuances per 07-CONTEXT.md:
    - **Fix-forward policy:** cosmetic issues can be filed as known-issue notes without blocking sign-off. Only home-page breakage or 404s on key routes block.
    - **Analytics lag:** Vercel Analytics + Speed Insights both can take up to 30 min to surface initial pageviews. That's expected, not a failure.

    Estimated time: 10 min active + 30 min waiting on Analytics dashboard (can multitask).
  </instructions>
  <resume-signal>
    Reply with "smoke passed" + brief note, OR "smoke failed: &lt;blocker&gt;"
  </resume-signal>
  <acceptance_criteria>
    - All 7 public routes return 200 OK over HTTPS (or documented exception)
    - resume.pdf serves at 200 OK with Content-Type: application/pdf
    - /opengraph-image, /sitemap.xml, /robots.txt all reachable
    - /projects/agenda-keeper renders with visible "code private" label and zero repo URL in HTML
    - Vercel Analytics dashboard shows ≥ 1 pageview within 30 min of smoke
    - Vercel Speed Insights dashboard shows ≥ 1 Web Vitals data point within 30 min
    - deploy-checklist.md § 7 + § 8 + § 9 + Sign-off block all filled in
    - Commit `docs(07-04): post-deploy smoke passed; site live at <url>` filed
  </acceptance_criteria>
  <done>
    Site is verified live and reachable; Analytics + Speed Insights are confirmed firing; DPL-02 + DPL-03 satisfied; deploy-checklist.md is fully ticked and signed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Mark QAL-01 + DPL-02 + DPL-03 complete in REQUIREMENTS.md</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — DPL-02 + DPL-03 are [ ] Pending; QAL-01 marked "Deferred to Phase 7 launch-week")
    - .planning/phases/06-seo,-og,-a11y-&-performance-audit/lighthouse-report.md (confirms Task 1 closed QAL-01)
    - .planning/phases/07-content-pass-+-launch/deploy-checklist.md (confirms Tasks 2 + 3 closed DPL-02 + DPL-03)
  </read_first>
  <action>
    Update `.planning/REQUIREMENTS.md` to reflect the three closed requirements.

    **Edits:**
    1. Find line `- [ ] **QAL-01**: Lighthouse scores ≥ 90 ...` → change to `- [x] **QAL-01**: Lighthouse scores ≥ 90 ...`
    2. Find line `- [ ] **DPL-02**: Site is reachable at a Vercel subdomain ...` → change to `- [x] **DPL-02**: Site is reachable at a Vercel subdomain ...`
    3. Find line `- [ ] **DPL-03**: Vercel Analytics is enabled ...` → change to `- [x] **DPL-03**: Vercel Analytics is enabled ...`

    **Traceability table updates** (bottom of REQUIREMENTS.md):
    4. `| QAL-01 | Phase 6 ... | Deferred to Phase 7 ... |` → `| QAL-01 | Phase 6 — SEO, OG, A11y & Performance Audit | Complete (closed in Phase 7 Plan 07-04) |`
    5. `| DPL-02 | Phase 7 — Content Pass + Launch | Pending |` → `| DPL-02 | Phase 7 — Content Pass + Launch | Complete |`
    6. `| DPL-03 | Phase 7 — Content Pass + Launch | Pending |` → `| DPL-03 | Phase 7 — Content Pass + Launch | Complete |`

    Verify:
    - `grep "QAL-01" .planning/REQUIREMENTS.md` shows both the checkbox + traceability are marked complete
    - `grep "DPL-02" .planning/REQUIREMENTS.md` same
    - `grep "DPL-03" .planning/REQUIREMENTS.md` same
    - Run `pnpm vitest run` — should still be green (this is a docs-only edit; no code impact)

    Commit message: `docs(07-04): close QAL-01 + DPL-02 + DPL-03 — Phase 7 + Milestone v1.0 launch complete`. Body lists the live URL + lhci pass date + analytics-confirmation date.
  </action>
  <verify>
    <automated>grep -q "^- \[x\] \*\*QAL-01\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^- \[x\] \*\*DPL-02\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^- \[x\] \*\*DPL-03\*\*" .planning/REQUIREMENTS.md &amp;&amp; pnpm vitest run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md QAL-01 checkbox: `[x]`
    - REQUIREMENTS.md DPL-02 checkbox: `[x]`
    - REQUIREMENTS.md DPL-03 checkbox: `[x]`
    - Traceability table reflects same statuses
    - `pnpm vitest run` exit 0
    - Commit `docs(07-04): close QAL-01 + DPL-02 + DPL-03` filed
  </acceptance_criteria>
  <done>
    All three Phase-7-blocking requirements are formally closed in REQUIREMENTS.md; Phase 7 verification can run; milestone v1.0 audit + complete-milestone + cleanup lifecycle is unblocked per 07-CONTEXT.md § "Milestone v1.0 lifecycle".
  </done>
</task>

</tasks>

<verification>
- lighthouse-report.md Status: ✓ PASSED with 8 cells filled
- deploy-checklist.md Sign-off block filled with date + final URL
- REQUIREMENTS.md QAL-01, DPL-02, DPL-03 all marked [x]
- Live site at recorded Vercel URL reachable over HTTPS
- Vercel Analytics + Speed Insights dashboards showing data
- `pnpm vitest run` exit 0 (no regressions from docs edits)
</verification>

<success_criteria>
1. QAL-01 closed: lhci ≥ 0.90 across all 8 cells (Phase 6 deferral resolved).
2. DPL-02 closed: site live at olivelliott.vercel.app (or actual URL); all 7 public routes return 200 OK over HTTPS.
3. DPL-03 closed: Vercel Analytics + Speed Insights confirmed firing.
4. deploy-checklist.md fully ticked through with sign-off block populated.
5. REQUIREMENTS.md updated to reflect closure of QAL-01, DPL-02, DPL-03.
6. Phase 7 success criteria #1, #2, #5 satisfied; milestone v1.0 verification unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-pass-+-launch/07-04-SUMMARY.md`
</output>
